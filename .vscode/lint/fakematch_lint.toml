# All reported pragmas are visible warnings, not permission to add scaffolding.
# Use --warnings-as-errors to make these diagnostics build-breaking.
schema_version = 1
exceptions = []
pragma_allowlist = []
