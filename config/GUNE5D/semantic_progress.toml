schema_version = 1

[units."game/g3d/g3dMath3D.cpp"]
status = "structurally_verified"
summary = "All original functions are reconstructed; the sole text residual is a register-coloring rotation in mat44InvBasis."
evidence = [
    "30 functions are byte-exact or relocation-only",
    "mat44InvBasis has identical instruction order and non-register operands",
]

[units."game/g3d/sndvoice.c"]
status = "structurally_verified"
summary = "The complete voice API is reconstructed; sndVoiceUpdateAll differs only in register coloring."
evidence = [
    "All eight original functions are present",
    "sndVoiceUpdateAll has 822/822 instructions with identical non-register operands",
]

[units."game/crt/vsprintf.c"]
status = "reconstructed"
summary = "The complete formatting implementation is present, including floating-point formatting and the binary conversion."
evidence = [
    "All five original functions are present",
    "Remaining residuals are isolated to cvt scheduling and vsprintf prologue/control-flow review",
]

[units."game/pb/pb_window.c"]
status = "reconstructed"
summary = "All original window, projection, viewport, camera, and clipping functions are represented in source."
evidence = [
    "All fifteen original functions are present",
    "Eleven functions are byte-exact or relocation-only",
]

[units."game/ps2/fakelib.c"]
status = "reconstructed"
summary = "The complete GameCube implementation of the PS2 compatibility API is represented in source."
evidence = [
    "All 48 original functions are present",
    "45 functions are byte-exact or relocation-only",
    "The remaining review is confined to fn_800AEBF4, sceRead, and sceOpen",
]
