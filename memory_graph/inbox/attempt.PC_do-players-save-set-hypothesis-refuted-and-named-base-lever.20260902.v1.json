{
  "attempted_axis": "Execute the mandatory step-1 hypothesis of attempt.PC_do-players-inplace-update-and-symbol-unification.20260902.v1 (ours saves 17 GPRs to the target's 16 and the spare callee-saved register IS the cached potionicon_tab+0xC40 web; the lever is reducing the declared-local set). The hypothesis was REFUTED by its own named criterion AND by a free register census that misidentified the register. Three unrelated keeps then paid: sinking a cached member read to its use site, and naming the record base WITHOUT the 0xC40 block offset at two call sites.",
  "attributes": {
    "law_screen": "claim.law.PC_a-named-pointer-local-selects-the-displacement-load-and-its-declaration-site-decides-the-hoist.20260902.v1 SCREENED, its CLEAN-CASE clause CONFIRMED at two NEW sites and its SECOND CLAUSE INDEPENDENTLY RE-CONFIRMED by a form it had not been tested against. The clean-case clause ('a site whose enclosing block is entered unconditionally on the path in question') predicted probes D and E, which are the run's two largest wins: at the fn_8009D610 call site and at the speech round-robin loop head the target's `add` is preceded by NO guard, the named base could be declared at block top, and both flipped to the target's add+displacement form. The second clause governs the twin j-loops, where the target's `add` sits BELOW the `j != i` guard (verified at fnasm 0xee8:0xf08 --diff: `beq ->f18` precedes `add r4,r31,r3`), and the law's own falsifier invited reproducing a positive there from any source form. Probes F and G close that invitation: G (declare-and-initialise at loop-body top) DID flip the form -- multiset 14t -> 12t, the ours-only lwzx pair gone -- but fresh fuzzy FELL 97.2692 -> 96.8433 because the `add` hoisted above the guard, reproducing the law's recorded numbers in a new alignment; F (declare at loop-body top, ASSIGN inside the guard, a combination neither probe C nor D of the prior run tested) did not flip the form at all and regressed real 840 -> 864. claim.law.cached-base-alias-defeats-rematerialized-index-base.20260831.v1 SCREENED and its cure MEASURED NOT TO REACH here: it names a retained `T* p = &arr[i]` alias as the whole-function defeater, and probe B removed exactly that alias for one loop by block-scoping and renaming the walked pointer -- object bytes came back BYTE-IDENTICAL, so the alias's SCOPE and NAME are not the lever and the law's procedure does not transfer from a single element's field cluster to a loop induction pointer. claim.law.constant-copy-vs-rematerialize-is-source-unreachable.20260831.v1 SCREENED and found OUT OF SCOPE rather than applied, which corrects the superseded record's use of it as a neighbouring dead-axis veto: its scope clause requires two locals taking the same small integer CONSTANT with the target COPYING and our build REMATERIALISING, whereas the +0x5cc/+0x678 sites are a relocated ADDRESS base with the direction inverted (target rematerialises, ours copies). Reading it as a veto on the address-base family would have closed this run's whole winning axis. claim.law.PL_permutation-pin-freezes-every-function-preceding-it-in-source-order.20260901.v1 APPLIED and HELD on all four count-changing probes; each was cleared by re-deriving do_exit's relocation hashes with all four body hashes returning byte-identical. claim.law.PC_real-and-multiset-agreement-does-not-license-a-keep-without-fresh-fuzzy.20260902.v1 and claim.law.IH_multiset-gain-does-not-imply-fuzzy-gain-arbitrate-with-count-distance.20260901.v1 SCREENED and both load-bearing: probe E moved the multiset at equal counts and defake_gate flagged it IMPROVED-CARRIER pending fuzzy, and probe G was a multiset GAIN that fuzzy rejected. claim.law.real-can-underweight-a-large-alignment-gain SCREENED via probe.py's slot map, which is what surfaced the save-set delta this record refutes.",
    "laws_applied": "[\"claim.law.PC_a-named-pointer-local-selects-the-displacement-load-and-its-declaration-site-decides-the-hoist.20260902.v1\", \"claim.law.PL_permutation-pin-freezes-every-function-preceding-it-in-source-order.20260901.v1\", \"claim.law.IH_multiset-gain-does-not-imply-fuzzy-gain-arbitrate-with-count-distance.20260901.v1\"]",
    "probed_form": "PROBE A (MANDATED STEP 1, REVERTED) src/game/game/player.c: deleted `f32 best;` from the function-scope declaration block and `best = 9999.0f;` from the initialisation run, and re-declared both at the top of the `if (lbl_803447B8 == 0 || lbl_8034481C != 0 || opt_restart_request != 0) {` block as `f32 best;` followed by `best = 9999.0f;` ahead of `lbl_80344804 = 0;`. `it` was NOT moved: it is read at `do_weakening(p, it == i)` in a later top-level loop, outside the block that writes it, so scoping it would change semantics -- the superseded record's hypothesis names `it` and `best` jointly without this constraint. PROBE B (REVERTED) the get_actual_screen_pos loop: wrapped it in a bare block declaring its own `Player* q;` and renamed every `p` inside the loop to `q`, removing that loop from the function-scope `p` web. PROBE C (KEPT, commit 791ebcd30) deleted the `state = p->state;` assignment standing ahead of the selected-flag block, and changed its single consumer `if (state == 1) {` to `if (p->state == 1) {`. PROBE D (KEPT, commit bf25848f4) the fn_8009D610 call: `fn_8009D610(0, PT(i)->col_pos);` -> `u8* prec = (u8*)potionicon_tab + i * PREC_STRIDE;` declared at the top of the enclosing `if (i < 4 && gGameMode != MG_SHOP)` block, then `fn_8009D610(0, ((Player*)(prec + 0xC40))->col_pos);`. PROBE E (KEPT, commit 52a9dd15f) the speech round-robin loop head: `Player* speaker = PT(k);` -> `u8* krec = (u8*)potionicon_tab + k * PREC_STRIDE;` followed by `Player* speaker = (Player*)(krec + 0xC40);`, and the guard `if (speaker->speech_req != NULL) {` -> `if (PF(krec, 0xC40 + offsetof(Player, speech_req), s32*) != NULL) {`. PROBE F (REVERTED) BOTH twin j-loops: added `u8* jrec;` beside the existing `s32 st;` at loop-body top and split the short-circuit chain into `if (j != i) { jrec = (u8*)potionicon_tab + j * PREC_STRIDE; if ((st = PF(jrec, 0xC40 + 0xE8, s32)) != 0 && st != 2 && st != 3) { break; } }`. PROBE G (REVERTED) BOTH twin j-loops: `u8* jrec = (u8*)potionicon_tab + j * PREC_STRIDE;` declared AND initialised at loop-body top with the original single chain retained as `if (j != i && (st = PF(jrec, 0xC40 + 0xE8, s32)) != 0 && st != 2 && st != 3) {`.",
    "residual": "14 multiset tokens and a 2-instruction count deficit remain, down from 25 --ops clusters to 21. The REMAINING seven clusters (+0x6c, +0x22c, +0x334, +0x464, +0x5cc-0x5e8, +0x678, +0x1174) are one mechanism, the cached-versus-rematerialised potionicon_tab+0xC40 web, now positively localised to our r20 (and the walked r18) rather than to r15 as the superseded record claimed; this record's hypothesis names the untried cure. The twin lwzx pair at +0xef8/+0xfd4 is CLOSED: three declaration forms have now been measured there, the form flips only when the `add` hoists above the `j != i` guard, and fuzzy rejects that every time. The char_stats lfsx/stfsx pair at +0x9f4 remains source-exhausted per the superseded record's probes I, L and M. +0x430 is ours CSE-ing an lbl_8034481C global load that the target re-loads, the same CSE-scope family, refuted as a pragma-reachable axis by the superseded record's probe N. +0x1118 is the constant-copy-versus-rematerialise fold, and unlike the address-base family it IS in scope for claim.law.constant-copy-vs-rematerialize-is-source-unreachable. The IMMEDIATE rows are the anonymous-pool naming (@1125, @1126, @1132, @303, @309, @310) that a NonMatching TU carries by construction, plus the frame and save-area rows.",
    "scope": "src/game/game/player.c (three commits: 791ebcd30, bf25848f4, 52a9dd15f) plus config/GUNE5D/webfrank.json do_exit before/after_relocations_sha256, re-derived on each count-changing probe and landing at 4e3e9490ce95abbf443f038f901bd531a1ad54e3d6fe12047e7444ba7f5afce8 in both slots. No header edit, no configure.py edit, no new WebFrank rule, no new pragma or volatile scaffold -- the TU's scaffold census is unchanged at 31 rows.",
    "verification": "SESSION BASELINE re-measured live at commit cf81a9103, NOT quoted from the work order: probe.py BASELINE real 1154, insns T1174/O1172, multiset 14t, and probe.py --fuzzy 96.3552% from a report whose build printed `build/GUNE5D/main.dol: OK` -- this independently confirms the work order's figure. PROBE A: REGRESSED real 1154 -> 1182, and DECISIVELY, probe.py's slot map still read `!! SAVE-SET DELTA: target r16 vs ours r15` with `frame: target 256 ours 264` AFTER the edit, which is verbatim the superseded record's own stated refutation condition ('If the prologue still reads stmw r15,140(r1) with frame 264 after that edit, the extra callee-saved register is not being bought by it/best'). PROBE B: NEUTRAL-IDENTICAL, object bytes unchanged -- the pointer's scope and name folded away before codegen, a stronger negative than a regression. PROBE C: IMPROVED real 1154 -> 1152, fresh fuzzy 96.3552 -> 96.5307, and the two lwz clusters at +0x6b8 (ours-only) and +0x72c (target-only) both DISAPPEARED from `fndiff --ops`, cluster count 25 -> 23. PROBE D: IMPROVED real 1152 -> 1148, fresh fuzzy 96.5307 -> 96.7053. PROBE E: IMPROVED real 1148 -> 840, fresh fuzzy 96.7053 -> 97.2692, cluster count 23 -> 21; defake_gate flagged it IMPROVED-CARRIER (multiset moved at equal counts) and the fresh-fuzzy arbitration, measured BEFORE banking by probe.py's fuzzy gate, resolved it FOR the keep. PROBE F: REGRESSED real 840 -> 864 with the multiset flat at 14t and the ours-only `-2 lwzx` unchanged, i.e. the form did not flip. PROBE G: CONFLICT-UNARBITRATED, real 852 against best 840 but multiset 14t -> 12t; `probe.py --arbitrate` built both states in one call and read BANKED real 840 fuzzy 97.2692% against CURRENT real 852 fuzzy 96.8433%, delta fuzzy -0.4259, ARBITER 'fuzzy FELL -- REVERT'. Every fuzzy figure comes from tools/gdl/probe.py on a report regenerated by a build that printed `build/GUNE5D/main.dol: OK`. FINAL GATES at commit 52a9dd15f: full `ninja -j2` prints `build/GUNE5D/main.dol: OK`; `defake_gate.py check game/game/player` prints GATE OK across all 93 functions of the TU (baseline anchored at commit 791ebcd30); tree clean apart from the untracked LANE_LOCK. Project figures at close: 98.45% fuzzy, 66.33% matched, STRICT 56.46% + EQUIVALENT 9.86% -- byte-identical to the session-start figures, as expected for a function that is improved but not yet exact."
  },
  "function": "function:do_players",
  "held_fixed": "Each probe varied ONE site and held every other site in do_players at its then-committed spelling. Probe A varied only the declaration and initialisation of `best` (removed from the function-scope declaration block and re-declared at the top of the `lbl_803447B8 == 0 || ...` block), holding `it` at its function-scope declaration because `it` is read at the do_weakening call OUTSIDE the block that writes it and cannot legally be scoped. Probe B varied only the walked pointer of the get_actual_screen_pos loop (block-scoped and renamed p to q), holding all seven other loops on the function-scope `p`. Probe C varied only the `state = p->state` assignment and its single consumer. Probe D varied only the fn_8009D610 argument site. Probe E varied only the speech round-robin loop head. Probes F and G varied only the two twin j-loops and held probes C, D and E fixed at their committed spellings; F and G differ ONLY in whether the named base local is initialised at its declaration (G) or assigned inside the `j != i` guard (F).",
  "hypothesis": {
    "cheapest_refuting_observation": "One probe converting the get_actual_screen_pos loop head (the +0x5cc region's consumer) to the base-without-0xC40 spelling. If `fndiff --ops` still shows `mr` in the ours-only column and +0x5cc still reads `addi r20,r31,3136` followed by a copy, then the lever does not transfer from a straight-line call site to a loop head, the walked-pointer induction variable is what forces the combined base, and the whole cached-web family is REGISTER_ONLY with source-exhaustion provenance from probes A, B, F and G here plus N, P and T of the superseded record.",
    "screened_against_target": "yes - the register census is measured, not inferred: a defs-per-callee-saved-GPR count over both streams reads target 16 registers (r16-r31) against ours 17 (r15-r31), and both streams materialise the SAME seven ADDR16_LO address bases. fnasm.py game/game/player do_players 0x5c0:0x5f8 --diff shows ours `addi r20,r31,3136` + `mr r16,r20` against target `addi r17,r31,3136` alone, and 0x660:0x690 --diff shows ours `mr r28,r20` against target `addi r21,r31,3136`. NOT yet probed as a source edit: no probe this run or in any prior record respelled a LOOP head's base.",
    "statement": "The remaining ours-only `mr` pair and the target-only `addi rX,r31,3136` rematerialisations are ONE mechanism whose register is now positively identified: our r20 holds potionicon_tab+0xC40 function-wide (materialised once at +0x5cc, then copied by `mr r16,r20` at +0x5d8, `mr r28,r20` at +0x670, `addi r3,r20,0` at +0x950 and `addi r16,r20,0` at +0x1064), while the target rematerialises `addi rX,r31,3136` at each of those regions and keeps no such register. The named-base lever proven three times this run is the untried cure at the REMAINING sites: the four r20 consumers are loop initialisers spelled `p = PT(0)`, i.e. the macro folds the 0xC40 block offset INTO the base. Respelling those four loop heads to walk a base named WITHOUT the block offset -- `u8* rec = (u8*)potionicon_tab; ... PF(rec, 0xC40 + <field>, T)` -- is the same edit that paid +0.1746 and +0.5639 fuzzy at the two sites already converted, and has never been tried at a LOOP head."
  },
  "id": "attempt.PC_do-players-save-set-hypothesis-refuted-and-named-base-lever.20260902.v1",
  "kind": "attempt",
  "outcome": "improved",
  "recorded_at": "2026-09-02T18:21:52Z",
  "residual": {
    "capability_needed": null,
    "family": "cse-share",
    "measured_at": "2026-09-02",
    "signature": "opcode multiset: DIFFERS target-only: +1 add +2 addi +1 lfsx +1 li +2 lwz +1 stfsx ours-only: -1 lfs -2 lwzx -2 mr -1 stfs; insns T1174/O1172; multiset 14t"
  },
  "residual_class": "MIXED",
  "schema_version": 1,
  "supersedes": "attempt.PC_do-players-inplace-update-and-symbol-unification.20260902.v1",
  "valid_from": "2026-09-02"
}
