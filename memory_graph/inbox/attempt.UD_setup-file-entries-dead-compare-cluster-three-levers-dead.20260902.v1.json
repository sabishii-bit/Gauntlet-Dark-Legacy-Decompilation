{
  "attempted_axis": "First real source pass on setup_file_entries (the prior record is a label-correction stub with has_probed_form false): recover the single target-only `cmpwi r0,0` at +0x138 that consumes the volatile VmuMenuEntry.value reload, driving T107/O106 to count parity.",
  "attributes": {
    "law_screen": "`gdlmem context setup_file_entries` run FIRST per discipline 11: it returned exactly one attempt, attempt.mp-label-correction.setup-file-entries.20260831.v1, which is an explicit no-probe label-correction stub (has_probed_form false), so no axis on this function was under a recorded veto and all three probes below were legitimately fresh. claim.law.stack-slots-descend-in-declaration-order.20260831.v1 APPLIED and it is the measured cause of probe 2's regression. claim.law.dead-local-identity-reuse-flips-callee-saved-coloring.20260831.v1 APPLIED: it is the law that named probe 3 as the counter-form to probe 2's frame growth, and it worked as advertised on the frame even though it did not reach the compare. claim.law.insn-count-parity-outranks-local-opcode-fidelity.20260831.v1 SCREENED: we are the SHORT stream here, so a form that ADDS the correct instruction is exactly what is wanted and the law does not disqualify one. claim.law.SL_pool-constant-errors-are-score-invisible.20260901.v1 SCREENED against the three IMMEDIATE rows at @13c-@144: they are NOT wrong constants, they are the 1-31-16-36 induction-variable increment run pair-shifted by the one-instruction drift.",
    "laws_applied": "[\"claim.law.stack-slots-descend-in-declaration-order.20260831.v1\", \"claim.law.dead-local-identity-reuse-flips-callee-saved-coloring.20260831.v1\"]",
    "probed_form": "All three in src/game/ui/select.c::setup_file_entries, each reverted. PROBE 1 (statement respell): changed `if (*(volatile s32*)(e + offsetof(VmuMenuEntry, value)) == 0) {\\n    continue;\\n}` to the same condition with an EMPTY body `{\\n}` -- the `continue` is the last statement of the loop body so it is a pure fallthrough and the two forms are semantically identical. RESULT: NEUTRAL-IDENTICAL, object bytes unchanged, real 95. probe.py's own diagnostic is the finding: the construct folds in the front end, so no further spelling of the SAME construct can reach the decision point. PROBE 2 (declaration lever, new local): added `s32 vcheck;` as the last declaration and made the test an assignment-in-condition, `if ((vcheck = *(volatile s32*)(e + offsetof(VmuMenuEntry, value))) == 0) {\\n    continue;\\n}`. RESULT: REGRESSED real 95 -> 105 with a NAMED cause -- the added local grew the frame from target-matching 56 to 64 (slot map: target-only slots -56/16/56/60, ours-only -64/24/64/68) while the `+1 cmpwi` cluster at @138 was left completely untouched. PROBE 3 (declaration lever, dead-local reuse, chosen specifically to pay probe 2's named frame cost): identical assignment-in-condition but reusing the existing `state` local, which is provably dead after line 2154, instead of declaring a new one. RESULT: NEUTRAL-REARRANGED, real 95, object bytes CHANGED but score flat -- the frame cost was indeed avoided, confirming probe 2's diagnosis, and the compare still did not appear.",
    "residual": "ONE dead compare, mechanism fully named, three distinct source levers measured dead against it -- which is the per-cluster cap, so this cluster is closed for statement-and-declaration source work and the next lane should go straight to the pragma lever in the hypothesis rather than respelling the `if` a fourth time. The three IMMEDIATE rows fndiff prints at @13c/@140/@144 are NOT a fourth defect: they pair `addi r29,r29,1`/`addi r25,r25,16`/`addi r24,r24,36` against `addi r25,r25,36`/`addi r27,r27,1`/`addi r26,r26,16`, which is the same three-induction-variable increment run shifted one slot by the missing compare. They will all close when the compare does, and a lane that chases them as separate wrong-constant bugs is chasing the drift artifact.",
    "scope": "src/game/ui/select.c::setup_file_entries. NOTHING RETAINED -- all three probes reverted via `probe.py --discard`; `git diff --numstat` empty and `git status --short` shows only the untracked LANE_LOCK.",
    "verification": "probe.py game/ui/select setup_file_entries BASELINE real 95 (insns T107/O106, multiset 1t) with fresh objdiff fuzzy 96.8879%, then the three probes with the verdicts quoted above, then --discard back to HEAD. defake_gate.py check game/ui/select.c --rebuild = GATE OK. Full `ninja -j2` = build/GUNE5D/main.dol: OK. STRICT 56.46% / EQUIVALENT 9.97%, unchanged."
  },
  "function": "function:setup_file_entries",
  "held_fixed": "Every probe held the loop's control flow, the three induction variables (k stride 1, nameOff stride 16, entOff stride 36), the four VmuMenuEntry stores, the strncmp arm, the verify_vmu_file_ok call shape and the `ok` flag CONSTANT. Only the spelling of the single trailing `if (*(volatile s32*)(e + offsetof(VmuMenuEntry, value)) == 0) { continue; }` statement and its associated local declaration varied per probe.",
  "hypothesis": {
    "cheapest_refuting_observation": "Wrap setup_file_entries alone in `#pragma opt_propagation off` / `on` and rebuild: `python tools/gdl/probe.py game/ui/select setup_file_entries`. If the object still reports insns T107/O106 with the same single `+1 cmpwi` cluster at @138, the fold is not opt_propagation's and this lever dies in one build. NOTE THE HAZARD before trying it: AGENTS.md and the graph both record that an UNSCOPED `#pragma peephole off` mid-TU poisons every downstream function -- scope the pragma and re-gate the whole TU with `defake_gate.py check game/ui/select.c --rebuild`, not just this function.",
    "screened_against_target": "yes -- the aligned view `fnasm game/ui/select setup_file_entries 0x118:0x168 --diff` was read for the whole loop latch, and the claim that nothing consumes the compare was verified by tracing cr0 from +0x138 to the `cmpw`/`blt` latch at +0x148/+0x14c.",
    "statement": "The residual is ONE dead compare and the source scaffold that should produce it is already present but is folded before codegen. Retail emits `lwz r0,32(r22)` then `cmpwi r0,0` at +0x134/+0x138 and NOTHING consumes the compare -- cr0 is overwritten by the loop-latch `cmpw r29,r31` four instructions later with no intervening branch. So retail's front end emitted compare-plus-branch, the branch-to-next was removed, and the compare survived as an orphan. Ours emits the volatile load but never the compare. Because the three source-level levers below are dead, the remaining untried lever is the one that changes WHEN the fold happens rather than how the statement is spelled: build this TU's setup_file_entries under `#pragma opt_propagation off` (the pragma is already used in this very file, immediately below at setup_sel_menu, so it is in-idiom for the TU) and see whether the orphan compare survives to the object as it does at retail."
  },
  "id": "attempt.UD_setup-file-entries-dead-compare-cluster-three-levers-dead.20260902.v1",
  "kind": "attempt",
  "outcome": "parked",
  "recorded_at": "2026-09-02T19:27:34Z",
  "residual": {
    "capability_needed": null,
    "family": "branch-pair",
    "measured_at": "2026-09-02",
    "signature": "DIFFERS target-only: +1 cmpwi ours-only: (none); insns T107/O106; 1 ops cluster at @138-13c"
  },
  "residual_class": "STRUCTURAL",
  "schema_version": 1,
  "supersedes": "attempt.mp-label-correction.setup-file-entries.20260831.v1",
  "valid_from": "2026-09-02"
}
