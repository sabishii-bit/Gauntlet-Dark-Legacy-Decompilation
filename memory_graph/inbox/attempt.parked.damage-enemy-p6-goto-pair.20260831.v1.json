{
  "attempted_axis": "Exact drive on damage_enemy's last --ops clusters (T@5a4 bgt vs ours cror+bne; T@5ac bne+b vs ours beq) via the one untried branch skeleton: guard1 as `if (health > upper) goto store_fight;` (which DOES reproduce the target's bgt) combined with the guard2 goto pair `if (type != E_DEATH) goto fight_calc; goto store_fight;` -- plus two adjacency-breaker variants when the pair collapsed.",
  "attributes": {
    "axis_log": "v1 (guard-parity, folded in): compound-&& form fixed count parity 570->571, real 151->66, fuzzy dipped 99.40->99.22 (arbitrated by aligned diff: parity realigns every downstream displacement); dead forms: empty-true-block (NEUTRAL fold), split-&&-nested (byte-identical), guard1-goto+guard2-nested (570/151). Older caps still standing from attempt.damage-enemy-constant-lifetime-closure.20260829.v1: the f64 constant-lifetime findings and the fight-web GPR/FPR cap. This v1: goto-triple / label-separated / do-while-break all collapse (570/151).",
    "law_screen": "claim.law.guard-polarity-restructure-fixes-count-parity-not-form.20260831.v1: APPLIED as the arbitration frame -- every probe was scored on count parity first, and the collapsed forms (570/571) were rejected on it despite locally matching the bgt. claim.law.insn-count-parity-outranks-local-opcode-fidelity.20260831.v1: APPLIED -- the retained best keeps 571/571 parity with real 66 over the bgt-matching 570/151 forms. claim.law.if-else-arm-swap-controls-fallthrough-placement.20260831.v1: SCREENED n/a (arms here are branch-reached blocks, not a swappable scalar pair). The v1 record's do-not-retry trio (empty-true-block, split-&&-nested, guard1-goto+guard2-nested) was honored -- none re-probed. P6-wall corpus screened before probing: attempt.parked.dopolyinst.20260831.v3 (front-end collapse of if-goto+goto ADJACENT pairs, 3 forms), attempt.parked.fn_8004db3c.20260829.v1 (same TU, same bne+b-vs-beq residual, nested/explicit-label/opt_propagation forms fold), attempt.playerdoweaptrail-declaration-web / adsMoveRawToCooked / FindWorldObj / DoAudioTallySFX / AudioEnterNextStage parked records (project-wide wall; historical exacts used prohibited asm branch devices). The probes here tested exactly the axis dopolyinst left open: whether breaking statement ADJACENCY defeats the front-end merge.",
    "laws_applied": "[\"claim.law.guard-polarity-restructure-fixes-count-parity-not-form.20260831.v1\", \"claim.law.insn-count-parity-outranks-local-opcode-fidelity.20260831.v1\"]",
    "residual": "PARKED at the v1 best: real 66, insns EXACT 571/571, fuzzy 99.2207, multiset 4t. The residual is now ONE NAMED MECHANISM with a project-wide identity: the P6 goto-pair wall. Target @5a4-5b4: `bgt ->5d0; cmpwi r5,30; bne ->5b4; b ->5d0` -- MWCC's FRONT END (not peephole; dopolyinst v3 proved it at peephole-off cflags) inverts a conditional jump over an adjacent unconditional jump into one folded branch, so every portable spelling yields either `cror eq,lt,eq; bne; cmpwi; beq` (the retained 571-insn form) or `bgt; cmpwi; beq` (570 insns, breaking every downstream displacement, real 151). Probes this session, each REGRESSED to the identical 570/151 beq collapse and reverted: (1) the full goto triple `if (health > upper) goto store; if (type != E_DEATH) goto calc; goto store; calc:` -- closed the bgt cluster (multiset 4t -> 3t) proving guard1's form, but the pair folded; (2) the same with an intervening unreferenced label `death_skip:` between the if-goto and the goto -- label deleted before the merge, byte-identical collapse; (3) a do-while(0) carrier with guard1 `break`, guard2 goto, and the unconditional as `break` -- byte-identical collapse, so break's lowering path merges too. Adjacency-breaking via zero-code statements is now measured dead in 2 forms on top of dopolyinst's 3 adjacent forms and the v1 trio: the axis is exhausted under the two-identical-A/Bs rule several times over. The whole-function FPR/GPR rotation (f3<->f1, f5<->f6, r5<->r7) rides on the cror form's register pressure and cannot be scored separately while the branch skeleton differs. DO-NOT-RETRY: any if-goto/goto, if-else-goto, switch-goto, break/continue, label-separated, or empty-block spelling of a conditional-over-unconditional adjacent jump pair. UNPARK CONDITIONS: a WebFrank-class lever is NOT eligible (the residual is STRUCTURAL: opcode multiset differs by +b +bgt / -beq -cror); genuine unpark requires either new compiler evidence (a flag/pragma proven to disable the front-end jump threading without side effects) or discovery of a retail-plausible construct whose lowering emits an uninverted conditional over an adjacent unconditional -- none is currently known in the corpus.",
    "scope": "src/game/enemy/enemy.c damage_enemy only; all probes reverted; retained source = the v1 goto-free compound guard (commit 4486e7003) plus an expanded in-source do-not-retry comment (commit 1a526245b). No header, configure.py, .s, or postprocessor change.",
    "verification": "probe.py clean BASELINE real 66 (571/571) banked before the first probe; probe 1 CONFLICT real 66->151 with multiset 4t->3t read via the aligned fnasm --diff (bgt cluster verified closed before rejecting on parity); probes 2 and 3 REGRESSED identically (the two-identical-A/Bs axis kill); best state restored from the committed HEAD and re-scored NEUTRAL real 66 571/571; defake_gate.py check game/enemy/enemy.c --rebuild GATE OK after the final comment commit; full ninja green with main.dol: OK."
  },
  "function": "function:damage_enemy",
  "id": "attempt.parked.damage-enemy-p6-goto-pair.20260831.v1",
  "kind": "attempt",
  "outcome": "parked",
  "recorded_at": "2026-08-31T05:40:50Z",
  "residual_class": "STRUCTURAL",
  "schema_version": 1,
  "supersedes": "attempt.damage-enemy-guard-parity.20260831.v1",
  "valid_from": "2026-08-31"
}
