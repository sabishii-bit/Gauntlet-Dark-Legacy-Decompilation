{
  "attempted_axis": "USER-DIRECTED reconstruction-quality pass on init_enemy (the project owner called it 'pretty ugly for a while'): adopt GC-verified header structs over raw offset casts, name locals and magic table displacements from consumer evidence, and remove Ghidra-isms - every edit gated so the 99.01% / real 27 score may not regress.",
  "attributes": {
    "law_screen": "Screened the full core-screen set via `laws --tag core-screen --full 1` plus `brief game/enemy/enemy` before the first edit. APPLIED: claim.law.write-site-alias-defeats-reload-parity.20260830.v1 - decisive, and this function is the same three-write mat[3][0..2] shape the law was recorded on (atree.c AtreeNodeInit); the shadow block was converted with a FRESH ((MBObject*)ptr)-> cast at each of the three sites and NO declared typed alias, which held real 27 exactly, and the target's per-statement reload (lwz 100/476 before every lfs/stfs pair) is the law's predicted tell. APPLIED: claim.law.offsetof-rename-preserves-protected-web.20260830.v1 - the three per-type table displacements (2080/2216/2760) were respelled as ETYPE_* macros inside the SAME single additive expression on the SAME u8* base, no retype, byte-identical. APPLIED as exclusion: claim.law.noop-cast-on-array-decay-not-neutral.20260831.v1 - the existing `(u8*)lbl_8011AF48` cast on an `extern u8 x[]` array was left exactly as written rather than 'simplified' to bare decay. APPLIED as VETO: attempt.enemy-extern-literal-sweep.20260831.v1 records that this TU's .sdata2 pool reads (lbl_80346820 = 0.0f, lbl_80346A40 = 0.1f, lbl_80346A28 = 0.333) must NOT be literalised, and explicitly records init_enemy's apparent 27 -> 17 as a downstream artefact of damage_enemy's pool reshaping - not re-chased. SCREENED, NOT TRIGGERED: lwzu-idiom-web-retention (no p += N load idiom in this function), multifield-alias-defeats-indexed-addressing, declared-alias-vs-repeated-cast (deliberately NOT used - the write-site law overrides it for this shape), pointer-walk-array-index-regression, sizeof-defeats-loop-stride-induction (no stride constant here), offsetof-rename-first-statement-outlier (converted sites are all mid-body), embedded-struct-member-whole-tu-cascade (no header was modified).",
    "laws_applied": "[\"claim.law.write-site-alias-defeats-reload-parity.20260830.v1\", \"claim.law.offsetof-rename-preserves-protected-web.20260830.v1\", \"claim.law.noop-cast-on-array-decay-not-neutral.20260831.v1\", \"claim.law.table-lookup-pre-keyed-on-segment-cast-repetition.20260831.v1\"]",
    "residual": "Score unchanged at real 27, insns 136/137; the single residual is still the one extra `lwz` at 0x98 from re-reading e->state as SetEnemyObj's 4th argument (target keeps `li r6,1` live across the store and the call). That is the recorded best - the deepscrutiny record's literal/temporary alternatives measured 43/43/35/51. It is now COMMENTED in source rather than left unexplained. ONE cleanup form was measured and REJECTED, with the cause read from the diff: flattening the scoped two-step row walk `{ u8* r = tbl; r += toff; health = *(f32*)(r + ETYPE_BASE_HEALTH); }` into the obvious single expression `*(f32*)(tbl + toff + ETYPE_BASE_HEALTH)` regressed real 27 -> 76, opcode multiset DIFFERS, target-only +1 add. Root cause read from --ops: the target re-materialises the table row AFTER the SetEnemyObj call (`add r3,r29,r28` at 0xb4); the flat form lets MWCC reuse the entry region's row register, deleting that add and cascading an entry-schedule rewrite (extra addi at 0x24, stw/lfs displacement at 0x30-0x40, li/addi swaps at 0x50-0x5c). The ugly two-step form is therefore RETAINED with that measurement recorded in a source comment. See claim.law.two-step-table-row-walk-survives-call-boundary.20260831.v1, which this pass proposes.",
    "scope": "src/game/enemy/enemy.c only; no header modified. Added #include game/player.h (gated alone, GATE OK). Changes: shadow-copy block converted from `*(f32*)((u8*)e->shadow + 48/52/56)` to MBObject mat[3][0..2] member form; three magic table displacements replaced by documented ETYPE_ATTN_Y / ETYPE_COLL_Y / ETYPE_BASE_HEALTH macros carrying their evidence; locals t4 -> toff and z -> zero; SetEnemyObj's 4th parameter renamed `one` -> `state`; a function-level behaviour comment and three residual-explaining comments added. Commit f9a15c907.",
    "verification": "probe.py clean BASELINE taken before the first edit (real 27, insns 136/137); each of the three edits probed individually - shadow block NEUTRAL, naming batch NEUTRAL, flatten REGRESSED and reverted. defake_gate.py check --rebuild GATE OK over all 66 TU functions (50 at real 0) after the batch. Full `ninja -j2` green; `ninja build/GUNE5D/ok` forced and reported `build/GUNE5D/main.dol: OK`; project progress unchanged at 97.96% fuzzy / 58.15% matched. git diff --check clean. WORKFLOW HAZARD hit and worth recording: probe.py banks a revert snapshot only on BASELINE/IMPROVED, never on NEUTRAL, so `--revert` after a failed probe in a cleanup pass discards every intervening byte-identical improvement back to the last banked state - it silently wiped two verified-neutral batches here. For de-fakematch work use defake_gate.py plus a git commit per gated batch as the revert point, and treat probe.py --revert as matching-only."
  },
  "function": "function:init_enemy",
  "id": "attempt.init-enemy-cleanup.20260831.v1",
  "kind": "attempt",
  "outcome": "neutral",
  "recorded_at": "2026-08-31T04:42:48Z",
  "residual_class": "REGISTER_ONLY",
  "schema_version": 1,
  "valid_from": "2026-08-31"
}
