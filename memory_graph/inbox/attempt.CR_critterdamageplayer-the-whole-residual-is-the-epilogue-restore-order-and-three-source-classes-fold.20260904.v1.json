{
  "attempted_axis": "First SOURCE work on a function whose only record is a screen-only label correction: classify the residual, census the epilogue-order class image-wide in BOTH streams, then spend three distinct construct classes on the `hit` pointer web.",
  "attributes": {
    "law_screen": "claim.law.pointer-walk-array-index-regression.20260830.v1 SCREENED and CONFIRMED IN THE POSITIVE DIRECTION: it predicts that switching a walked pointer to indexed form on a named array regresses, and the `c->unk1BC[playerIndex][0]` probe regressed real 16 -> 24 exactly as written -- the law is strengthened, not overturned, and the walked `counter` form is kept. claim.law.identical-multiset-is-blind-to-displacements.20260831.v1 SCREENED and APPLIED: the multiset is IDENTICAL 68/68, so the three clusters were read individually from the aligned view rather than trusted. claim.law.webfrank-cannot-close-instruction-count-deltas.20260831.v1 SCREENED and NOT a blocker here -- counts are equal, which is what makes the permutation class reachable. claim.law.MV_callee-saved-numbering-has-a-width-class-ahead-of-declaration-order.20260902.v1 SCREENED as the reading of probe (a)'s NEUTRAL-IDENTICAL. claim.law.point-of-use-single-use-pointer-delay.20260830.v1 SCREENED, NOT applicable: `hit` is multi-use and its address is not the residual. Pin screen: config/GUNE5D/webfrank.json carries 8 pins on game/enemy/critter (CritterGetTargetPlayers, CritterLineRootColSub, CritterResolveMultipleTargets, CritterLineNodeColSub, CritterLookForCriticalMove, CritterLineCollide, CritterDoTexmodNode, ProcessCritter) and none on CritterDamagePlayer; the kept edit is byte-identical so no pinned window moved, and the full ninja replayed every rule green.",
    "laws_applied": "[\"claim.law.pointer-walk-array-index-regression.20260830.v1\", \"claim.law.identical-multiset-is-blind-to-displacements.20260831.v1\"]",
    "probed_form": "PROBE (a): `u8 *hit; u8 *counter;` moved from the trailing inner block to the head of the function's declaration list, ahead of `u32 damageFlags;`, and the inner block braces removed. PROBE (b): `hit = (u8 *)gPlayers + playerIndex * sizeof(Player);` replacing `hit = (u8 *)(playerIndex * sizeof(Player)); hit = (u8 *)gPlayers + (u32)hit;`. PROBE (c), KEPT (commit c769d9f21): `Player *hit; u8 *counter; hit = &gPlayers[playerIndex]; hit->bossdamage = lbl_80346470; counter = (u8 *)c + playerIndex * 0x10; hit->fxhittime = (f32)(lbl_80346500 + (f64)sMusicFadeBase);` replacing the `u8 *hit` + `((Player *)hit)->` cast forms. PROBE (d), REVERTED (real 16 -> 24): `c->unk1BC[playerIndex][0] += damage; c->unk1BC[playerIndex][1] = sMusicFadeBase;` with `counter` deleted.",
    "residual": "Three ops clusters, ONE mechanism: the epilogue. `insert T[63:63]@fc-fc=[] O[63:64]@fc-100=['lfd']`, `replace T[64:65]@100-104=['lfd'] O[65:66]@104-108=['addi']`, `delete T[66:67]@108-10c=['addi'] O[67:67]@10c-10c=[]`. Plus a volatile recolor of the `hit` web that costs no instruction: target holds it in r5 (`mulli r5,r30,13148` then `add r5,r0,r5`, IN PLACE) where ours holds it in r3 (`mulli r4,r30,13148` then `add r3,r0,r4`, FRESH destination). Left because all three source classes that could reach either one measured NEUTRAL-IDENTICAL against digest f27bf9a8c6bf.",
    "scope": "src/game/enemy/critter.c::CritterDamagePlayer only, commit c769d9f21, 4 insertions / 6 deletions -- a byte-identical DE-FAKEMATCH (three `((Player *)hit)->` casts and a `(u32)`/`(u8 *)` round-trip removed in favour of a typed `Player *` and the GC-verified field names `bossdamage`/`fxhittime`). No header, config, webfrank.json or symbols.txt change.",
    "verification": "Clean-state BASELINE banked before the first edit (first-baseline trap honoured): real 16, T68/O68, multiset 0t, FUZZY (fresh report) 95.6618%, RELOC-SYMBOL SCREEN clean, `SLOT MAP IDENTICAL`. KEEP is byte-identical to that baseline (digest f27bf9a8c6bf). `defake_gate.py check game/enemy/critter --rebuild` = `GATE OK` with no row. Full `ninja -j2` printed `build/GUNE5D/main.dol: OK`; `python -m unittest discover tools/gdl/tests -b` = `Ran 1829 tests ... OK (skipped=2)`; `python -m memory_graph.test_graph --changed -b` = `SKIP: no changed path under memory_graph/, tools/gdl/, config/GUNE5D/, research/xbox_symbols/`. `git diff --check HEAD` and `git diff --cached --check` both exit 0. Field authority for the de-fakematch: `gdlmem.py struct Critter --offset 0x1BC` returns the GC project header `include/game/critter.h` line 174 `f32 unk1BC[4][4]; /* 0x1BC */`, whose 0x10 row stride is exactly the `playerIndex * 0x10` walk -- which is why probe (d) was semantically correct and still had to be reverted on score."
  },
  "denial": {
    "expiry_check": "python tools/gdl/probe.py game/enemy/critter CritterDamagePlayer --ops -- re-probe these classes only if the three-cluster list `insert O[63:64]@fc-100=['lfd'] / replace T[64:65]@100-104=['lfd'] O=['addi'] / delete T[66:67]@108-10c=['addi']` changes shape, or if real is no longer 16 at T68/O68.",
    "falsifier": "Any retyping, rescoping or restatement of the `hit` web that moves the object bytes at all -- the denial is byte-level, and the NEUTRAL-IDENTICAL-PROOF digest f27bf9a8c6bf is the anchor.",
    "premise_measurement": "All three measured NEUTRAL-IDENTICAL against the SAME object digest. Baseline `probe.py game/enemy/critter CritterDamagePlayer --ops` at head d5ea67b0e: `BASELINE real 16 (insns T68/O68, multiset 0t)`, `FUZZY (fresh report): 95.6618%`, `RELOC-SYMBOL SCREEN: clean`. Probe (a): `NEUTRAL real 16 (insns T68/O68, multiset 0t) [NEUTRAL-IDENTICAL: object bytes unchanged]`, `NEUTRAL-IDENTICAL-PROOF unit=game/enemy/critter fn=CritterDamagePlayer bytes=f27bf9a8c6bf real=16 insns=T68/O68 multiset=0t head=d5ea67b0e`. Probe (b): `NEUTRAL real 16 (insns T68/O68, multiset 0t)`, and the aligned view `fnasm ... 0xb0:0xc8 --diff` confirmed every register unchanged. Probe (c): `NEUTRAL real 16 ... [NEUTRAL-IDENTICAL: object bytes unchanged]`. A FOURTH probe, converting the `counter` raw offsets to the GC-header-verified `c->unk1BC[playerIndex][0]/[1]`, REGRESSED `real 16 -> 24` at T68/O68 and was reverted -- that is claim.law.pointer-walk-array-index-regression.20260830.v1 firing exactly as it predicts.",
    "scope": "THREE source construct classes on CritterDamagePlayer's `hit` pointer web and its declaration block: (a) declaration POSITION/SCOPE -- hoisting `u8 *hit; u8 *counter;` out of the inner block to the FRONT of the function's declaration list; (b) STATEMENT SHAPE -- replacing the two-step `hit = (u8 *)(playerIndex * sizeof(Player)); hit = (u8 *)gPlayers + (u32)hit;` with the single expression `hit = (u8 *)gPlayers + playerIndex * sizeof(Player);`; (c) TYPE + ADDRESSING -- `Player *hit; hit = &gPlayers[playerIndex];` with named-field writes. It does NOT deny a frame/save-set lever, and it does NOT deny the epilogue order itself as a postprocessor target."
  },
  "function": "function:CritterDamagePlayer",
  "held_fixed": "All three probes held the 68-instruction body, the frame (64), the save set (f31 + r28-r31), the `damage_player` call and every statement of the damage/flags computation constant, and varied exactly one dimension of the tail block: (a) the SCOPE and POSITION of the `hit`/`counter` declarations, (b) the STATEMENT SHAPE of the `hit` address computation, (c) the TYPE of `hit` plus the addressing form of its two field writes.",
  "hypothesis": {
    "cheapest_refuting_observation": "Zero builds: `python tools/gdl/fnasm.py game/enemy/critter CritterDamagePlayer 0x0:0x28 --diff`. If the prologue is EVER not byte-identical -- if the frame, the `stfd f31,56(r1)`/`stmw r28,40(r1)` pair or their order differ -- then the epilogue difference has a prologue-side cause after all and the 'nothing left to steer' framing is wrong. Measured this session: they ARE byte-identical, all ten rows `=`.",
    "count_consequence": "Count-neutral is already TRUE and must be preserved: T68/O68 today. That is what makes this function postprocessor-ELIGIBLE, unlike its TU sibling ProcessCritterList; any cure that leaves parity forfeits the only class that can currently close it.",
    "screened_against_target": "yes -- the aligned views at 0x0:0x28, 0xa8:0xe8 and 0xe0:0x118 were all read this pass and both streams' epilogues are quoted instruction by instruction above. NOT screened: the determinant of the epilogue order itself is unidentified; the census in the companion law record cross-tabulated saved-GPR count, saved-FPR count, FPR-slot-above-GPR-block and the FPR number, and NONE of them separates the two shapes.",
    "statement": "The entire residual is the EPILOGUE restore order and nothing else is left to steer from source. Target `lwz r0,68(r1) / lmw r28,40(r1) / lfd f31,56(r1) / mtlr r0 / addi r1,r1,64 / blr` against ours `lwz r0,68(r1) / lfd f31,56(r1) / lmw r28,40(r1) / addi r1,r1,64 / mtlr r0 / blr` -- two independent swaps, {lmw,lfd} and {mtlr,addi}, over a byte-IDENTICAL prologue (`stfd f31,56(r1)` then `stmw r28,40(r1)` in both), an identical frame (64) and an identical save set. Because the prologue and every instruction up to +0xf4 already agree, no statement-level lever remains: the next lane should either find the frame/save-set-level determinant named in claim.law.CR_mwcc-epilogue-restore-order-is-contextual-and-our-build-reproduces-the-target-on-108-of-109-image-wide.20260904.v1 -- where this function is measured to be the SOLE image-wide anomaly (ours-only LFD_FIRST, target-only empty) -- or route this function to the postprocessor as a two-swap instruction_permutation request, for which every precondition is already measured (T68/O68, multiset IDENTICAL 68/68, RELOC-SYMBOL screen clean)."
  },
  "id": "attempt.CR_critterdamageplayer-the-whole-residual-is-the-epilogue-restore-order-and-three-source-classes-fold.20260904.v1",
  "kind": "attempt",
  "outcome": "capped",
  "recorded_at": "2026-09-04T01:24:26Z",
  "residual": {
    "capability_needed": "instruction_permutation",
    "family": "save-area",
    "measured_at": "2026-09-04",
    "signature": "opcode multiset: IDENTICAL (68/68) -- pure reorder, schedule-class residual; 3 ops clusters at @0xfc-0x10c"
  },
  "residual_class": "SCHEDULE",
  "schema_version": 1,
  "valid_from": "2026-09-04",
  "verifiers_run": [
    "NONE RUN, and deliberately so -- this record does NOT close the postprocessor path, it OPENS it. The function is count-parity T68/O68 with an IDENTICAL 68/68 opcode multiset and a clean RELOC-SYMBOL screen, which is the precondition set that instruction_permutation needs, and the three NEUTRAL-IDENTICAL probes below are the source-exhaustion provenance a rule would have to cite.",
    "copy_register_fields: NOT RUN",
    "verify_consistent_recolor: NOT RUN",
    "verify_value_equality_recolor: NOT RUN -- named explicitly because it is the escape a partial screen usually misses",
    "verify_relocation_binding: NOT RUN",
    "instruction_permutation: NOT RUN",
    "unproven_recolor_audit: NOT RUN",
    "webfrank_audit.py: NOT RUN on this function"
  ]
}
