{
  "attempted_axis": "Close the last 16 real rows of adsInitFromHeader (155/155, opcode multiset IDENTICAL) with two source levers the prior record did not separate: the statement ORDER of the cur/end pair inside the 32-bit branch, and the `extern f32/f64 lbl_` scaffold that our source used in place of the original float LITERALS.",
  "attributes": {
    "law_screen": "claim.law.identical-multiset-is-blind-to-displacements.20260831.v1 APPLIED and DECISIVE: the multiset read IDENTICAL at real 16 while `fndiff --ops` reported 2 aligned same-opcode IMMEDIATE rows (T addi r27,r4,2 / O addi r26,r4,-1 at +0x140 and the mirror at +0x144), which is exactly the blindness that law names, and reading them is what identified the cur/end pair rather than a register theory. claim.law.explicit-fp-symbol-vs-literal-zero-regression.20260830.v1 SCREENED and found to point the OTHER way here: it warns that spelling a pooled zero as an explicit symbol regresses; this function is the converse case, where the explicit symbol is what regresses and the literal is correct, so the law is not contradicted but its scope does not cover generated conversion constants (see the new law record). claim.law.fuzzy-can-underweight-a-real-improvement / real-can-underweight-a-large-alignment-gain: not needed, real, multiset and fresh fuzzy all moved the same way at every step. core-screen offsetof/index/lwzu/addr-CSE laws: none applicable, no member access or walked pointer was touched.",
    "laws_applied": "[\"claim.law.identical-multiset-is-blind-to-displacements.20260831.v1\"]",
    "probed_form": "PROBE 1, NEGATIVE but INFORMATIVE (real 16 -> 22): move `cur` before `end` inside the 32-bit branch, keeping addr[0]/addr[1] between them --\n            cur = aram * 2 + 2;\n            addr[0] = 1;\n            addr[1] = 0;\n            end = (aram + sizeVoiceLoop) * 2 - 1;\nThe two IMMEDIATE rows CLOSED (the target's slwi/addi emission order was reproduced word for word) but six new renaming rows appeared: the `lwz` of sizeVoiceLoop landed in r0 and the addr constants in r3, the reverse of the target.\nPROBE 2, KEPT (real 16 -> 8, fuzzy 99.6645 -> 99.8710): the same move with addr[0]/addr[1] AFTER both assignments --\n            cur = aram * 2 + 2;\n            end = (aram + sizeVoiceLoop) * 2 - 1;\n            addr[0] = 1;\n            addr[1] = 0;\nThis puts the sizeVoiceLoop load ahead of the two `li` constants in the unscheduled order, so the load keeps r3 and the constants take r0 as in the target; the whole integer block became byte-identical.\nPROBE 3, KEPT and EXACT (real 8 -> 0, fuzzy 99.8710 -> 100.0000): delete the `f32 kdiv; f64 kscale;` locals and the two prologue assignments `kscale = lbl_80349310; kdiv = lbl_80349308;`, and write the literals at their use sites --\n        ratio = ratio / 32000.0f;\n        ...\n        srcb[1] = (u16)(s32)(65536.0 * ratio);\nValues read from the retail image at their addresses: 0x80349308 = 32000.0f, 0x80349310 = 65536.0, 0x80349318 = 4503599627370496.0 (u32 bias), 0x80349320 = 4503601774854144.0 (s32 bias).",
    "residual": "NONE. The function is byte-exact and STRICT (no postprocessor rule involved).",
    "scope": "src/game/audio/adstream.c, function adsInitFromHeader only. Two locals and two assignments deleted, two float literals introduced, four statements reordered inside the 32-bit branch. No header, struct, sibling or configure.py edit. The four `extern f32/f64 lbl_803493xx` declarations were left in place as documentation of the TU's sdata2 pool; they are now unreferenced and emit nothing.",
    "verification": "probe.py BASELINE real 16 / fuzzy 99.6645 taken before the first edit. Final: `fndiff --clean` = MATCH (pool-name noise only), 0 real diff lines; `probe.py --fuzzy` fresh report 100.0000%; `defake_gate.py check game/audio/adstream --rebuild` GATE OK (1 improved, no sibling flags). datadiff --sections arbitrated the DATA-CHANGED warning: .rodata target 0x0 vs ours 0x2D and .sdata2 target 0x0 vs ours 0x20 are both PRE-EXISTING flip blockers (the split map assigns this TU no data), and .sdata2 grew 0x10 -> 0x20 because our object now emits the four pool constants the original TU owned at 0x80349308..0x80349320 instead of importing them - the section was never matched and moves toward the retail layout, not away. Full ninja green, `build/GUNE5D/main.dol: OK`; project Data% unchanged at 7.52%; project matched 66.43 -> 66.49, STRICT 56.46 -> 56.52 (2575 -> 2576 fns), EQUIVALENT unchanged 9.97. Commit 7d13eb2b9 'Match adsInitFromHeader'. game/audio/adstream carries no webfrank pins, so no pin re-derivation was required."
  },
  "function": "function:adsInitFromHeader",
  "held_fixed": "Both probes held the whole loop body, the ADPCM/SRC/ADDR call sequence, the else-branch (`sShortenedSizeVoiceLoop`) statements and their order, the k48/aram/vnum prologue assignments and the frame; PROBE 1 and 2 varied ONLY the position of `cur`/`end`/`addr[0]`/`addr[1]` inside the if-branch, and PROBE 3 varied ONLY whether kscale/kdiv are named locals read from `extern` sdata2 objects or float literals written at the use site.",
  "id": "attempt.NM_adsinitfromheader-extern-scaffold-and-statement-order-close.20260902.v1",
  "kind": "attempt",
  "outcome": "improved",
  "recorded_at": "2026-09-02T23:08:31Z",
  "residual": {
    "capability_needed": null,
    "family": "constant-hoist",
    "measured_at": "2026-09-02",
    "signature": "closed -- was '0t opcode multiset IDENTICAL (155/155); insns T155/O155; 0 ops clusters; 2 aligned IMMEDIATE rows; real 16'"
  },
  "residual_class": "NONE",
  "schema_version": 1,
  "supersedes": "attempt.adsinitfromheader-portable-schedule-closure.20260829.v1",
  "valid_from": "2026-09-02"
}
