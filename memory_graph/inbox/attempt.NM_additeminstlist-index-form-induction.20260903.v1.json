{
  "attempted_axis": "Zero-record close-lane candidate (nearmiss real 10). Five distinct source forms across two mechanisms: (A) sink the loop-counter inits to the for-init/inner block, (B) let MWCC build the induction web from base+i*stride instead of a named byte-offset accumulator. (B) landed real 10 -> 4 and is committed; the surviving 2-word residual is an emission-position difference in one basic block.",
  "attributes": {
    "law_screen": "claim.law.explicit-offset-accumulator-places-induction-updates-early.20260831.v1: APPLIED, and its TELL is what produced the win. The law says to read the DIRECTION off the target's induction-update PLACEMENT rather than assume it. Target updates at the BOTTOM of the body, immediately before the loop compare (`addi r29,r29,1` at +0x130 and `addi r26,r26,60` at +0x134, with `cmpw r29,r28` at +0x138), which the law assigns to the `base + i * STRIDE` form -- so the named `instance_offset` accumulator that the tree carried was the WRONG side of this law's boundary for this loop. claim.law.sizeof-defeats-loop-stride-induction.20260831.v1: APPLIED as a screen -- the pre-existing source used `instance_offset += sizeof(iteminst)` as a loop STRIDE, exactly the substitution this law records as non-neutral; the fix removes the stride expression entirely rather than respelling it, and the surviving `sizeof(Item)` is a one-shot allocation multiplicand outside any loop, left as-is. claim.law.webfrank-pinned-function-source-freeze: SCREENED before editing -- game/world/items has three pins (DistanceToClosestPlayer, InitLighting, InitItemInfoData); AddItemInstList is not among them, the edit changes no instruction COUNT (100/100 before and after) and touches no anonymous-pool population, and all three pins replayed in the full green build.",
    "laws_applied": "[\"claim.law.explicit-offset-accumulator-places-induction-updates-early.20260831.v1\", \"claim.law.sizeof-defeats-loop-stride-induction.20260831.v1\"]",
    "probed_form": "PROBE 1 (real 10 -> 36): declarations changed to `s32 i;` and `s32 instance_offset;` (initializers removed) with `for (i = 0, instance_offset = 0; i < instance_count; i++, instance_offset += sizeof(iteminst))`. PROBE 2 (real 36, identical object): same two declarations removed from the outer list entirely and re-declared in a new inner block wrapping the loop, `{ s32 i = 0; s32 instance_offset = 0; for (; i < instance_count; i++, instance_offset += sizeof(iteminst)) { ... } }`. PROBE 3 (real 36, identical object): both declarations restored WITH their `= 0` initializers AND the for-init kept as `for (i = 0, instance_offset = 0; ...)`, testing whether a dead declaration-store would fix the rank while the live for-init store fixed the position -- MWCC deleted the dead store and the rank followed the SURVIVING definition. PROBE 4 (real 10 -> 4, KEPT): `instance_offset` deleted; `for (; i < instance_count; i++)`; `if ((instance = (iteminst*)((u8*)instances + instance_offset))->index < 0)` becomes `if ((instance = &instances[i])->index < 0)`. PROBE 5 (real 4 -> 36): probe 4 plus `s32 i;` and `for (i = 0; i < instance_count; i++)`.",
    "residual": "4 real rows / 18 raw words, ONE named mechanism. Target emits `li r29,0` (i) and `li r26,0` (the 60-byte-stride IV MWCC builds from `&instances[i]`) in the loop preheader at +0x58/+0x5c, immediately before the entry branch `b ->138`; ours emits the same two words at +0x18/+0x1c, at the top of the SAME basic block. Every other word, including the complete callee-saved assignment r30..r24 = instances, i, instance_count, visible_sum_coins, IV, &gIdentityMatrix, &sNewItemBadIndex, is now identical to target. NOT a recolor and not a count gap: 100/100 instructions, opcode multiset IDENTICAL, zero relocation-symbol mismatch, so this is exactly the instruction_permutation class. SOURCE EXHAUSTION for that rule request: five literal forms above, spanning both the initializer-position mechanism (3 probes, all real 36, two of them byte-identical objects) and the induction-form mechanism (2 probes), establish that the callee-saved RANKING and the initializer EMISSION POSITION are coupled in this function -- holding the ranking (declaration initializer) forces top-of-block emission, and moving the emission to the preheader (for-init, inner block, or dead-store-plus-live-for-init) demotes `i` and the IV below the two loop-invariant address hoists and costs +32 real. No source form reached both.",
    "scope": "src/game/world/items.c AddItemInstList only; portable C; one de-fakematch conversion (raw `(u8*)base + offset` pointer arithmetic to typed `&instances[i]` indexing) that is also the matching win; probes 1-3 and 5 reverted.",
    "verification": "probe.py BASELINE real 10 (insns T100/O100, multiset 0t) banked before the first edit; fresh objdiff fuzzy measured by probe's own fuzzy gate at 94.5500% before and 94.7500% after, so the parity-held real improvement is arbitrated on FRESH fuzzy per AGENTS.md discipline 9c; fndiff --clean confirms the surviving 4 rows are the two `li` positions and their counterparts; wf_word_diff.py reports 18 raw differing words / 13 mnemonic divergence / 0 reloc-symbol mismatch, count parity 100/100, PINNED = no; defake_gate.py baseline + check game/world/items.c = GATE OK across 52 functions (43 at real 0) with 5 non-text sections digested and no sibling regression; full ninja -j2 green with build/GUNE5D/main.dol: OK; git diff --check clean; commit f8c199810."
  },
  "function": "function:AddItemInstList",
  "held_fixed": "The declaration ORDER of the five outer locals (instances, i, instance_count, visible_sum_coins, instance_offset) was held constant across probes 1-3 while only the POSITION of the `i`/`instance_offset` zero-initializers was varied; probes 4-5 then held the initializer position and varied the induction FORM.",
  "hypothesis": {
    "cheapest_refuting_observation": "Any source form that measures real 0..3 on AddItemInstList while keeping insns 100/100 refutes the claim that ranking and emission position are inseparable here. Run `probe.py game/world/items.c AddItemInstList` after the edit.",
    "screened_against_target": "yes -- target roles read off the loop tail at +0x12c..+0x13c (`addi r27,r27,1` then `stw r27 -> sVisibleSumCoinCount` = visible_sum_coins; `addi r29,r29,1`/`cmpw r29,r28` = i vs instance_count; `addi r26,r26,60` = the 60-byte-stride IV), and the target's ranking r30..r24 = instances, i, instance_count, visible_sum_coins, IV, &gIdentityMatrix, &sNewItemBadIndex was confirmed to equal our post-fix ranking exactly.",
    "statement": "The remaining 2 words are `li r29,0` (i) and `li r26,0` (the compiler-built byte-offset IV), which the target emits in the loop preheader at +0x58/+0x5c and we emit at the top of the same basic block at +0x18/+0x1c. Every source form that moves them to the preheader also demotes `i` and the IV below the two loop-invariant address hoists in the callee-saved ranking (measured 3x, real 36 each time), and every form that holds the ranking emits them at the top (measured 2x, real 4). The entry region +0x0..+0x60 is ONE basic block (the two `bl`s do not split it), so this is a within-block emission-order difference with count parity 100/100 and zero reloc mismatch -- i.e. a genuine instruction_permutation rule request, not a source question."
  },
  "id": "attempt.NM_additeminstlist-index-form-induction.20260903.v1",
  "kind": "attempt",
  "outcome": "improved",
  "recorded_at": "2026-09-03T03:41:58Z",
  "residual": {
    "capability_needed": "instruction_permutation",
    "family": "schedule-window",
    "measured_at": "2026-09-03",
    "signature": "opcode multiset IDENTICAL (100/100); insns T100/O100; 2 ops clusters; RAW DIFFERING WORDS = 18, MNEMONIC DIVERGENCE = 13, RELOC-SYMBOL MISMATCH = 0 (wf_word_diff.py, 2026-09-03)"
  },
  "residual_class": "SCHEDULE",
  "schema_version": 1,
  "valid_from": "2026-09-03"
}
