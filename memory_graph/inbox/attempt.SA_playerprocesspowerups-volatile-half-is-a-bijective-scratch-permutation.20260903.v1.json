{
  "attempted_axis": "Execute the mandatory step-1 hypothesis of attempt.SA_playerprocesspowerups-pool-literal-and-propagation-drive.20260902.v1: attack the VOLATILE half of the weaphold-FX block at +0xdc0..+0xdf0 first (the five-way permutation whose tell is the divw operand home - target divides in place `divw r3,r3,r0`, ours materialised a fresh quotient `divw r7,r5,r4`) and observe whether the callee-saved source/parent pair follows. Eight distinct source axes spent; one KEPT.",
  "attributes": {
    "axis_log": "axis1 compound-assignment divw coalescing = KEPT (class change, score-neutral); axis2 hoist tier above the guard = REFUTED 28w/11 mnemonic; axis3 outer declaration = NEUTRAL-IDENTICAL; axis4 remove opt_propagation = REFUTED as this block's cause (region byte-identical) and 133w overall; axis5 dissolve the tier local = REFUTED, count-asymmetric +18; axis6 reverse the float compare = NEGATIVE, canonicalised; axis7 initialised pair declarations = REFUTED 31w; axis8 swap pair assignment order = NEUTRAL-IDENTICAL.",
    "law_screen": "claim.law.declaration-order-numbers-the-registers-assignment-order-schedules-the-loads.20260901.v1 SCREENED and REFUTED for this block in BOTH halves: axis 3 moved `tier`'s declaration from the inner block to the outer block (with source/parent/tree_flags) while keeping `tier = p->level / 10;` and the object was byte-identical to the baseline in the whole region; axis 8 swapped the two ASSIGNMENTS of the split source/parent pair to (source, parent) and the 24-word list came back element-for-element identical. So for this block neither half of that law bites - the only lever that moved a register was the compound-assignment form (axis 1), which is a THIRD axis the law does not name. claim.law.constant-copy-vs-rematerialize-is-source-unreachable.20260831.v1 SCREENED as the transferable cohort's top law (via attempt.adsputbuffer-lo-fold-home-vs-scratch.20260831.v2, match_strength 0.25) and it is CONSISTENT with the outcome: the surviving 12-word block is exactly a copy-vs-rematerialize colouring choice over five dead scratch values. claim.law.identical-multiset-is-blind-to-displacements.20260831.v1 SCREENED and honoured - every verdict in this lane was arbitrated on the RAW wf_word_diff count, never on the multiset, which read `IDENTICAL (1598/1598)` in six of the eight probed states including two that were 28 and 31 words. claim.law.adding-a-local-renumbers-the-anonymous-pool-and-drifts-siblings.20260831.v1 SCREENED as the hazard of the kept edit and measured CLEAR: the kept form adds no local (it splits one initialiser into a declaration plus a compound assignment), all five game/game/player webfrank pins still replay, and the full ninja printed build/GUNE5D/main.dol: OK. claim.law.webfrank-pinned-function-source-freeze.20260831.v1 SCREENED: PlayerProcessPowerups is not one of the five pinned functions in this TU. claim.law.fuzzy-can-underweight-a-real-improvement.20260830.v1 SCREENED and NOT applicable - fuzzy, real and the raw word count all agree that the kept edit is score-neutral; the keep is justified on CLASS, not on score.",
    "laws_applied": "[\"claim.law.identical-multiset-is-blind-to-displacements.20260831.v1\", \"claim.law.adding-a-local-renumbers-the-anonymous-pool-and-drifts-siblings.20260831.v1\", \"claim.law.constant-copy-vs-rematerialize-is-source-unreachable.20260831.v1\"]",
    "probed_form": "KEPT, axis 1 (commit e77ef10ed): `s32 tier = p->level / 10;` -> `s32 tier = p->level;` + blank line + `tier /= 10;` at src/game/game/player.c:5412-5414. Effect measured at the word level: ours went from `divw r7,r5,r4` (0x7ce523d6) + `mulli r3,r7,12` (0x1c67000c) to `divw r6,r6,r4` (0x7cc623d6) + `mulli r3,r6,12` (0x1c66000c) - the quotient now COALESCES onto the dividend exactly as the target's `divw r3,r3,r0` does, and r7 disappears from the function. REFUTED, axis 2: hoisting `tier = p->level / 10;` above `if (fresh != 0 && p->weaphold_atree != NULL)` (declaring `s32 tier;` in the outer block) - 24 -> 28 words and MNEMONIC DIVERGENCE 0 -> 11, i.e. the divide left the guarded block and the streams stopped being index-aligned; so the target's tier IS computed inside the guard. NEUTRAL-IDENTICAL, axis 3: outer declaration + `tier = p->level / 10;` inside - region bytes identical to the baseline (divw r7 back), so declaration POSITION is inert and only the assignment FORM matters. REFUTED, axis 4: deleting `#pragma opt_propagation off`/`reset` around the function and re-compensating `u8 unused[112]` -> `[64]` - 24 -> 133 words overall, AND the twelve +0xdc0..+0xdf0 words came back BYTE-IDENTICAL to the pragma-on state, so opt_propagation is NOT what colours this block (it is load-bearing elsewhere in the function). REFUTED, axis 5: dissolving the `tier` local entirely and writing `p->level / 10` at all seven use sites - COUNT-PARITY LOST, 1598 vs 1616, multiset `ours-only: -6 divw -6 lwz -6 mulli`, so CSE does not fold the division and the target must carry a named local. NEGATIVE, axis 6: reversing the float compare to `if (0.0f != ((TierColor*)(lbl_80282930[p->index] + 0xE0))[tier].rgb[0])` - the +0xe50/+0xe64 f0/f1 transposition did not move at all, matching the run-40 finding on the commutative fadd at +0x3a4. REFUTED, axis 7: initialised declarations `void* parent = p->hand_node; void* source = WeapHoldFxTree[index][kind];` for the callee-saved pair - 24 -> 31 words: it left r22/r24 transposed AND additionally corrupted the WeapHoldFxTree address web at +0xcf4/+0xd04/+0xd08/+0xd0c/+0xd10/+0xd14/+0xd20. NEUTRAL-IDENTICAL, axis 8: swapping the two ASSIGNMENTS of the split pair to `source = ...; parent = ...;` - all 24 word rows returned element-for-element identical.",
    "residual": "24 raw differing words, unchanged in COUNT from the run-40 close but changed in CLASS. Five clusters, re-measured at commit e77ef10ed. (a1) 7 words at +0xcfc, +0xd1c, +0xd28, +0xd34, +0xd54, +0xd5c, +0xd8c - the callee-saved source/parent transposition (target parent=r24 source=r22, ours the reverse); denial below, now covering declaration order, assignment order AND the initialised form. (a2) 12 words at +0xdc0..+0xdf0 - the volatile tier-colour prologue, and this is what MOVED: the five live values (atree pointer, p->level, the constant 10, p->index, the lbl_80282930 @ha) now occupy exactly FIVE registers in each stream against six in ours before, so the mapping target->ours is the bijection r4->r5, r3->r6, r0->r4, r6->r0, r5->r3 (a single 5-cycle). Before the keep, ours also held the quotient in r7 while the target held it in r3, i.e. six distinct registers against five, which is NOT a bijection and therefore outside every permutation-class rule by construction. (b) 2 words at +0x20 and +0x30, the FIRST of four inlined player_scale_att expansions only: target `lfs f0,244(r3)` + `fmul f2,f3,f0`, ours `lfs f2,244(r3)` + `fmul f2,f3,f2` - the same coalescing question as (a2) but pointing the OTHER way (here the target does NOT coalesce and we do), and it is the one expansion whose load is hoisted above `mr r31,r3`; the other three are byte-identical. (c) 2 words at +0xe50/+0xe64, an f0/f1 transposition across `fcmpu cr0,f1,f0` - axis 6 proves it is not reachable by reversing the source operands. (d) 1 word at +0x3a4, the commutative fadd, source-unreachable per the run-40 NEUTRAL-IDENTICAL proof. Cluster (e) of the run-40 record (~13 pool-name rows, @@NNNN vs lbl_8034xxxx) does NOT appear in wf_word_diff at all - it is visible only in fndiff's real 48 and remains unclosable from source because game/game/player.c owns no .sdata2 split range.",
    "scope": "src/game/game/player.c only, function PlayerProcessPowerups only, lines 5412-5414. One commit: e77ef10ed. No other function in the TU edited; no header touched; no webfrank rule authored (this lane does not own webfrank - see the rule-request record).",
    "verification": "Expiry check of the run-40 denial run FIRST and it did NOT expire: probe.py real 48, insns T1598/O1598, multiset 0t; wf_word_diff 24 words, MNEMONIC DIVERGENCE 0, class RECOLOR. Session baseline banked by a clean probe before the first edit, fresh objdiff fuzzy 99.9124%. Final kept state: real 48, insns T1598/O1598, multiset 0t, 24 raw differing words, fresh objdiff fuzzy 99.9124% (prev 99.9124) - score-neutral on every arbiter, which is why the outcome is a CLASS result and not a score result. savedregs.py at the final state still prints `ASSIGNMENT MATCHES: every callee-saved register holds the same value in both streams` (note: savedregs is FIRST-DEFINITION based and is structurally blind to the (a1) transposition, which is a SECOND lifetime of r22/r24 at +0xcfc/+0xd1c - do not read its all-clear as covering that pair). datadiff.py game/game/player.c --sections shows only the four pre-existing FLIP BLOCKER rows (.rodata/.data/.sdata2/.bss, target sizes all 0x0); no extabindex row, i.e. the exception tables did not move. defake_gate.py check game/game/player.c --rebuild = GATE OK against the baseline taken this session at commit 7300fe688 (93 functions, 71 at real 0, source sha1 6073b6ed5). Full `ninja -j2` green with `build/GUNE5D/main.dol: OK` and retail sha1 7cba77aa496eb0fc5ffec60efd9680aa9635d679; all five game/game/player webfrank pins replayed. Project progress at the kept state: All 98.47% fuzzy, 66.52% matched, Data 7.52%; STRICT matched 56.52% + EQUIVALENT 10.00%. git diff --check clean; no assembly added; one file changed, 3 insertions, 1 deletion."
  },
  "coverage_range": "0x0:0x18f8 - the whole 1598-instruction body. The 24-row word table and the five-register correspondence for +0xdc0..+0xdf0 both come from `wf_word_diff.py --list` over the complete postprocess body, not from a window; the aligned views quoted (`fnasm 0x0:0x70`, `0xcf0:0xdf8`, `0xdf4:0xe90`) were read to interpret rows the whole-body table had already found.",
  "denial": {
    "expiry_check": "python tools/gdl/probe.py game/game/player.c PlayerProcessPowerups --ops && python tools/gdl/composed_census/wf_word_diff.py game/game/player.c PlayerProcessPowerups --list - if real is no longer 48, or the word count is no longer 24, or +0x0cfc no longer reads O 82df06d0 / T 831f06d0, the premise has moved and this denial has expired.",
    "falsifier": "Any edit that moves the +0xcfc/+0xd1c pair onto the target's registers with counts held at 1598/1598 and fuzzy not falling disproves it. Note that the two obvious remaining candidates are now spent: declaration order, assignment order and the initialised form are all measured, and the volatile-half-first route named as the run-40 falsifier has been executed and did not carry the pair. What would still disprove it: a change OUTSIDE this block that alters r22/r24's earlier lifetimes (their first definitions are `slwi r24,r29,2` at +0x488 and `add r22,r0,r24` at +0x4ac in BOTH streams) such that the allocator reaches +0xcfc with the opposite preference.",
    "premise_measurement": "real 48 at insns 1598/1598, opcode multiset IDENTICAL, 24 raw differing words, of which seven are this pair. SIX probes now, all negative. Three from run 40, re-screened and not re-run: (a) reversing the two split declarations to `void* parent; void* source;` = NEUTRAL-IDENTICAL, object bytes UNCHANGED; (b) deleting the `tree_flags` local and passing 0x81880 inline REGRESSED real 48 -> 365 and made the function count-asymmetric 1597 vs 1598, because the target's `lis r5,8` at +0xd08 IS that local's own materialisation; (c) retyping `tree_flags` to uninitialised-then-assigned = NEUTRAL with extabindex MOVED. Three new this run: (d) initialised declarations `void* parent = p->hand_node; void* source = WeapHoldFxTree[index][kind];` = 24 -> 31 words, the pair STILL transposed and the WeapHoldFxTree address web additionally corrupted at seven further offsets, which also refutes the transfer of run 40's PLAYER_SET_FAMILIAR counter-example to this site; (e) swapping the two ASSIGNMENTS of the split form to `source = ...; parent = ...;` = all 24 word rows element-for-element identical, so within the split form the assignment ORDER is inert here too; (f) closing the volatile half first (axis 1, KEPT) did NOT make the pair follow - the seven pair words are byte-for-byte what they were before the volatile block changed class. (f) is the falsifier of the run-40 denial being executed and coming back NEGATIVE, which is why this denial is a strengthened restatement rather than a new one.",
    "scope": "fn PlayerProcessPowerups ONLY, and within it only the callee-saved source/parent pair of the weaphold-FX block at src/game/game/player.c:5387-5393 (the r22/r24 transposition at +0xcfc, +0xd1c, +0xd28, +0xd34, +0xd54, +0xd5c, +0xd8c). It denies nothing about the volatile permutation at +0xdc0..+0xdf0, nothing about clusters (b) and (c), and nothing about any other function."
  },
  "function": "function:PlayerProcessPowerups",
  "held_fixed": "Every probe held the run-40 close FIXED as its base: the four kept levers of attempt.SA_playerprocesspowerups-pool-literal-and-propagation-drive.20260902.v1 (pool literals, the #pragma opt_propagation bracket with u8 unused[112], the PLAYER_SET_FAMILIAR split declarations, the weaphold split declarations) were left in place for every axis except axis 4, which varied the pragma ALONE and restored it. Axes 1, 2, 3 and 5 varied only the tier statement and held the source/parent pair fixed; axes 7 and 8 varied only the source/parent pair and held the tier statement at its kept axis-1 form; axis 6 varied only the float compare. No probe varied two of those three sites at once.",
  "hypothesis": {
    "cheapest_refuting_observation": "python tools/gdl/fnasm.py game/game/player.c PlayerProcessPowerups 0x0:0x40 --diff after any such reorder: if the target's +0x20 load still names a different FPR from ours while both remain above `mr r31,r3`, the hoist is not what selects the register and cluster (b) is allocator variance too.",
    "screened_against_target": "yes - the aligned view at 0x0:0x70 and at 0xcf0:0xdf8 was read at commit e77ef10ed, and every offset quoted in this record is a row from `wf_word_diff --list` at that commit, not a paraphrase.",
    "statement": "The whole of cluster (a2) is now a single bijective 5-cycle over dead scratch registers and no source axis remains that reaches it, so the next lane's step 1 is NOT another source probe on the tier block - it is the rule-request record rule_request.SA_playerprocesspowerups-tier-block-bijective-scratch-permutation.20260903.v1. The one source question still genuinely open is cluster (b), and its untried axis is the ONE thing that distinguishes the first inlined player_scale_att expansion from the other three: its `*att` load is scheduled ABOVE `mr r31,r3` and therefore reads the raw parameter register r3, while expansions two through four read r31. The untried step is to make the FIRST expansion's argument something that cannot be hoisted above the `p` copy - for instance by giving `p` a use before line 5021, or by reordering the four `player_scale_att` statements so that a different attribute is first - and then re-measuring whether the target's non-coalescing `lfs f0` + `fmul f2,f3,f0` form appears. Do NOT edit player_scale_att itself: it has eight expansions across two functions and seven of them are currently byte-exact."
  },
  "id": "attempt.SA_playerprocesspowerups-volatile-half-is-a-bijective-scratch-permutation.20260903.v1",
  "kind": "attempt",
  "outcome": "improved",
  "recorded_at": "2026-09-03T01:28:58Z",
  "residual": {
    "capability_needed": "webfrank-recolor",
    "family": "regalloc-web",
    "measured_at": "2026-09-03",
    "signature": "0t opcode multiset IDENTICAL (1598/1598); insns T1598/O1598; 0 ops clusters; 24 raw differing words"
  },
  "residual_class": "REGISTER_ONLY",
  "schema_version": 1,
  "supersedes": "attempt.SA_playerprocesspowerups-pool-literal-and-propagation-drive.20260902.v1",
  "valid_from": "2026-09-03"
}
