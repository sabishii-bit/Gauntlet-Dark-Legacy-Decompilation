{
  "attempted_axis": "pbWinSetup dir-light loop induction-variable initialization: four distinct source shapes (chained named byte-offset accumulator `off = i = 0;` with `off += 0x20`; loop-body pointer temp to force RHS-before-LHS IV creation; splitting the nested `(g = gWinGlobals)->lights` reassignment into its own statement; moving `i = 0` into the for-init) aimed at target's `addi r4,r6,0` where we emit `li r4,0`",
  "attributes": {
    "law_screen": "claim.law.chained-init-constant-share.20260829.v1 APPLIED as the primary hypothesis (target's `li r6,0` immediately followed by `addi r4,r6,0` is the integer analogue of the law's fmr-from-inner-variable tell) - NEGATIVE, real 14 -> 30, and critically the --ops cluster did NOT move: MWCC constant-folded `off = i = 0` back to a fresh `li` because i is statically 0 there, so the law's mechanism does not reach a compiler-created induction variable. claim.law.sizeof-defeats-loop-stride-induction.20260830.v1 and claim.law.recomputed-index-array-form-safe.20260830.v1 screened; the recorded induction guidance that `base + i*stride` beats a named offset accumulator was CONFIRMED here in the negative direction (the named accumulator cost 16 real). claim.law.declaration-initializer-split-controls-entry-schedule.20260830.v1 APPLIED via the split-g probe - NEGATIVE, real 14 -> 38: the nested `(g = gWinGlobals)->lights` assignment-in-expression is the form the target wants, and hoisting it to its own statement split g's register web further. claim.law.independent-statement-reorder-matches-target-schedule.20260830.v1 APPLIED via the for-init move - NEUTRAL. claim.law.point-of-use-single-use-pointer-delay.20260830.v1 and claim.law.reload-vs-cache-liveness.20260829.v1 screened, consistent with the split-g negative. lwzu-idiom / cast-transit / offsetof families screened: the one raw-offset site `(u8*)lb + 0xBC + i*0x20` was left raw deliberately, since every re-spelling of it regressed.",
    "laws_applied": "[\"claim.law.chained-init-constant-share.20260829.v1\", \"claim.law.declaration-initializer-split-controls-entry-schedule.20260830.v1\", \"claim.law.independent-statement-reorder-matches-target-schedule.20260830.v1\"]",
    "residual": "283/283 instructions, fuzzy 98.55124%, real 14, unchanged. NOTE the park record attempt.parked.pbwinsetup.20260829.v1 was STALE: it recorded fuzzy 95.22262 / real 48; the function had since improved to 98.55 / real 14, so this was a legitimate reopen candidate. Exactly three --ops clusters remain and ALL FOUR probes left them bit-for-bit unchanged, moving only the surrounding register colors (real 14 -> 30 -> 24 -> 38 -> 14): (a) @0x188 target `li r3,65; stw r3,476(r29); lis r3,17200` vs ours `li r4,65; lis r3,17200; stw r4,476(r29)` - target reuses r3 for the 65 and then for the int-to-float magic high word, we take r4 and hoist the lis one slot; (b) @0x2c0 target reuses r30 for the post-pbProjCalc gWinGlobals reload (one coalesced web for `g` across the whole function) while ours colors the second web r27 - 3 diff lines, and the ONE source lever for it (splitting the assignment out of the expression) made it worse; (c) @0x2c4-0x2d4 the only opcode-multiset difference in the whole function, target-only +1 addi / ours-only -1 li: of the two compiler-created induction variables for `dirs[i] = (f32*)((u8*)lb + 0xBC + i*0x20)` (byte index stride 4, byte offset stride 0x20), target materializes the stride-0x20 one as `addi r4,r6,0`, a copy of i's just-materialized zero, while we rematerialize with `li r4,0`. Since `i` is provably 0 at loop entry both forms are value-identical and the choice is made by MWCC's allocator when it materializes the IV's entry value, downstream of anything the source expresses - four source shapes that swung the surrounding coloring by 24 real never perturbed this one instruction. DO-NOT-RETRY: named offset accumulator, chained integer init, loop-body address temp, for-init vs pre-loop `i = 0`, splitting the g reassignment.",
    "scope": "src/game/pb/pb_window.c, pbWinSetup body only; all four probes reverted, tree identical to HEAD",
    "verification": "tools/gdl/probe.py --ops per probe against a clean BASELINE banked before the first edit (BASELINE real 14; probes 30 / 24 / 38 / 14; insns 283/283 throughout); reverted via probe.py --revert to the banked best; tools/gdl/defake_gate.py check --rebuild = GATE OK; full ninja green, build/GUNE5D/main.dol OK, sha1 540bed0b54c94c6fc63e94b6db20ec927cc66b53; git status clean"
  },
  "function": "function:pbWinSetup",
  "id": "attempt.pbwinsetup-iv-init-materialization.20260831.v1",
  "kind": "attempt",
  "outcome": "capped",
  "recorded_at": "2026-08-31T04:38:50Z",
  "residual_class": "REGISTER_ONLY",
  "schema_version": 1,
  "supersedes": "attempt.parked.pbwinsetup.20260829.v1",
  "valid_from": "2026-08-31"
}
