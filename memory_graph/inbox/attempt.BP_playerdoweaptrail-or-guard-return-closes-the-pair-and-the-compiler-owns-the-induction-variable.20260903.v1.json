{
  "attempted_axis": "Close the +0x3c branch pair (target `bne ->44` + `b ->21c`, ours a single fused `beq ->21c`) and the +0x104 extra `mr`, in game/sfx/psfx.c.",
  "attributes": {
    "law_screen": "claim.law.BF_the-unfused-branch-pair-is-an-inlined-callee-return-not-any-return.20260903.v1 SCREENED and its CURE was measured DEAD here twice (P1/P2 below) while its PREDECESSOR's `if (A || B) { return; }` form from claim.law.NM_branch-pair-fusion-is-blocked-by-a-return-not-by-a-goto.20260903.v2 PART 1 closed the pair on the first probe -- so the refinement's cure is not a replacement for v1's, it is a second member of the same toolkit. claim.law.dead-nominal-local-area-tracks-declaration-count.20260831.v1 APPLIED and explains P1's failure (the lift moved every local out of the outer function and the frame fell 128 -> 120). claim.law.MV_callee-saved-numbering-has-a-width-class-ahead-of-declaration-order.20260902.v1 SCREENED: P5 proves declaration order DOES reach codegen for this function's word-sized initialised class.",
    "laws_applied": "[\"claim.law.NM_branch-pair-fusion-is-blocked-by-a-return-not-by-a-goto.20260903.v2\", \"claim.law.dead-nominal-local-area-tracks-declaration-count.20260831.v1\", \"claim.law.MV_callee-saved-numbering-has-a-width-class-ahead-of-declaration-order.20260902.v1\"]",
    "probed_form": "P1 NEGATIVE (real 102 -> 177, T140/O137, frame 120 vs 128): moved everything from the gFrameTicks guard onward into `static inline void PlayerFadeWeapTrail(PlayerTrailState* player, s32 lastIndex)` with `if (gFrameTicks == 0) { return; }` as its first statement, leaving `void PlayerDoWeapTrail(p) { if (p->activeNode == NULL) { return; } PlayerFadeWeapTrail(p, p->lastTrail); }`. P2 NEGATIVE, IDENTICAL SCORE (real 177, T140/O137): the same lift with `lastIndex` restored as a LOCAL of the inline (`s32 lastIndex = player->lastTrail;`) and only `player` passed, to give the frame its declaration back -- the aligned view shows the guard STILL folded to `beq ->210` and the four zero-inits sank past the activeNode guard, so the early return at the TOP of an inline is not a merge point and does not carry the pair. P3 NEGATIVE (real 104, multiset 4t -> 6t): `switch (gFrameTicks) { case 0: goto done; }` -- the one-case switch retyped the compare (`cmplwi` -> `cmpwi`) and still fused. P4 the KEEP, part 1: `if (player->activeNode == NULL || gFrameTicks == 0) { return; }` replacing the two separate `if (...) { goto done; }` guards, plus deleting the now-unreferenced `ticks_active:` and `done: ;` labels -- the pair landed byte-identical (`bne ->44` / `b ->220`) and the multiset fell 4t -> 1t with only the +0x104 `mr` left. P5 the KEEP, part 2: DELETED the second loop's explicit offset accumulator, `offset = lastIndex * 4; while (...) { lastIndex--; offset -= 4; }` becoming `while (lastIndex >= 0 && player->trails[lastIndex] == NULL) { lastIndex--; }` and letting MWCC create the induction variable itself -- that removed the `mr` and regained count parity. P6 the KEEP, part 3: declaration order `minimumAlpha, maximumAlpha, minimumIndex, maximumIndex` (alpha pair first). Two forms of P4's leftover mr were NEUTRAL-IDENTICAL first and are dead: rewriting the loop as `for (offset = lastIndex * 4; ...; lastIndex--, offset -= 4)`, and giving the second loop its own `s32 tailOffset` local (both produced byte hash d774da97b552, unchanged).",
    "residual": "real 24 at count parity with an IDENTICAL opcode multiset: a two-web callee-saved transposition. `savedregs.py` reports role mismatch on exactly r31 and r29 -- target holds `li 0` (minimumIndex) in r31 and `li 255` (minimumAlpha) in r29, ours the reverse -- with r30/r28 unresolvable by that tool because both hold `li 0`. The declaration-order lever moved it from six mismatched webs to two: order [minAlpha, maxAlpha, minIdx, maxIdx] gives the clean descending run r31..r28 (real 24) while BOTH orders that put an index pair first, [minIdx, maxIdx, minAlpha, maxAlpha] and [minIdx, maxAlpha, minAlpha, maxIdx], give r31, r30, r25, r24 with the alphas demoted below the parameter and `index` (real 78). Making `lastIndex` uninitialised-plus-assignment instead of an initialised declaration was NEUTRAL-IDENTICAL, so the initialised/uninitialised class boundary is proved not to move these two.",
    "scope": "src/game/sfx/psfx.c::PlayerDoWeapTrail only. Committed as f5fa7a8e8.",
    "verification": "`probe.py game/sfx/psfx PlayerDoWeapTrail --ops` BASELINE real 102, insns T140/O140, multiset 3t... 4t, FRESH FUZZY 96.7857%. Final state: real 24, insns T140/O140, multiset 0t, FRESH FUZZY 99.6286%. `defake_gate.py check game/sfx/psfx --rebuild` GATE OK. Full `ninja -j2` prints `build/GUNE5D/main.dol: OK`; PROGRESS Data held at 178704 bytes across the keep (the `static inline` emitted no standalone body -- `powerpc-eabi-nm build/GUNE5D/src/game/sfx/psfx.o` returns nothing for the helper name)."
  },
  "function": "function:PlayerDoWeapTrail",
  "held_fixed": "Each of the five probes changed exactly one thing and every other statement, declaration and label in the function was held byte-identical: (P1/P2) the guard region only, (P3) the guard spelling only, (P4) the second loop's offset accumulator only, (P5) the four initialised declarations' ORDER only, with their initialisers and every use unchanged.",
  "hypothesis": {
    "cheapest_refuting_observation": "One probe of [maxAlpha, minIdx, minAlpha, maxIdx]. If it reads real 78 like the other two index-first orders, the assignment is not following declaration order at all in this class and the axis is dead in three measurements.",
    "screened_against_target": "yes -- `savedregs.py game/sfx/psfx PlayerDoWeapTrail` was read at every state and the target's first-definition column (r31 `li 0` @0x14, r30 `li 0` @0x18, r29 `li 255` @0x1c, r28 `li 0` @0x20, r27 `lwz 2048(r3)` @0x28, r26 `mr r3` @0x10) is the table this hypothesis is fitted to.",
    "statement": "The remaining r31<->r29 transposition is reachable by a declaration order this lane did not try, because the two orders measured both moved the alpha pair AND the index pair together. Probe the four orders that keep the alpha pair contiguous but move only ONE member across the index pair -- [maxAlpha, minIdx, minAlpha, maxIdx] first, since it is the only one whose emitted constant sequence (0, 0, 255, 0) matches the target's while keeping the alpha pair adjacent."
  },
  "id": "attempt.BP_playerdoweaptrail-or-guard-return-closes-the-pair-and-the-compiler-owns-the-induction-variable.20260903.v1",
  "kind": "attempt",
  "outcome": "improved",
  "recorded_at": "2026-09-03T17:45:21Z",
  "residual": {
    "capability_needed": null,
    "family": "regalloc-web",
    "measured_at": "2026-09-03",
    "signature": "target-only: (none)  ours-only: (none) -- opcode multiset IDENTICAL 140/140 after the keep"
  },
  "residual_class": "REGISTER_ONLY",
  "schema_version": 1,
  "valid_from": "2026-09-03"
}
