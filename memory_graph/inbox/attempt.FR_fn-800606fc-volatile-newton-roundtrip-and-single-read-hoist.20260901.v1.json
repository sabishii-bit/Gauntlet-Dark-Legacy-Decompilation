{
  "attempted_axis": "Cluster-decomposed drive on the 2221-instruction gauntworld dispatcher: close the three target-only stfs/lfs Newton-refinement memory round-trips by making the existing f32 root temporaries VOLATILE (the untried opposite of the prior lane's root-REMOVAL probe), and close a redundant double-read of a pointer expression whose CSE is defeated by an intervening store to an SDA global.",
  "attributes": {
    "law_screen": "claim.law.store-placement-family.20260829.v1 APPLIED and now SERVED: its 'two-phase idiom' entry describes exactly this stfs+lfs round-trip class and previously 'offered no counter-form that reproduced target here' \u2014 the counter-form is a volatile-qualified local, and this record supplies it. claim.law.cached-base-alias-defeats-rematerialized-index-base.20260831.v1 SCREENED against the T[357:365] neg cluster and MEASURED INAPPLICABLE (see refutation in residual: the source is ALREADY in the law's prescribed rematerialized form). claim.law.compare-form-dictionary.20260829.v1 SCREENED for the two branch-polarity clusters; the continue/goto respelling it suggests FOLDED AWAY before codegen (NEUTRAL-IDENTICAL). claim.law.reload-vs-cache-liveness.20260829.v1 and claim.law.whole-fn-address-cache.20260829.v1 APPLIED to diagnose the T[1572] double-read: an intervening store to an SDA global defeats CSE of a pointer load, and a single named local restores it. claim.law.fuzzy-can-underweight-a-real-improvement SCREENED and used in its INVERSE direction to arbitrate the volatile keep (see the explicit metric conflict recorded below). Core-screen defake laws screened, none applicable: no raw-offset conversion attempted.",
    "laws_applied": "[\"claim.law.store-placement-family.20260829.v1\", \"claim.law.reload-vs-cache-liveness.20260829.v1\"]",
    "probed_form": "KEPT, commit 4de3b15b0 \u2014 `f32 root;` became `volatile f32 root;` at BOTH Newton-refinement reciprocal-sqrt sites (the case-1/0xF arrow-drop block and the case-2-else 4-player-distance loop); no other text changed. KEPT, commit 8c1f1cdb7 \u2014 in the case-9 subtype-0x32 block, `s32 rec;` was added to the block's declaration list after `s32 pbits = *(s32*)&it->data[4];`, and the adjacent pair `lbl_8034481C = (u8)*(s16*)&it->data[0] + 3; SaveAllRecords(i, b, gPlayers[b].pos, (u8)*(s16*)&it->data[0]);` became `rec = (u8)*(s16*)&it->data[0]; lbl_8034481C = rec + 3; SaveAllRecords(i, b, gPlayers[b].pos, rec);` with the assignment left at its original program point (after `it->active |= 1;`) so the load is not hoisted above the store. NEGATIVE and REVERTED (real 1354 -> 1512, +158, opcode multiset UNCHANGED so it produced no extsh at all): rewriting `wob = (s16)(...); *(s16*)&it->data[8] = wob;` as the chained assignment `*(s16*)&it->data[8] = wob = (s16)(...);` to try to force the target's `extsh r0,r0` before the `sth`. NEUTRAL-IDENTICAL and REVERTED (object bytes unchanged): rewriting the lookout-param scan `for (i = 0; i < n; i++) { if (lp->param == 0) { goto lookout_found; } lp++; }` as `for (i = 0; i < n; i++) { if (lp->param != 0) { lp++; continue; } goto lookout_found; }` to try to reproduce the target's `bne`-to-increment plus explicit `b`-to-exit pair.",
    "residual": "TWO CLUSTERS CLOSED AND ONE PREMISE REFUTED. Closed: T[884:886] and T[1972:1974], both `stfs`/`lfs` round-trips the target performs and we kept in registers; the cluster census fell 37 -> 33 and the opcode multiset 16t -> 10t, with `+2 lfs +2 stfs` leaving the target-only set entirely. The third round-trip site, T[1017:1029], survives but is now a pure REORDER (no missing opcodes) and is schedule-class. REFUTED PREMISE, worth more than the score: attempt.fn-800606fc.20260830.v2 left the T[357:365] `neg` cluster with the implied cure 'target recomputes -t fresh after the call; ours caches it', which reads as an instruction to rematerialize. The source ALREADY spells `RandInt(-t) + ((-t) >> 1)` in full at BOTH use sites (lines 4613 and 4645) \u2014 MWCC CSEs `-t` into a callee-saved register regardless. That cluster is NOT reachable by rematerialized spelling and the next lane must not spend a probe re-deriving it. Remaining open multiset after this pass: target-only `+1 addi +2 b +1 bne +1 extsh +1 mr. +1 neg`, ours-only `-1 beq -1 cmpwi -1 mr`. The `+1 addi -1 mr` pair is the generate_enemy 6th argument (target `addi r8,r31,0`, ours `mr r8,r31`); it was deliberately NOT probed because Item offset 0 is `info` (resolved via gdlmem struct Item --offset 0x0), so the only source spelling that yields an offset-0 address computation is `&it->info`, which is exactly the contrived form the fakematch policy forbids \u2014 classify it as a peephole artifact, not a source lever. The `+1 mr. -1 cmpwi` pair is the recorded-dead generate_enemy slot register choice.",
    "scope": "src/game/world/gauntworld.c, fn_800606FC only; two commits, both matching edits. No header touched. gauntworld carries 2 WebFrank pins (fn_8005FB48, fn_8005FDA8); neither is this function, and no edit added or removed a literal, so the anonymous pool population was never disturbed.",
    "verification": "probe.py clean-state BASELINE banked before the first edit: real 1354, insns T2221/O2215, multiset 16t; baseline fuzzy MEASURED LIVE at HEAD on a clean tree = 97.2557% (the prior record's 97.25566% at real 1362 was stale; the TU had already improved to real 1354). Both volatile probes returned CONFLICT (real up, structure down) and were arbitrated on FRESH report fuzzy per AGENTS discipline 9c, never on real: site 1 gave fuzzy 97.2557 -> 97.3449 with count distance 6 -> 4 and multiset 16t -> 14t; site 2 gave 97.3449 -> 97.4295 with count distance 4 -> 2 and multiset 14t -> 12t. The single-read hoist then returned a clean IMPROVED on every axis: real 1358 -> 1356, multiset 12t -> 10t, fuzzy 97.4295 -> 97.5196. defake_gate check reports NO sibling regression in either commit. Full `ninja -j2` after the final commit = 'build/GUNE5D/main.dol: OK', exit 0; project totals unchanged at 98.41% fuzzy / 65.92% matched / 2706 functions. EXPLICIT METRIC CONFLICT, isolated by A/B and reported rather than hidden: with the volatile pair REMOVED and the hoist retained, the function measures real 1352 / insns 2213 / multiset 14t / fuzzy 97.3458 and defake_gate says 'GATE OK (1 improved)'; with the volatile pair PRESENT it measures real 1356 / insns 2217 / multiset 10t / fuzzy 97.5196 and defake_gate says 'REGRESSION real 1354 -> 1356, genuine structural rows 71 -> 75'. The volatile form was KEPT against the real-based gate on the ground that it emits four instructions the target genuinely contains (two verified cluster closures) and is therefore the only one of the two states on a path to byte-exact, while `real`/rows inflate on downstream register colouring; the gate red is deliberate, isolated, and flagged here for integrator judgement rather than suppressed."
  },
  "function": "function:fn_800606FC",
  "held_fixed": "Each of the three edits was probed and arbitrated SEPARATELY, and an explicit A/B isolation was run at the end with the two volatile qualifiers removed while the single-read hoist was HELD CONSTANT, so the two levers' contributions are independently measured and reported. The generate_enemy slot register (r3-vs-r4) axis and the `root`/`d2` two-step form were held CONSTANT and never touched, both being recorded do-not-retry from attempt.fn-800606fc.20260830.v2.",
  "id": "attempt.FR_fn-800606fc-volatile-newton-roundtrip-and-single-read-hoist.20260901.v1",
  "kind": "attempt",
  "outcome": "improved",
  "recorded_at": "2026-09-01T23:00:19Z",
  "residual": {
    "capability_needed": null,
    "family": "schedule-window",
    "measured_at": "2026-09-01",
    "signature": "DIFFERS target-only: +1 addi +2 b +1 bne +1 extsh +1 mr. +1 neg ours-only: -1 beq -1 cmpwi -1 mr; insns T2221/O2217; 33 ops clusters"
  },
  "residual_class": "MIXED",
  "schema_version": 1,
  "supersedes": "attempt.fn-800606fc.20260830.v2",
  "valid_from": "2026-09-01"
}
