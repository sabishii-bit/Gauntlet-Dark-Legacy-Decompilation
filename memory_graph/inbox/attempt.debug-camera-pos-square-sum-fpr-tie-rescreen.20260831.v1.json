{
  "attempted_axis": "re-screen the 2026-08-23 P9/P4 square-sum load/FPR schedule tie against the laws recorded since; three distinct source-shape axes on the sole residual cluster, then re-arbitrate the results with objdiff fuzzy after discovering real/multiset are blind here",
  "attributes": {
    "axis_log": "AXIS1 (neutral) permuted the three same-type f32 declarations to put `scale` ahead of `distance`/`extent`, aiming the proven declaration-order register-home lever straight at the FPR tie: real 12 unchanged. RE-MEASURED with a full ninja + objdiff fuzzy after the AXIS2 discovery below: fuzzy 99.4450%, i.e. genuinely inert, independently confirming the 2026-08-23 park's 'declaration order invariant' note with the finer instrument. AXIS2 (measured NEUTRAL by probe, later proven a REGRESSION) split the contrived nested-assignment sum `distance = extent + (distance = scale + distance);` -- which is also undefined behaviour, an unsequenced double modification of `distance`, flagged by -Wunsequenced -- into two plain statements. probe.py reported NEUTRAL real 12, insns 427/427, opcode multiset IDENTICAL, and fndiff --ops showed a bit-for-bit identical cluster list, so it was committed as a byte-identical de-fakematch. A later full ninja showed debug_camera_pos fuzzy at 99.3513% against 99.4450% for the original form; reverting ONLY that hunk and rebuilding restored 99.4450%, proving the split causally regressed the object. Reverted and the revert committed. AXIS3 (neutral) reordered the three product statements to dy,dx,dz to try to flip the load-issue order per the statement-reorder law: real 12 unchanged.",
    "conclusion_for_next_agent": "CAPPED on the named mechanism above: three distinct source-shape axes (declaration order, sum-expression shape, product-statement order) leave the load-issue order and the scratch-FPR assignment untouched. DO NOT re-split the nested-assignment sum -- it looks like a free UB cleanup and every cheap metric agrees it is free, but it costs ~0.09 fuzzy points. Note the contrast with camera_mode_target, where the analogous nested form is load-bearing in the opposite direction (splitting there ADDS an fmr and costs 65 real): the nested square-sum idiom in this TU is NOT uniformly removable and each site must be measured with fuzzy, not real.",
    "law_screen": "claim.law.independent-statement-reorder-matches-target-schedule.20260830.v1 APPLIED as the diagnostic (target's first-touch of dx precedes ours) and tried -- negative, see axis_log AXIS3. claim.law.declaration-initializer-split-controls-entry-schedule.20260830.v1 NOT APPLICABLE: the three products are already plain statements, no declaration initializers involved. claim.law.volatile-pins-the-later-of-two-adjacent-reads.20260831.v1 SCREENED, NOT APPLICABLE: its precondition is two LOADS whose relative order is swapped; here the three loads issue in a different order but the decisive divergence is which scratch FPR each lands in, and pinning a local (not a global or record field) with a volatile cast would be an artificial device rather than the law's use case. claim.law.dead-local-identity-reuse-flips-callee-saved-coloring.20260831.v1 SCREENED, NOT APPLICABLE: these are scratch FPRs (f0/f1/f2/f3), not two callee-saved webs holding each other's register. claim.law.float-clamp-ternary-diamond-vs-negated-guard.20260831.v1 NOT APPLICABLE: no clamp/ternary in the cluster. claim.law.literal-sweep-needs-lfd-stfd-tell-first.20260831.v1 SCREENED: the lfd tell is absent from the cluster (the only lfd loads are the frsqrte refinement constants, already exact), so no literal sweep indicated.",
    "residual": "real 12, insns 427/427, opcode multiset IDENTICAL (pure reorder). One single cluster at fn+0x94-0xb4. TARGET: `lfs f1,332; lfs f0,336; fmuls f2,f1,f1; lfs f3,340; fmuls f0,f0,f0; lfs f1,<const>; fmuls f3,f3,f3` -- i.e. dx,dy,dz issued in source order with load/mul interleaved. OURS: `lfs f1,336; lfs f0,332; lfs f2,340; fmuls f3,f1,f1; fmuls f0,f0,f0; lfs f1,<const>; fmuls f2,f2,f2` -- dy loaded BEFORE dx and all three loads grouped ahead of the muls. Both fadds operand orders already match. NAMED MECHANISM: MWCC issues our dy load first and groups the loads, which permutes which scratch FPR each of the three squared terms lands in; the arithmetic, the operand order and every other instruction are already exact.",
    "scope": "src/game/world/camera.c::debug_camera_pos (square-sum at ~line 3551-3554); three probes applied, all reverted; net source change committed then REVERTED (see axis_log AXIS2)",
    "verification": "probe.py clean BASELINE real 12 (insns 427/427, multiset 0t) banked before the first edit. Each axis gated with probe.py; the surviving candidate additionally gated with defake_gate.py check (GATE OK) AND a full ninja + per-function objdiff fuzzy read, which is what exposed the regression. Final tree: full ninja green, build/GUNE5D/main.dol: OK, GATE OK, git diff --check clean, fuzzy restored to 99.4450%."
  },
  "function": "function:debug_camera_pos",
  "id": "attempt.debug-camera-pos-square-sum-fpr-tie-rescreen.20260831.v1",
  "kind": "attempt",
  "outcome": "capped",
  "recorded_at": "2026-08-31T14:50:43Z",
  "residual_class": "SCHEDULE (load-issue order driving scratch-FPR assignment)",
  "schema_version": 1,
  "supersedes": "attempt.parked.debug_camera_pos.20260829.v1",
  "valid_from": "2026-08-31"
}
