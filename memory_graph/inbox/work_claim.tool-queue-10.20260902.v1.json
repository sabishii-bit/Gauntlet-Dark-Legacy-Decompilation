{
  "attributes": {
    "integrator": "claude-fable-integrator",
    "scope": "TOOL lane (owns tools/gdl + memory_graph surfaces): run-39 amendment queue; one commit per item BEFORE the next; reproduce each symptom first, and any 'measured run N' below without a record id is a HYPOTHESIS. (1) FLAGSHIP, stability+speed: propose-record rebuilds the graph DB per call and died with MemoryError under fleet load twice in run 39 (UE: _import_pdb_types/_sha256; RC: ast.parse; plus a 0x800705af commit-limit crash shortly before the host restarted) - profile it, cache/incrementalize the rebuild, bound peak memory; ALSO: a bare MemoryError prints no record id and reads as rejection - wrap with id + retry guidance; ALSO: the 16KB attempt cap is enforced only after authoring - add a --size preflight that reports bytes-over + the largest fields on dry-run (PC spent ~10 calls hand-trimming). (2) probe verdict gap: a CONFLICT-shaped keep (real worse, multiset better) still banks a fuzzy-rejected state as BEST (run 39 probe A: banked at fuzzy 96.84 vs baseline 97.27 despite the run-38 fuzzy-before-bank fix) - the bank decision must be governed by fuzzy on EVERY verdict shape, and REGRESSED-vs-best lines must also print the HEAD/baseline number (MV re-based every negative mentally). (3) sibling-record consistency gate: two same-session records contradicted each other 84s apart (one mandated an instrument the other's law declares nonexistent) and a hypothesis contradicted a law in its own laws_applied - at propose, screen the hypothesis text against the laws the record itself cites + same-session records on the same function; CALIBRATE against the corpus before shipping (the hard rule). (4) laws --residual volume: 520KB spill for 6 hits (UE) - return matched rows + counts, spill only on --full. (5) implement the tool:/workflow: subject namespace for records (third recurrence; tool-behaviour laws all land in project:gdl where no tool query finds them). (6) probe --discard --function: span-restricted restore, or refuse when other functions in the TU are dirty (whole-file discard is a live footgun in multi-fn TUs, measured twice). (7) Gate E scope: it fired on prose QUOTING another record rather than the record's own residual claim (UE) - match the claim field, not narration. (8) count-asymmetry preempts the score verdict: a count change is categorical (outside every postprocessor class) and should print as its own verdict class before any real/fuzzy comparison (MV). (9) correspondence-table coverage gate: a record publishing a register correspondence must carry the byte range it covers (RC; calibrate). (10) sweep screens: add the mnemonic-divergence count + PINNED column to wf_word_diff or the roster (RC: word count alone merged two classes and ranked pinned fns first; rc_recolor_class.py has the discriminator - fold it in). Scoped tests between items, FULL suites per commit; ninja 'main.dol: OK'; expected verdict changes stated; parallel fleets please pick other TUs"
  },
  "claimed_at": "2026-09-02",
  "function": "function:TextHeightMLines",
  "id": "work_claim.tool-queue-10.20260902.v1",
  "kind": "work_claim",
  "owner": "claude-fleet-worker-T10",
  "recorded_at": "2026-09-02T22:28:43Z",
  "schema_version": 1,
  "state": "active",
  "valid_from": "2026-09-02"
}
