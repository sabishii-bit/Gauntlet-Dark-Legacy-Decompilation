{
  "attempted_axis": "Execute the predecessor's MANDATORY step 1 (widen the `type` web so it stops being a short test-only web), after first verifying its diagnosis; when the savedregs guidance named declaration order instead, probe the two declaration classes the predecessor did NOT try -- `type` above `item` (the web it actually swaps with) and `type` given an initialiser.",
  "attributes": {
    "law_screen": "claim.law.MV_callee-saved-numbering-has-a-width-class-ahead-of-declaration-order.20260902.v1 SCREENED and APPLIED -- it is exactly what probe (1) proved for the `type`/`item` pair, and probe.py cites it by id in its own NEUTRAL-IDENTICAL banner. The savedregs guidance line ('MWCC numbers callee-saved GPRs downward from r31 in groups fixed before declaration order is walked (initialised locals before uninitialised ones, and NARROW/byte-typed locals as their own class ahead of every word-sized local). Reorder WITHIN a class to move a web one position; a cross-class reorder is byte-identical and proves the boundary rather than moving it') SCREENED and is what motivated probe (2); probe (2) then measured the initialised-locals half UNREACHABLE here, because the initialiser is dead-stored before numbering. claim.law.declaration-order-numbers-the-registers-assignment-order-schedules-the-loads.20260901.v1 SCREENED and already MEASURED FALSE for `type` in this function by the predecessor. claim.law.webfrank-cannot-close-instruction-count-deltas.20260831.v1 SCREENED and NOT a blocker (counts equal). claim.law.identical-multiset-is-blind-to-displacements.20260831.v1 SCREENED and APPLIED -- the 3 IMMEDIATE rows were read individually. Pin screen: config/GUNE5D/webfrank.json carries 8 pins on game/enemy/critter, none on CritterCollideItems; no edit was retained so no pin could move. NOTE FOR THE CORPUS: the predecessor record's own pin screen states 'config/GUNE5D/webfrank.json carries no pin on any game/enemy/critter function', which was true when written (2026-09-03T22:57) and is FALSE today -- the 8 pins were added later the same day, and that stale line is the origin of the run-51 dispatch census's 'critter has none' claim.",
    "laws_applied": "[\"claim.law.MV_callee-saved-numbering-has-a-width-class-ahead-of-declaration-order.20260902.v1\"]",
    "probed_form": "PROBE (1): `s32 index; s32 type; u8 *item; f32 *cpos;` replacing `s32 index; u8 *item; s32 type; f32 *cpos;`. PROBE (2): `s32 index; u8 *item; s32 type = 0; f32 *cpos;` replacing the same. Both reverted with `probe.py game/enemy/critter CritterCollideItems --discard`.",
    "residual": "Unchanged from the predecessor and re-measured: 4 clusters + 3 IMMEDIATE rows, being ONE load-order mechanism seen at two sites (where the source says `X[0] + delta[0]` the target loads `delta[0]` first and ours loads `X[0]` first) plus the clean 5-cycle callee-saved permutation pivoting on `type`. This pass closes the declaration-list axis on that pivot; the load-order third class and the live-range cure are both still open.",
    "scope": "src/game/enemy/critter.c::CritterCollideItems -- NO RETAINED CHANGE. The file is byte-identical to HEAD (`git status --short` shows only the untracked LANE_LOCK) and the function remains at the run-50 keep, real 68 / fuzzy 95.9932%. No header, config, webfrank.json or symbols.txt change.",
    "verification": "Baseline re-measured live at head c769d9f21 and it REPRODUCES the predecessor's record exactly: real 68, T148/O148, multiset 0t, the same 4 clusters and the same 3 IMMEDIATE rows (`T[10]@28 T: lfs f1,92(r27) O: lfs f0,0(r4)` [PAIRING UNRELIABLE], `T[60]@f0 T: lfs f0,0(r28) O: lfs f1,60(r7)`, `T[62]@f8 T: lfs f1,60(r7) O: lfs f0,0(r29)`). savedregs reproduces the 6-row ROLE MISMATCH and the `LIFETIME PERMUTATION: 9 live range(s)` line verbatim. After `--discard`: `RESTORED real 68 (insns T148/O148, multiset 0t)`. `defake_gate.py check game/enemy/critter` = `GATE OK`; full `ninja -j2` = `build/GUNE5D/main.dol: OK`."
  },
  "denial": {
    "expiry_check": "python tools/gdl/savedregs.py game/enemy/critter CritterCollideItems -- re-probe only if the `LIFETIME PERMUTATION: 9 live range(s) -- r25->r27 x3, r26->r27 x2, r27->r28, r28->r29, r29->r31, r31->r26` line changes shape, or if `type`'s row stops reading target `@0x94 mr. r31,r3` against ours `@0x94 mr. r26,r3`.",
    "falsifier": "Any declaration-list edit touching `type` that moves the object bytes at all -- the denial is byte-level against digest ff257619ee6a. For the class-boundary claim specifically: any reordering of `s32 type;` and `u8 *item;` that DOES change their register assignment, which would show they share an allocator class after all.",
    "premise_measurement": "Both NEUTRAL-IDENTICAL against the SAME digest as the baseline. Baseline `probe.py game/enemy/critter CritterCollideItems --ops` at head c769d9f21: `BASELINE real 68 (insns T148/O148, multiset 0t)`, 4 clusters, 3 IMMEDIATE rows. Probe (1): `NEUTRAL real 68 (insns T148/O148, multiset 0t) [NEUTRAL-IDENTICAL: object bytes unchanged]`, `NEUTRAL-IDENTICAL-PROOF unit=game/enemy/critter fn=CritterCollideItems bytes=ff257619ee6a real=68 insns=T148/O148 multiset=0t head=c769d9f21`. Probe (2): same verdict, same digest ff257619ee6a. probe.py's own reading of a NEUTRAL-IDENTICAL declaration REORDER is the operative one: `if you REORDERED or RETYPED two declarations and the object did not move, the two are in different allocator classes and no further reordering will ever move them past each other -- that is a proved class BOUNDARY`.",
    "scope": "TWO declaration-side classes on CritterCollideItems' `type` web, which is the pivot of the recorded 5-cycle callee-saved permutation: (1) moving `s32 type;` ABOVE `u8 *item;` in the declaration list, and (2) giving it an initialiser `s32 type = 0;`. Together with the predecessor's probe (c) (`type` above `s32 hit;`) this closes the declaration-list axis for `type`. It does NOT deny a live-range change to the `fn_8005D5C8` result, and it does NOT deny the two remaining lfs load-order clusters."
  },
  "function": "function:CritterCollideItems",
  "held_fixed": "Both probes held the entire 148-instruction body, every statement, both loop bounds and all other declarations constant, and varied exactly one dimension of the `s32 type;` declaration: (1) its POSITION relative to `u8 *item;`, (2) the presence of an INITIALISER on it. The predecessor's own probe (c) had varied its position relative to `s32 hit;`.",
  "hypothesis": {
    "cheapest_refuting_observation": "Zero builds: `python tools/gdl/savedregs.py game/enemy/critter CritterCollideItems --per-web`. If the target's r31 web (`mr. r31,r3`) has the SAME number of definitions and the same last-use offset as our r26 web, then its live range is not wider in the target at all, the whole live-range framing dies with no builds, and the remaining work is the postprocessor route rather than another source probe.",
    "count_consequence": "Count-neutral is REQUIRED and not guaranteed -- lengthening a live range can add a copy. T148/O148 today, and critter.c is -Cpp_exceptions with 1460 bytes of link-level exception tables, so a count change relocates every exception range.",
    "screened_against_target": "yes -- the predecessor's savedregs table was re-measured live this session and reproduces row for row (type target r31 / ours r26; item target r29 / ours r31; c target r27 / ours r28; delta target r28 / ours r29; j target r26 / ours r27; r25 identical in both), as does its 4-cluster/3-IMMEDIATE-row --ops signature at real 68. NOT screened: no live-range edit has been built, so its effect on the numbering remains unmeasured, exactly as the predecessor recorded.",
    "statement": "`type` (s32) and `item` (u8 *) sit in DIFFERENT MWCC allocator classes, so the 5-cycle permutation cannot be reached by reordering them, and the initialised-locals class is unreachable too because `s32 type = 0;` dead-stores away before it can change the numbering. What remains is the predecessor's untried cure in its original form: give the `fn_8005D5C8` result a genuinely WIDER LIVE RANGE -- not an initialiser, which folds, but a real second read of `type` after the `while ((index = NextGridEnemy()) >= 0)` loop. That requires a semantic reason to read it, so the next lane should first establish from the target whether `type` is read anywhere our reconstruction does not read it; if it is not, the permutation is source-unreachable and the function should be routed to the postprocessor, for which the preconditions are already met (T148/O148, multiset IDENTICAL 148/148)."
  },
  "id": "attempt.CR_crittercollideitems-type-and-item-are-in-different-allocator-classes-so-the-5-cycle-is-decl-unreachable.20260904.v1",
  "kind": "attempt",
  "outcome": "negative",
  "recorded_at": "2026-09-04T01:24:20Z",
  "residual": {
    "capability_needed": "instruction_permutation",
    "family": "regalloc-web",
    "measured_at": "2026-09-04",
    "signature": "opcode multiset: IDENTICAL (148/148) -- but 3 IMMEDIATE word(s) differ at aligned same-opcode positions; 4 ops clusters (2 lfs pairs at @0x18-0x34)"
  },
  "residual_class": "SCHEDULE",
  "schema_version": 1,
  "valid_from": "2026-09-04"
}
