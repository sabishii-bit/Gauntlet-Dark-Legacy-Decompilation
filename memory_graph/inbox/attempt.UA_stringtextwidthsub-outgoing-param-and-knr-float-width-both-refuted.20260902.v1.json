{
  "attempted_axis": "Execute the predecessor record's MANDATORY step-1 hypothesis (audit the outgoing parameter area against the callee prototypes) and then close the only remaining way to reach a 20-byte parameter area without a fifth argument: a K&R old-style definition, which widens a float parameter to double.",
  "attributes": {
    "law_screen": "claim.law.parameter-save-area-width-sets-the-local-block-floor APPLIED as the framework and RE-CONFIRMED on two independent siblings this pass (FontHeight: params f32+s32 = 8 bytes, floor 16, locals start at 16 -- matches; TextHeightMLines: params f32+s32+char* = 12 bytes, floor 20, `u8 unused[16]` occupies 20..36 -- matches). The law's arithmetic is sound; what is refuted for THIS function is every source route to a 20-byte parameter list. claim.law.dead-nominal-local-area-tracks-declaration-count APPLIED -- it predicts and explains the K&R result's +8 rather than +4. claim.law.webfrank-cannot-close-a-count-asymmetric-residual SCREENED -- counts are symmetric 121/121, but the postprocessor is barred here for the reason the predecessor gave: the divergent words are addi IMMEDIATE fields, not register fields. claim.law.local-block-is-bottom-anchored-so-the-lowest-pad-is-the-shift-lever and claim.law.inner-block-pad-reaches-below-the-compiler-temps: both APPLIED by the predecessor and NOT re-probed (its four pad measurements stand).",
    "laws_applied": "[\"claim.law.parameter-save-area-width-sets-the-local-block-floor.20260901.v1\", \"claim.law.dead-nominal-local-area-tracks-declaration-count.20260831.v1\"]",
    "probed_form": "K&R OLD-STYLE DEFINITION (the only untried route to a 20-byte parameter area, since default argument promotion widens a float parameter to 8 bytes: 8 + 4 + 4 + 4 = 20 = the target's floor). The in-file forward declaration at btext.c:439 had to be unprototyped as well or MWCC rejects the definition outright with `identifier 'StringTextWidthSub(float, struct StrList *, long, long)' redeclared ... now declared as 'long (...)'`:\n    s32 StringTextWidthSub();\n...\n    s32 StringTextWidthSub(scale, p, msg, idx)\n    f32 scale;\n    StrList* p;\n    s32 msg;\n    s32 idx;\n    {",
    "residual": "UNCHANGED at real 86. THE PREDECESSOR'S HYPOTHESIS (i) IS REFUTED WITHOUT SPENDING A PROBE, which is this pass's first deliverable. It proposed that the 28-byte below-array reservation might be sized by the widest CALL's argument list read from a callee prototype rather than by this function's own parameters. The callee set is FixMLineText, MBSetFont, MBSetFontScaleSpace and MBFontStringWidth (TextLinesWidth is a file-local inline, not a call). The widest of those is FixMLineText at three pointer arguments = 12 bytes, which is already BELOW our existing 16-byte floor, so no callee prototype can be the binding constraint -- the floor is set by this function's own 4-word parameter list, exactly as the law says. Widening a callee to 20 bytes would also require a five-word call site, adding argument set-up to a stream whose opcode multiset is already IDENTICAL at 121/121, i.e. a strict regression by construction. FixMLineText is moreover byte-EXACT at three parameters, so its prototype is not in question at all. HYPOTHESIS (ii) FROM THE SAME RECORD IS ALSO WEAKENED: it proposed a 4-byte compiler temp the target materialises and we do not, but this function performs no int-to-float or float-to-int conversion (the only float work is `fmuls f31,f1,f0`), so there is no magic-number or fctiwz temp to account for, and the predecessor's own end-to-end r1-slot census found every non-array stack word identical. THE K&R ROUTE IS NOW MEASURED AND REFUTED, and it fails in TWO independent ways, which is why it closes the parameter-width axis rather than merely losing: it emits the `frsp` narrowing the predecessor predicted for the f64 route (so the instruction stream is NOT silent about float parameter width, confirming that half of the predecessor's finding), and the frame grows by EIGHT bytes to 256 rather than the four needed, because the narrowed float is charged again as nominal local area per the dead-nominal-area law. AXIS CLOSED: with a fifth parameter refuted (both callers byte-exact), an f64 parameter refuted (predecessor, by `fmuls f1` reading the incoming FPR directly), the outgoing-argument route refuted (this pass, by callee census), and K&R float widening refuted (this pass, by measurement), there is no remaining source route to a 20-byte parameter area.",
    "scope": "src/game/ui/btext.c, StringTextWidthSub's definition and its in-file forward declaration only. NO RETAINED CHANGE -- reverted; `git status --short` clean apart from the untracked LANE_LOCK, and a post-revert probe re-read real 86 / 121 insns / multiset 0t.",
    "verification": "probe.py clean-state BASELINE banked before the first edit (real 86). K&R probe: REGRESSED real 86 -> 127, insns T121/O122 (count asymmetry INTRODUCED), multiset DIFFERS `target-only +1 addi / ours-only -1 frsp -1 mr`, `stwu r1,-256(r1)` against target's -248, and probe additionally flagged the extabindex non-text section as MOVED. Reverted; full `ninja -j2` printed `build/GUNE5D/main.dol: OK` at pass end."
  },
  "denial": {
    "expiry_check": "python tools/gdl/fnasm.py game/ui/btext StringTextWidthSub --diff -- if the four array-base materialisations (`addi r23,r1,108`, `addi r5,r1,108`, `addi r25,r1,36`, `addi r5,r1,36` in target; 104 and 32 in ours) are no longer the only differing words, this denial has expired.",
    "falsifier": "Any parameter-list change to StringTextWidthSub that moves the array bases from 32/104 to 36/108 while keeping the frame at 248 and both callers (ScrollTextWidth, StringTextWidth) byte-exact.",
    "premise_measurement": "Callee census by hand against the source (widest callee FixMLineText = 3 words = 12 bytes < our 16-byte floor; FixMLineText itself byte-exact at 3 params), plus `python tools/gdl/probe.py game/ui/btext StringTextWidthSub` on the K&R form: REGRESSED real 86 -> 127, T121/O122, `stwu r1,-256(r1)` vs target -248, one `frsp` added.",
    "scope": "The PARAMETER-WIDTH axis on StringTextWidthSub only -- every route to a 20-byte incoming parameter area. It does NOT deny the below-array pad axis (the predecessor closed that separately with four measurements) and does NOT deny that some unmodelled local-block mechanism could still be found."
  },
  "function": "function:StringTextWidthSub",
  "id": "attempt.UA_stringtextwidthsub-outgoing-param-and-knr-float-width-both-refuted.20260902.v1",
  "kind": "attempt",
  "outcome": "negative",
  "recorded_at": "2026-09-02T16:39:47Z",
  "residual": {
    "capability_needed": null,
    "family": "frame-slot",
    "measured_at": "2026-09-02",
    "signature": "0t opcode multiset IDENTICAL (121/121); insns T121/O121; 7 ops clusters; real 86"
  },
  "residual_class": "STACK_LAYOUT",
  "schema_version": 1,
  "supersedes": "attempt.bb-stringtextwidthsub-param-floor-refuted-pad-axis-closed.20260901.v1",
  "valid_from": "2026-09-02"
}
