{
  "attempted_axis": "Re-screen of the branch-shape residual the prior record named as 'the genuinely reachable work' in this function (target-only mr @220/@238 with beq+b where ours emits bne). Read-only diagnosis: the residual was traced to its root and reclassified, no source probe was spent, because the diagnosis showed it is the SAME uncoalesced-copy mechanism already measured dead across two sibling functions this pass.",
  "attributes": {
    "axis_log": "C1 (read-only) decoded the @220-@23c window against source lines 6413-6422. Ours and target are SEMANTICALLY IDENTICAL: both set the flag when `(st & 47) != 0` in the first arm and when `(st & 0xF) == 0` in the second. The divergence is shape only - ours emits a single `bne` that skips the `li`, target emits `beq`-to-the-block plus an unconditional `b` over it (2 instructions), and target additionally emits `mr r4,r3` at the END OF BOTH ARMS. C2 (read-only) resolved what those two mr's mean, which the prior record could not: `s32 on` is declared at line 6339 and is NEVER INITIALIZED - it is written only inside the two conditional arms and then read at lines 6423 and 6434. Target's `beq ->220` skipping `li r3,1` and then executing `mr r4,r3` is therefore copying an UNDEFINED register, and our build reproduces the same undefined read. This is faithful reconstruction of an original-source bug, NOT a reconstruction defect, and it must not be 'fixed' - initializing `on` would diverge from target. The two target-only mr's are consequently the same value-into-callee-saved-home copy that fn_8005D730 and fn_8005F0F4 both show, and the beq+b layout is downstream of that copy needing its own basic block. NO probe was spent: the copy axis was measured dead against four source spellings plus a scope change in fn_8005D730 this same session, and re-running it here would violate the two-identical-A/B rule across the TU.",
    "law_screen": "claim.law.cached-base-alias-defeats-rematerialized-index-base.20260831.v1 - the alias axis it governs is CAPPED by the superseded record (4 source forms, 3 identical codegen results) and was HONORED, not retried: the 4 add / 5 lfsx / 1 stfsx clusters were left entirely alone. claim.law.if-else-arm-swap-controls-fallthrough-placement.20260831.v1 SCREENED and REJECTED on evidence: the outer arm order already matches target (target `beq ->228` and ours `beq ->224` both send the `flags8 & 0x20` false case to the second arm, with the `st & 47` arm as fallthrough in both), so there is no arm to swap. claim.law.float-clamp-ternary-diamond-vs-negated-guard.20260831.v1 SCREENED and REJECTED by its own stated boundary: it is explicitly scoped to FLOAT compares whose negation costs an extra instruction, and records that for INTEGER guards - which is exactly what `!(st & 0xF)` is - the same rewrite does NOT reach the target. Spending a probe here would have re-run a documented negative. claim.law.load-then-conditional-overwrite-vs-ternary.20260831.v1 SCREENED and confirmed ALREADY SATISFIED: the `*(s8*)(w+53) = (s8)(on != 0 ? 255 : 0);` ternary at line 6423 already compiles to target's exact diamond (cmpwi/beq/li 255/b/li 0/extsb), byte-aligned in both builds - no change needed. claim.law.dead-local-identity-reuse-flips-callee-saved-coloring.20260831.v1 SCREENED: no dead alternate carrier for `on`. claim.law.guard-polarity-restructure-fixes-count-parity-not-form.20260831.v1 SCREENED - its precondition (a lone guard clause being the LAST insn-count-parity gap) does not hold: this function is 6 instructions short and 4 of those belong to the capped alias axis, so a guard flip cannot restore parity here. Pinned-function screen: config/GUNE5D/webfrank.json has NO unit entry for game/world/gauntworld.",
    "laws_applied": "[\"claim.law.if-else-arm-swap-controls-fallthrough-placement.20260831.v1\", \"claim.law.float-clamp-ternary-diamond-vs-negated-guard.20260831.v1\"]",
    "residual": "REGISTER_ONLY, unchanged at real 430, insns 380/374, multiset 20t, objdiff fuzzy 93.0710%. Two named components, both now fully diagnosed: (1) the CAPPED alias/rematerialization axis - target re-forms `add rBase,rIdx` + lfsx/stfsx per wobj-table access while MWCC promotes our base+index web to a callee-saved register (11 saved GPRs / 128-byte frame vs target's 10 / 120), a rematerialize-vs-spill cost decision, 4 source forms already proven identical; (2) the branch-shape gap, now resolved to the SAME uncoalesced callee-saved-home copy as its two siblings (see claim.law.callee-saved-home-copy-coalescing-is-source-unreachable.20260831.v1), with the extra `b` a layout consequence of the copy. Both components are allocator-internal. This function cannot reach EXACT from source; it is a WebFrank REGISTER_ONLY postprocessor candidate. Do not retry the alias axis or the copy axis without genuinely new compiler evidence.",
    "scope": "src/game/world/gauntworld.c::fn_80062A00 - NO code changed, diagnosis only. Baseline retained bit-for-bit.",
    "verification": "probe.py BASELINE banked before any TU edit (real 430, insns 380/374, multiset 20t) and re-confirmed unchanged after both sibling commits via tools/gdl/fuzzy.py (93.0710%, identical to session start). Full `ninja -j2` green, build/GUNE5D/main.dol: OK."
  },
  "function": "function:fn_80062A00",
  "id": "attempt.fn-80062a00-branch-shape-rescreen.20260831.v1",
  "kind": "attempt",
  "outcome": "capped",
  "recorded_at": "2026-08-31T15:47:15Z",
  "residual_class": "REGISTER_ONLY",
  "schema_version": 1,
  "supersedes": "attempt.fn-80062a00-alias-lever.20260831.v1",
  "valid_from": "2026-08-31"
}
