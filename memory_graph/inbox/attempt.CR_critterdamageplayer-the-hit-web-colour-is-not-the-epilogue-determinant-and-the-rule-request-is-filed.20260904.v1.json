{
  "attempted_axis": "Spend the ONE new axis the work order allows on the axis the predecessor's denial explicitly leaves open -- the in-place-versus-distinct-destination shape of the `hit` address arithmetic, via a compound assignment -- then, per the order, run the recolor screen and file the postprocessor rule request with the full decode.",
  "attributes": {
    "law_screen": "claim.law.CR_mwcc-epilogue-restore-order-is-contextual-and-our-build-reproduces-the-target-on-108-of-109-image-wide.20260904.v1 SCREENED and NARROWED, not overturned -- its census stands and this function is still the sole image-wide anomaly; this pass eliminates ONE candidate determinant (the `hit` web colour) by moving it and watching the epilogue not move. claim.law.BG_compound-assignment-splitting-decides-in-place-versus-distinct-destination-arithmetic.20260904.v1 SCREENED and APPLIED as the probe's basis -- it is stated for FP accumulation (`fsubs fD,fD,fS` against `fsubs fD,fT,fS`) and this pass tests whether it transfers to integer address arithmetic. IT DOES: the compound assignment demonstrably changed the destination/operand shape at `@0xc0` where three prior non-compound spellings had folded away identically, so the law's mechanism transfers to the integer case. It transferred in the wrong DIRECTION for this function, which is a fact about the operand chosen, not about the law. claim.law.webfrank-cannot-close-a-count-asymmetric-residual.20260831.v1 SCREENED and NOT a blocker -- counts are 68/68, which is what makes the request below admissible. claim.law.identical-multiset-is-blind-to-displacements.20260831.v1 SCREENED and APPLIED: the multiset is IDENTICAL 68/68 so all three clusters were read from the aligned view. claim.law.pointer-walk-array-index-regression.20260830.v1 SCREENED, confirmed by the predecessor and not re-probed. PIN SCREEN, run not assumed: `python memory_graph/gdlmem.py brief game/enemy/critter` returns `webfrank_pins` of 8 (CritterGetTargetPlayers, CritterLineRootColSub, CritterResolveMultipleTargets, CritterLineNodeColSub, CritterLookForCriticalMove, CritterLineCollide, CritterDoTexmodNode, ProcessCritter) and CritterDamagePlayer is NOT among them; the probe was reverted so the function ends this pass byte-identical to HEAD and nothing pinned moved.",
    "laws_applied": "[\"claim.law.BG_compound-assignment-splitting-decides-in-place-versus-distinct-destination-arithmetic.20260904.v1\", \"claim.law.identical-multiset-is-blind-to-displacements.20260831.v1\"]",
    "probed_form": "ONE probe, reverted. Replaced `hit = &gPlayers[playerIndex];` with `hit = gPlayers; hit += playerIndex;`, changing nothing else in the block (the `Player *hit` type, the `counter` walk, both field writes and the enclosing braces were untouched).",
    "residual": "RULE REQUEST, with the full decode, filed because this lane cannot author it: `config/GUNE5D/webfrank.json` and `tools/gdl/webfrank.py` are owned this run by claude-fleet-worker-WV (`python tools/gdl/claimscope.py --audit` rows, checked before any edit). THE ASK: a COMPOSED rule on game/enemy/critter::CritterDamagePlayer -- instruction_permutation over the epilogue composed with copy_register_fields over the `hit` live range. PRECONDITIONS, ALL MEASURED AT 47ae4d37c: instruction counts 68/68 (ours/target); opcode multiset IDENTICAL (68/68); `wf_word_diff` DIFFERING WORDS = 10, MNEMONIC DIVERGENCE = 4, RELOC-SYMBOL MISMATCH = 0, PINNED = no, DECODE REGFIELD-ONLY 6 / RA-ZERO 0 / IMMEDIATE 0 / BRANCH 0 / OPCODE 4 / RELOCATED 0; probe's own `RELOC-SYMBOL SCREEN: clean` and `SLOT MAP IDENTICAL` (frame 64 both sides, 6 slots both sides). THE PERMUTATION HALF, 4 words, window @0xfc-0x108, TWO independent adjacent transpositions: ours `@0xfc lfd f31,56(r1)` / `@0x100 lmw r28,40(r1)` against target `@0xfc lmw r28,40(r1)` / `@0x100 lfd f31,56(r1)`; and ours `@0x104 addi r1,r1,64` / `@0x108 mtlr r0` against target `@0x104 mtlr r0` / `@0x108 addi r1,r1,64`. INDEPENDENCE, which is what makes the permutation legal and is stated here so the rule author does not have to re-derive it: `lfd f31,56(r1)` writes f31 and reads r1; `lmw r28,40(r1)` writes r28-r31 and reads r1; both are LOADS, so there is no store to reorder against, and their memory ranges are DISJOINT -- the lmw covers bytes 40-55 (four words at 40, 44, 48, 52) and the lfd covers 56-63. `addi r1,r1,64` writes r1 and reads r1; `mtlr r0` reads r0 and writes LR; they share no register. Both r1-relative loads precede both instructions of the second pair in BOTH streams, so the frame is still live when they read it either way. The `blr` sits at `@0x10c`, OUTSIDE the window, so `permute_instruction_atoms`' refusal of any region containing a control op does not fire. THE RECOLOR HALF, 6 words, all volatile, a per-live-range renaming rather than a global transposition: `@0xb0` ours `mulli r4,r30,13148` / target `mulli r5,r30,13148`; `@0xb8` ours `lis r3,0 @gPlayers(ADDR16_HA)` / target `lis r4,0 @gPlayers(ADDR16_HA)`; `@0xbc` ours `addi r0,r3,0 @gPlayers(ADDR16_LO)` / target `addi r0,r4,0 @gPlayers(ADDR16_LO)`; `@0xc0` ours `add r3,r0,r4` / target `add r5,r0,r5`; `@0xc4` ours `stfs f0,2324(r3)` / target `stfs f0,2324(r5)`; `@0xe0` ours `stfs f0,2280(r3)` / target `stfs f0,2280(r5)`. The mapping is THREE webs, not one bijection, and the rule author must handle it per live range: the @ha temp is ours r3 -> target r4 (@0xb8, @0xbc); the mulli product is ours r4 -> target r5 (@0xb0, and as the second source of the add); the `hit` pointer is ours r3 -> target r5 (@0xc0, @0xc4, @0xe0). Ours reuses r3 for two sequential ranges (@ha temp then hit) that map to two DIFFERENT target registers, and the target reuses r5 for two sequential ranges (mulli product then hit) -- so a whole-function register map is NOT sound here and `verify_consistent_recolor` must be run at live-range granularity. SOURCE-EXHAUSTION PROVENANCE, as the Mandatory result policy requires: four typed denials now cover this function's source surface -- the three classes of attempt.CR_critterdamageplayer-the-whole-residual-is-the-epilogue-restore-order-and-three-source-classes-fold.20260904.v1 (declaration position/scope, statement shape, and type+addressing, all NEUTRAL-IDENTICAL against digest f27bf9a8c6bf) plus this record's compound-assignment denial, which is the first probe on this function to reach codegen at all. The hypothesis above names the one remaining source lever (the save set) and it should be run before the rule ships, per the promotion directive's preference for a source-exact close.",
    "scope": "src/game/enemy/critter.c::CritterDamagePlayer only. NO EDIT RETAINED -- the probe was reverted with `probe.py --discard` and `git diff --numstat` is empty for this file at the end of the pass. No header, config, webfrank.json or symbols.txt change.",
    "verification": "Clean-state BASELINE banked before the edit (first-baseline trap honoured): real 16, T68/O68, multiset 0t, FUZZY (fresh report) 95.6618%, `RELOC-SYMBOL SCREEN: clean`, 3 clusters. The predecessor's denial was expiry-checked BEFORE the probe and HELD (the three-cluster list and real 16 at T68/O68 both unchanged), so none of its three `hit`-web classes was re-run. The probe was arbitrated on FRESH fuzzy in one call and reverted on the arbiter's verdict (see denial.premise_measurement). After `--discard` the function re-scored `RESTORED real 16 (insns T68/O68, multiset 0t)`. Full `ninja -j2` printed `build/GUNE5D/main.dol: OK`; `python -m unittest discover tools/gdl/tests -b` = `Ran 2038 tests ... OK (skipped=2)`; `git diff --check HEAD` and `git diff --cached --check` both exit 0."
  },
  "denial": {
    "expiry_check": "python tools/gdl/probe.py game/enemy/critter CritterDamagePlayer --ops -- re-probe this class only if the three-cluster list `insert O[63:64]@fc-100=['lfd'] / replace T[64:65]@100-104=['lfd'] O=['addi'] / delete T[66:67]@108-10c=['addi']` changes shape, or if real is no longer 16 at T68/O68.",
    "falsifier": "Any statement form of the `hit` address that produces the target's `@0xc0 add r5,r0,r5` operand order -- accumulating the BASE onto the INDEX product rather than the index onto the base -- while holding T68/O68 and without reintroducing a pointer-from-integer cast. The compound-assignment direction measured here accumulates the wrong operand and is the one this denial covers.",
    "premise_measurement": "`probe.py game/enemy/critter CritterDamagePlayer --ops` at head 47ae4d37c. BASELINE real 16, insns T68/O68, multiset 0t, FUZZY (fresh report) 95.6618%, `RELOC-SYMBOL SCREEN: clean`, `SLOT MAP IDENTICAL`, 3 clusters. After `hit = gPlayers; hit += playerIndex;`: `NEUTRAL real 16 (insns T68/O68, multiset 0t) [NEUTRAL-REARRANGED: OBJECT BYTES CHANGED]` with `SLOT MAP IDENTICAL` -- so unlike the predecessor's three probes, which all returned NEUTRAL-IDENTICAL against digest f27bf9a8c6bf, this edit REACHED CODEGEN and moved pure register colour. It moved it the WRONG WAY: the aligned view went from ours `@0xb0 mulli r4,r30,13148` / `@0xc0 add r3,r0,r4` (operand order matching the target's `@0xc0 add r5,r0,r5`) to ours `@0xb0 mulli r0,r30,13148` / `@0xc0 add r3,r3,r0` (operand order INVERTED against the target). `probe.py --arbitrate --vs-head` printed `BANKED (HEAD) real 16 fuzzy 95.6618% / CURRENT (working) real 16 fuzzy 95.5147% / DELTA real +0 fuzzy -0.1471 / ARBITER: fuzzy FELL -- REVERT to the banked state`. Reverted.",
    "scope": "ONE axis on CritterDamagePlayer, added to the three the predecessor denies: the COMPOUND-ASSIGNMENT form of the `hit` address computation (`hit = gPlayers; hit += playerIndex;` against `hit = &gPlayers[playerIndex];`). It does NOT deny a frame or save-set lever, and it does NOT deny the epilogue order itself as a postprocessor target."
  },
  "function": "function:CritterDamagePlayer",
  "held_fixed": "The single probe held the 68-instruction body, the frame (64), the save set (f31 + r28-r31), every statement of the damage/flags computation, the `damage_player` call and both of `hit`'s field writes constant, and varied exactly one thing: whether the `hit` address is formed by a single binary expression or accumulated onto the pointer with a compound assignment.",
  "hypothesis": {
    "cheapest_refuting_observation": "Zero builds: `python tools/gdl/fnasm.py game/enemy/critter CritterDamagePlayer 0xf8:0x110 --diff`. If the epilogue's two transpositions -- ours `@0xfc lfd f31,56(r1)` / `@0x100 lmw r28,40(r1)` against the target's `@0xfc lmw r28,40(r1)` / `@0x100 lfd f31,56(r1)`, and ours `@0x104 addi r1,r1,64` / `@0x108 mtlr r0` against the target's `@0x104 mtlr r0` / `@0x108 addi r1,r1,64` -- are ever not exactly those two swaps, the whole framing is stale and the residual must be re-decoded. Measured this pass at head 47ae4d37c: they ARE exactly those two swaps, and every instruction from `@0x0` to `@0xf4` pairs `=` or `~`. A save-set probe that changes the instruction COUNT also refutes itself immediately, since parity is the precondition of the rule request below.",
    "count_consequence": "Instruction counts are 68/68 (ours/target) and MUST stay there. That parity, plus the IDENTICAL 68/68 multiset, is the entire precondition set for the composed rule requested below; a save-set probe that widens the frame and changes the count forfeits it, per claim.law.webfrank-cannot-close-a-count-asymmetric-residual.20260831.v1.",
    "screened_against_target": "yes -- the aligned views at 0xb0:0xe8 and 0xd0:0x114 were read at head 47ae4d37c in both the baseline and probe states, and every register quoted in this record is quoted at its own offset in both streams. NOT screened: the save-set lever itself, which has not been compiled, and the determinant of the epilogue order, which remains unidentified after this pass as it was after the last.",
    "statement": "THE `hit` WEB'S COLOUR IS NOT THE EPILOGUE DETERMINANT, and that is this pass's contribution to the open question claim.law.CR_mwcc-epilogue-restore-order-is-contextual-and-our-build-reproduces-the-target-on-108-of-109-image-wide.20260904.v1 leaves unresolved. The probe below MOVED the `hit` web's colour -- the object bytes changed and the register fields at `@0xb0`, `@0xc0`, `@0xc4` and `@0xe0` all rotated -- while the epilogue's three clusters at `@0xfc-0x10c` stayed byte-for-byte unchanged. One candidate determinant is therefore eliminated in one build, and the next lane should not re-probe the `hit` web hoping to steer the epilogue. What remains untried, and is the concrete next step: the epilogue restore order is emitted from MWCC's frame-teardown template, and the ONE input to it this function has not varied is the SAVE SET itself -- adding or removing a callee-saved local (for example widening a local so a fifth GPR is saved, taking `stmw r28,40(r1)` to `stmw r27,36(r1)`) moves the function out of the exact configuration the census pins it at (4 saved GPRs, 1 inline FPR, FPR slot above the GPR block, f31) where the target is `LMW_FIRST 14 of 14`. That is the frame/save-set lever the predecessor's denial explicitly does not cover."
  },
  "id": "attempt.CR_critterdamageplayer-the-hit-web-colour-is-not-the-epilogue-determinant-and-the-rule-request-is-filed.20260904.v1",
  "kind": "attempt",
  "outcome": "capped",
  "recorded_at": "2026-09-04T06:01:37Z",
  "residual": {
    "capability_needed": "a COMPOSED rule: instruction_permutation over the 4-word epilogue window (two independent adjacent transpositions) composed with copy_register_fields over the 6-word `hit` live-range recolor. Neither half reaches the other's words -- `wf_word_diff` reports `4 of 10 word(s) lie OUTSIDE every register-field class, so NO copy_register_fields rule can close this residual however small the count is`.",
    "family": "save-area",
    "measured_at": "2026-09-04",
    "signature": "opcode multiset: IDENTICAL (68/68); DIFFERING WORDS = 10, MNEMONIC DIVERGENCE = 4, REGFIELD-ONLY 6, OPCODE 4, IMMEDIATE 0, BRANCH 0, RELOCATED 0; 3 ops clusters at @0xfc-0x10c"
  },
  "residual_class": "SCHEDULE",
  "schema_version": 1,
  "valid_from": "2026-09-04"
}
