{
  "attempted_axis": "move_logic10 exact drive: kill the ours-only -2 fmr opcode-multiset residual by unifying the two spellings of the angle-normalization idiom (a f64 temp normalizing an f32 `cand` against +/-PI) onto the in-place assignment-in-condition form.",
  "attributes": {
    "axis_log": [
      "AXIS 1 (NEGATIVE, reverted): rewrite the 3-arm block at enemy.c:3100 - `f64 a; if (cand > PI) a = cand - TWOPI; else if (cand <= -PI) a = TWOPI + cand; else a = cand; cand = a;` - into the in-place form its sibling at enemy.c:3318 already uses (`if ((a = cand) > PI) a -= TWOPI; else if (a <= -PI) a = TWOPI + a;`). real 476 -> 633, insns 1087 -> 1086, multiset 2t -> 3t (now ALSO missing a `b`). DECISIVE COUNTER-EVIDENCE: the edited block is the one at target 0x3c0-0x3f8, and target HAS the `b`+`fmr` pair there - i.e. the explicit-else 3-arm spelling at line 3100 is ALREADY the correct target form and must be left alone. The two spellings in this function are not an inconsistency to be unified; they are two genuinely different original forms."
    ],
    "law_screen": "claim.law.chained-init-constant-share.20260829.v1 SCREENED as the corpus's named source of 'one extra fmr per pair' and found NOT applicable: the extra fmr here is not a shared-constant copy into two homes, it is a copy of a live value across an if/else join. claim.law.hoisted-literal-temp-colors-below-parameter-homes.20260831.v1 screened: not applicable, the copy is of a computed value, not a hoisted literal. claim.law.extern-scalar-read-defeats-fp-literal-hoist.20260831.v1 screened and NOT applied: our stream reads the +/-PI and 2PI constants as anonymous pool entries (@@706/@@707/@@708) where target reads named split labels (lbl_80346840/48/50), but the loads sit at the same offsets with the same opcodes in both streams, so reload counts are already at parity and the sweep is not indicated (attempt.enemy-extern-literal-sweep.20260831.v1 is already NEGATIVE for this TU). claim.law.value-identity / dead-local-identity-reuse-flips-callee-saved-coloring.20260831.v1 screened as the motivating idea for the axis. attempt.scalar-cache-scan.20260830.v1 screened: it had already surveyed move_logic10's clusters and classified them as 'lone fmr/register-color noise' - this pass tested that classification rather than assuming it.",
    "residual": "Unchanged from baseline: real 476, insns 1085/1087, opcode multiset DIFFERS ours-only -2 fmr, plus a `li` reorder pair at 0x50. NAMED MECHANISM for the surviving 2 fmr (at ours 0x998 and 0xf5c, both the `cand = lbl_80344720` else-arm feeding a normalization block): target COALESCES the f64 normalization temp with the incoming f32 `cand` into one callee-saved FPR (f28) and does fcmpo/fsub/fadd and the closing `frsp f28,f28` all in place; our build gives `cand` a callee-saved home (f30/f29) and the temp a volatile (f1), so the join needs `fmr f1,fXX`. Target frsp's exactly ONCE at the join, which confirms the f64 temp is real in the original source and is NOT reachable by deleting it (operating on the f32 `cand` directly would frsp per arm). NEXT UNTRIED AXIS, deliberately not started for budget reasons rather than because it is dead: declare `cand` itself as f64 for the whole of move_logic10 (its uses - sin/cos, set_enemy_trans's f32 param, e->ang stores - are all consistent with an f64 original that frsp's at each narrowing use), which would remove the temp/home split at BOTH sites at once. This is a whole-function retype and needs its own clean baseline.",
    "scope": "src/game/enemy/enemy.c, move_logic10 only; probe-reverted, NOTHING RETAINED. No files changed by this attempt.",
    "verification": "probe.py clean BASELINE banked before the first edit (real 476, insns 1085/1087, multiset 2t); axis 1 probed then `probe.py --revert` restored and RE-SCORED to NEUTRAL real 476, confirming the tree is back at baseline. Working tree subsequently confirmed clean against HEAD."
  },
  "function": "function:move_logic10",
  "id": "attempt.move-logic10-normalization-temp-coalesce.20260831.v1",
  "kind": "attempt",
  "outcome": "negative",
  "recorded_at": "2026-08-31T14:44:39Z",
  "residual_class": "STRUCTURAL",
  "schema_version": 1,
  "valid_from": "2026-08-31"
}
