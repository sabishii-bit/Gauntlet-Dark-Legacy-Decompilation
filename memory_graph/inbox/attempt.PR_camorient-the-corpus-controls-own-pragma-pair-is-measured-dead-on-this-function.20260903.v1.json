{
  "attempted_axis": "PROMOTION lane: close the last untried lever named by the cap record's OWN corpus control -- the `#pragma opt_common_subs off` half of the pragma pair that the exact-emitting analogue ui/attract.c::init_screen2d is compiled under -- first JOINTLY with the `register` staging local (the joint probe AGENTS.md discipline 6 requires when two correct-alone negatives touch the same instructions), then ALONE.",
  "attributes": {
    "law_screen": "claim.law.T19_savedregs-default-read-reports-the-postprocessor-not-the-compiler-on-a-pinned-function.20260903.v1 APPLIED -- the function is a verdict flip and its --raw role mismatch (r31) is what selected it off the re-ranked backlog; the flip turned out to RESTATE the residual the 2026-08-31 cap already names rather than to supply a new lever, which is itself the finding. claim.law.direct-global-member-flips-index-base-association.20260831.v1 and claim.law.local-alias-vs-direct-global-spelling-diverges.20260831.v1 SCREENED via the prior record, which already measured both dead here. claim.law.webfrank-pinned-function-source-freeze.20260831.v1 APPLIED -- both probes measured on the raw pre-postprocess body through probe --raw. claim.law.MV_callee-saved-numbering-has-a-width-class-ahead-of-declaration-order.20260902.v1 NOT APPLICABLE: the disputed register is the volatile r4, not a callee-saved numbering slot.",
    "laws_applied": "[\"claim.law.T19_savedregs-default-read-reports-the-postprocessor-not-the-compiler-on-a-pinned-function.20260903.v1\"]",
    "probed_form": "Probe 1: `#pragma opt_propagation off` and `#pragma opt_common_subs off` inserted immediately above `void cam_orient_to_80029E8C(s32 camIdx)`, `#pragma opt_common_subs reset` and `#pragma opt_propagation reset` immediately below its closing brace, and the body head changed from `u8* gcs = (u8*)gCameraState; Camera* cam = (Camera*)(gcs + camIdx * 396);` to `u8* gcs = (u8*)gCameraState; register s32 camOff = camIdx * 396; Camera* cam = (Camera*)(gcs + camOff);`. Probe 2: the register local and the opt_propagation pragma removed, leaving only `#pragma opt_common_subs off` above the function and `#pragma opt_common_subs reset` below it, with the HEAD body head restored verbatim.",
    "residual": "The 2-word add-destination recolor stands and its shipped rule is untouched. The 2026-08-31 cap's corpus control cited init_screen2d's pragma pair as an unexplored half of the explanation; this record closes that half by measurement, leaving the cap's stated unpark_condition (a source form keeping the index product LIVE past the add) as the only surviving source axis -- and cam_orient_to has no natural second use of the multiply, which is why the cap holds.",
    "scope": "src/game/game/combat.c::cam_orient_to_80029E8C only. Both probes reverted with `probe.py game/game/combat cam_orient_to_80029E8C --discard --whole-file`; probe re-scored RESTORED real 0, insns exact, and `git status --short` shows no modification to src/. No source change retained; the function's webfrank rule is unchanged.",
    "verification": "defake_gate baseline game/game/combat --at-head at commit 1370dce50: 46 functions, 34 at real 0, source sha1 eb2160898, PROGRESS SPLIT stamped STRICT 57.15% (2591 fns) + EQUIVALENT 11.27% (154 fns). `python -m unittest discover tools/gdl/tests -b` Ran 1779 tests, OK (skipped=2); `python -m memory_graph.test_graph --changed -b` Ran 426 tests, OK."
  },
  "denial": {
    "expiry_check": "python tools/gdl/probe.py game/game/combat cam_orient_to_80029E8C --raw followed by python tools/gdl/composed_census/wf_word_diff.py game/game/combat cam_orient_to_80029E8C -- if DIFFERING WORDS is no longer 2, or savedregs --raw no longer reports the single r31 role mismatch, the alignment moved and the pragma axis is worth one re-probe.",
    "falsifier": "Any state of src/game/game/combat.c in which the raw body emits `add r31,r5,r4` at +0x1c and `addi r31,r31,200` at +0x20 with 102 instructions and frame 112 -- that is, in which the one-instruction-lived intermediate is coloured into r31 rather than coalesced with the dying mulli operand r4.",
    "premise_measurement": "Baseline `python tools/gdl/probe.py game/game/combat cam_orient_to_80029E8C --raw --ops` at 1370dce50: BASELINE real 4, insns T102/O102, opcode multiset IDENTICAL (102/102). `python tools/gdl/composed_census/wf_word_diff.py game/game/combat cam_orient_to_80029E8C`: 102 insns, DIFFERING WORDS = 2, MNEMONIC DIVERGENCE = 0, DECODE REGFIELD-ONLY 2 IMMEDIATE 0 BRANCH 0 OPCODE 0 RELOCATED 0, CLASS RECOLOR, PINNED = YES. `savedregs.py game/game/combat cam_orient_to_80029E8C --raw` reports the single role mismatch r31: target holds `add r5,r4`, ours holds `addi r4,200` -- and the DEFAULT read says ASSIGNMENT MATCHES, so this function is one of the 84 verdict flips. Probe 1 (joint) measured REGRESSED real 4 -> 10 with the multiset still IDENTICAL. Probe 2 (`opt_common_subs off` alone) measured NEUTRAL real 4, SCORE-BLIND REARRANGEMENT with the forced slot map reading `frame: target 112 ours 112` and `SLOT MAP IDENTICAL` (12 slots each), and a re-run of wf_word_diff on that state still read DIFFERING WORDS = 2 with the same DECODE line. The pragma therefore REACHES codegen (the object bytes moved) and does not touch this residual.",
    "scope": "The `opt_common_subs off` pragma as a lever on cam_orient_to_80029E8C's two-word add-destination recolor at +0x1c/+0x20, both jointly with a `register` staging local and alone, at commit 1370dce50. It does NOT re-deny the six neutral spellings and one negative pragma already recorded in attempt.camorient-add-destination-recolor-park.20260831.v1, and it does not touch that record's unpark_condition (a source form keeping the index product live past the add)."
  },
  "function": "function:cam_orient_to_80029E8C",
  "held_fixed": "Probe 1 varied TWO things deliberately and nothing else: the scoped pragma pair (`#pragma opt_propagation off` + `#pragma opt_common_subs off` bracketing the function, reset after it) and the introduction of `register s32 camOff = camIdx * 396;` feeding `(Camera*)(gcs + camOff)`. Every statement, the `cam = (Camera*)((u8*)cam + 0xC8);` split, the declaration list and all types were held constant. Probe 2 removed BOTH the register local and the opt_propagation pragma, leaving `#pragma opt_common_subs off` as the single varied thing against the HEAD source.",
  "id": "attempt.PR_camorient-the-corpus-controls-own-pragma-pair-is-measured-dead-on-this-function.20260903.v1",
  "kind": "attempt",
  "outcome": "negative",
  "recorded_at": "2026-09-03T22:43:26Z",
  "residual": {
    "capability_needed": "webfrank-recolor",
    "family": "regalloc-web",
    "measured_at": "2026-09-03",
    "signature": "102/102 insns, RAW DIFFERING WORDS = 2, MNEMONIC DIVERGENCE 0, DECODE REGFIELD-ONLY 2; target `@0x1c add r31,r5,r4` + `@0x20 addi r31,r31,200`, ours `@0x1c add r4,r5,r4` + `@0x20 addi r31,r4,200`"
  },
  "residual_class": "REGISTER_ONLY",
  "schema_version": 1,
  "valid_from": "2026-09-03"
}
