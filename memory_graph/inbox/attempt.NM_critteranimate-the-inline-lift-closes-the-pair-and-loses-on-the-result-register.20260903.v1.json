{
  "attempted_axis": "Close the +0x60 unfused branch pair (the last operand of the four-way `||` guard that picks nextmove) by lifting the guard into a static inline helper with `return` exits -- the mechanism that closed three siblings byte-exact this run -- then chase the extra copy it leaves behind.",
  "attributes": {
    "law_screen": "claim.law.NM_branch-pair-fusion-is-blocked-by-a-return-not-by-a-goto.20260903.v2 SCREENED and APPLIED -- its part-2 inline-join clause predicted the pair would close and it DID; this record is the measured counter-example to the PAYOFF, and v2 quotes it under 'WHEN IT DOES NOT PAY'. claim.law.live-zero-copy-vs-remat-is-allocator-not-source.20260831.v1 SCREENED and applicable to the SECOND cluster (the extra `li` at +0x144 feeding `xor r0,r5,r0` where the target reuses r4 from `xor r3,r6,r4`) -- not worked here, and named so the next lane does not re-derive it. claim.law.real-can-underweight-a-large-alignment-gain SCREENED: probe reported no slot-class residual on the lifted state, so real was not being fought by a frame delta and the fuzzy arbiter was the honest tie-break. claim.law.MV_callee-saved-numbering-has-a-width-class-ahead-of-declaration-order.20260902.v1 SCREENED and CONSISTENT with probes 2 and 3: two NEUTRAL-IDENTICAL results on declaration spelling prove that class cannot reach the result register.",
    "laws_applied": "[\"claim.law.NM_branch-pair-fusion-is-blocked-by-a-return-not-by-a-goto.20260903.v2\"]",
    "probed_form": "BASELINE (retained), src/game/enemy/critter.c:\n    if (c->unk11E < 0 || c->unk120 < 0 || c->unk120 >= 8 ||\n        (candidate = ((CritterPackedType *)c->hdr)->patternsPtr[c->unk11E].sequence[c->unk120]) < 0) {\n        candidate = c->nextmove;\n    }\n    nextIndex = candidate;\nPROBE 1, real 38 -> 108, reverted:\nstatic inline s32 sCritterNextMove(Critter *c)\n{\n    if (c->unk11E >= 0 && c->unk120 >= 0 && c->unk120 < 8) {\n        s32 candidate = ((CritterPackedType *)c->hdr)->patternsPtr[c->unk11E].sequence[c->unk120];\n        if (candidate >= 0) { return candidate; }\n    }\n    return c->nextmove;\n}\n... call site: candidate = sCritterNextMove(c); nextIndex = candidate;\nPROBE 2, NEUTRAL-IDENTICAL, reverted: call site `nextIndex = candidate = sCritterNextMove(c);`\nPROBE 3, NEUTRAL-IDENTICAL, reverted: helper's `s32 candidate;` hoisted to the helper's top-level declaration list.",
    "residual": "Two clusters, both named: (1) the +0x60 branch pair, whose cure is known and whose COST is the result-register copy; (2) the extra `li` at +0x144, a live-zero-remat row -- ours materialises zero twice (`li r4,0` and `li r0,0`, feeding `xor r3,r6,r4` and `xor r0,r5,r0`) where the target materialises it once at +0x144 and feeds both `xor`s from r4.",
    "scope": "src/game/enemy/critter.c::CritterAnimate. NO source change retained; the file was restored with `git checkout --` and the TU rebuilt.",
    "verification": "Pre-edit residual measured with fndiff --ops from the committed tree. Probe 1 measured real 108 / T188/O190 and its cluster list read against the aligned view (`fnasm game/enemy/critter CritterAnimate 0x58:0x90 --diff` shows +0x60 and +0x64 as `=` and the surplus `> 70: addi r5,r3,0`). Probes 2 and 3 both NEUTRAL-IDENTICAL with the proof line quoted. Fuzzy arbitration run explicitly (`probe.py --fuzzy`, fresh report: 98.7766%). After revert: `defake_gate.py check game/enemy/critter --rebuild` GATE OK, full `ninja -j2` prints `build/GUNE5D/main.dol: OK`."
  },
  "denial": {
    "expiry_check": "python tools/gdl/probe.py game/enemy/critter CritterAnimate --ops -- if real is no longer 38, or the +0x60 cluster is no longer `T[24:26]=['blt','b']` vs `O[24:25]=['bge']`, re-run. Re-probe specifically if any lane learns to steer an inlined helper's RESULT REGISTER: the branch half is already solved here, exactly as FindWORLDOBJ's branch half was before its lift, and that one turned out to be one construct class away from exact.",
    "falsifier": "Any source form that holds the exact `blt ->68` / `b ->6c` pair at +0x60 AND leaves the value in r5 with only the single `addi r29,r5,0` at +0x70 -- i.e. the lift without the result-register copy. Either half alone is already achieved: the baseline has the register assignment and no pair; probe 1 has the pair and an extra copy.",
    "premise_measurement": "Three probes at commit 70d475512, baseline real 38 (insns T188/O188, fuzzy 98.86%), two clusters: `replace T[24:26]@60-68=['blt','b'] O[24:25]@60-64=['bge']` and `insert T[81:81]@144-144=[] O[80:81]@140-144=['li']`. Probe 1, the lift -- guard rewritten as `static inline s32 sCritterNextMove(Critter *c)` returning the pattern sequence slot or `c->nextmove`, call site `candidate = sCritterNextMove(c); nextIndex = candidate;`: the branch pair CLOSED EXACTLY (aligned rows at +0x60 `blt ->68` and +0x64 `b ->6c` both became `=`), but the inline's result landed in r3 and MWCC spent an `addi r5,r3,0` at +0x70 that the target does not have, where the target does the single `addi r29,r5,0` at +0x70 with the two `lha`s writing r5 directly. Net real 38 -> 108, insns T188/O190. Probe 2, `nextIndex = candidate = sCritterNextMove(c);`: NEUTRAL-IDENTICAL (NEUTRAL-IDENTICAL-PROOF unit=game/enemy/critter fn=CritterAnimate bytes=485dc7cf34c7 real=108 insns=T188/O190 multiset=2t head=70d475512). Probe 3, hoisting the helper's `candidate` local out of the inner block: NEUTRAL-IDENTICAL, same proof bytes. ARBITRATED, not assumed: `probe.py --fuzzy` on the lifted state measured FRESH objdiff fuzzy 98.7766% against the baseline's 98.86% -- real and fuzzy point the SAME way, so the state was reverted rather than kept.",
    "scope": "ONE cluster on ONE function: CritterAnimate's branch pair at +0x60, via the inlined-helper lift and the DECLARATION-SPELLING variants of its result assignment. It does NOT deny the lift on other functions -- three closed exact this run -- and it does not deny the function's SECOND cluster (the extra `li` at +0x144, a live-zero-remat row that was present at baseline and is untouched by this work)."
  },
  "function": "function:CritterAnimate",
  "held_fixed": "The four guard conditions and their operand order, the `nextIndex`/`candidate` variable pair and every statement after +0x74 were held constant; only the guard's CARRIER (inline `||` chain vs lifted helper) and then the assignment spelling varied.",
  "id": "attempt.NM_critteranimate-the-inline-lift-closes-the-pair-and-loses-on-the-result-register.20260903.v1",
  "kind": "attempt",
  "outcome": "negative",
  "recorded_at": "2026-09-03T16:21:04Z",
  "residual": {
    "capability_needed": null,
    "family": "branch-pair",
    "measured_at": "2026-09-03",
    "signature": "target-only: +1 b +1 blt  ours-only: -1 bge -1 li"
  },
  "residual_class": "STRUCTURAL",
  "schema_version": 1,
  "valid_from": "2026-09-03"
}
