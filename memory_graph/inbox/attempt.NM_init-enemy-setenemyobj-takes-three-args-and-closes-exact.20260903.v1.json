{
  "attempted_axis": "Close init_enemy's whole remaining residual - one redundant instruction around SetEnemyObj's 4th argument - by re-probing the four spellings the run-30 record listed as worse on the new alignment, then, when all four reproduced, questioning the PROTOTYPE instead of the call-site spelling.",
  "attributes": {
    "law_screen": "claim.law.constant-copy-vs-rematerialize-is-source-unreachable.20260831.v1 SCREENED and NOT applicable by signature: it covers `target-only +1 mr|addi / ours-only -1 li` over two locals sharing a constant with the target spelling a COPY; here the target spelled no copy and our extra instruction was a `lwz` field reload (or, in the literal spelling, a second `li`). claim.law.identical-multiset-is-blind-to-displacements.20260831.v1 SCREENED, not applicable at the start (the multiset DIFFERED) and APPLIED at the end (the closing residual was a count-symmetric 3-cycle FPR permutation read only in the aligned view). AGENTS.md discipline 16 (abicheck before prototype work) SCREENED: the disagreement here is arity, not type class, and abicheck models GPR/FPR sequence assignment rather than parameter COUNT, so it could not have surfaced this - the deciding evidence was the callee's own bytes plus a sibling call site. core-screen offsetof/lwzu/index laws: none applicable.",
    "laws_applied": "[\"claim.law.identical-multiset-is-blind-to-displacements.20260831.v1\"]",
    "probed_form": "BASELINE real 27, fuzzy 99.0074, insns T136/O137, multiset 1t; the only divergence was our extra `lwz r6,180(r31)` at +0x98, immediately after `stw r6,180(r31)` at +0x90.\nPROBE 1 (REGRESSED real 27 -> 37, still 137): `SetEnemyObj(e, type, level, ACTIVE);` - MWCC emitted TWO constants (`li r8,1` at +0x50 for the store, `li r6,1` at +0x74 for the argument) and cascaded r8/r9 through the table-row registers.\nPROBE 2 (REGRESSED real 37 -> 43, still 137; reproduces the run-30 record's 43): shared temporary `s32 state; state = ACTIVE; e->state = state; ... SetEnemyObj(e, type, level, state);`.\nPROBE 3 (REGRESSED real 43 -> 41, still 137): assignment-in-argument `SetEnemyObj(e, type, level, e->state = ACTIVE);` with `e->endurance = 0;` moved ahead of it.\nPROBE 4 (REGRESSED real 41 -> 35, still 137): maximum store-to-load adjacency, `e->endurance = 0; e->state = ACTIVE; SetEnemyObj(e, type, level, e->state);`.\nPROBE 5 (THE WIN, real 27 -> 10, T136/O137 -> T136/O136, COUNT PARITY GAINED, fuzzy 99.0074 -> 99.7426): SetEnemyObj takes THREE arguments. Changed src/game/game/gamemain.c:837 `void SetEnemyObj(Enemy* enemy, s32 type, s32 level, s32 unused)` to `void SetEnemyObj(Enemy* enemy, s32 type, s32 level)`, the extern in src/game/enemy/enemy.c:7426 to match, and the call to `SetEnemyObj(e, type, level);`. The `lwz` disappeared: the target's `li r6,1` at +0x50 was never argument setup, it is `e->state = ACTIVE`'s own constant, and every one of probes 1-4 was fighting a phantom fourth argument.\nPROBE 6 (EXACT, real 10 -> 0, multiset 0t): the surviving residual was a 3-cycle FPR permutation (target f0/f1/f2 = converted level / magic constant / A*health; ours f1/f2/f0) inside the s32-to-f64 conversion of `health = (f32)(lbl_80346A28 * health * level);`. Naming the first product forces the fresh-destination `fmul` the target has --\n        f64 scaled = lbl_80346A28 * health;\n        health = (f32)(scaled * level);\nAlso deleted the now-false source comment at 7464-7469 that documented the 4th argument as faithful.",
    "residual": "None - STRICT exact. Two `--clean`-suppressed pool-name rows remain in the readout (`@1785` where the target names `lbl_80346938`, the s32-to-f64 magic constant); they are the anonymous-vs-named KIND difference the run-41 tool queue asks fndiff to stop suppressing, and the image STRICT count rising by one confirms they are genuinely noise HERE.",
    "scope": "src/game/enemy/enemy.c (init_enemy body, its SetEnemyObj extern, and the stale comment) and src/game/game/gamemain.c (the SetEnemyObj definition's parameter list). TWO commits: c8229d5ee (arity, 27 -> 10) and 5aa9fe24b (exact). The prototype edit is FILE-SCOPE, so both TUs were gated whole per AGENTS.md; no header, struct, configure.py or webfrank.json edit. game/enemy/enemy carries 10 webfrank pins (fn_8004646C, closest_enemy, move_logic00/02/04/14/15/30, gendir_8004FBC8, find_neighbor_milestone) and game/game/gamemain carries none of relevance - init_enemy's instruction count fell by 1, so a downstream pin position could have shifted, and the full ninja after each keep replayed every rule and printed main.dol: OK with no re-derivation needed.",
    "verification": "probe.py BASELINE real 27 / fresh fuzzy 99.0074 at commit 0dda4ddc6. The prototype probe tripped probe.py's TU-SCOPE gate (file-scope declaration change, nothing banked) exactly as designed; `defake_gate.py baseline game/enemy/enemy --at-head` was taken at d9c0ac6e0 (66 functions, 54 at real 0) and `defake_gate.py baseline game/game/gamemain` at d9c0ac6e0 (77 functions, 61 at real 0) BEFORE the keep. After the arity edit: `defake_gate.py check game/enemy/enemy --rebuild` GATE OK (1 improved, no sibling regression), `defake_gate.py check game/game/gamemain --rebuild` GATE OK (SetEnemyObj stayed EXACT - an unread parameter emits no code, which is why the callee could not decide the arity), full ninja `build/GUNE5D/main.dol: OK`, image Data held at 177572 / 2361748 (7.52%). After the exact edit: `fndiff --clean` = MATCH (pool-name noise only), 0 real diff lines; `defake_gate.py check game/enemy/enemy --rebuild` GATE OK (real 27 -> 0); full ninja `build/GUNE5D/main.dol: OK`; image progress STRICT matched 56.58% (2577 fns) -> 56.63% (2578 fns), EQUIVALENT unchanged at 10.00%, All matched 66.58% -> 66.63%. git diff --check clean."
  },
  "function": "function:init_enemy",
  "held_fixed": "Probes 1-4 held everything outside SetEnemyObj's call site constant (the offset stores, the pyr zeroing, the type-table walk, the health derivation, fn_8005A338 and the shadow-node tail were untouched) and kept the four-parameter prototype fixed while varying only the argument spelling; probe 5 held every call-site spelling fixed and varied only the prototype.",
  "id": "attempt.NM_init-enemy-setenemyobj-takes-three-args-and-closes-exact.20260903.v1",
  "kind": "attempt",
  "outcome": "improved",
  "recorded_at": "2026-09-03T01:52:01Z",
  "refutes": "attempt.enemy-c-deepscrutiny.20260830.v1",
  "residual": {
    "capability_needed": null,
    "family": "no-residual",
    "measured_at": "2026-09-03",
    "signature": "no residual: fndiff --clean MATCH, 0 real diff lines, insns T136/O136, multiset 0t"
  },
  "residual_class": "NONE",
  "schema_version": 1,
  "valid_from": "2026-09-03"
}
