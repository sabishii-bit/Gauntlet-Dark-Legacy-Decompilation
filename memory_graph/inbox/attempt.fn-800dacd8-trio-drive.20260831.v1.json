{
  "attempted_axis": "work_claim.movieplayer-structural.20260831.v1 structural-residual-trio matching pass: re-verified the prior REGISTER_ONLY/SCHEDULE park via fresh --ops before treating it as a VETO, then tried further source-shape axes on the still-open residual.",
  "attributes": {
    "anchors": "src/game/movie/movieplayer.c fn_800DACD8 0x800DACD8, size 0xCC (204 insns)",
    "axis_log": "(1) IMPROVING, KEPT: single-use temp elimination. Source had 'chunk_ofs = ofs + 4; if (ReadF32LE(param_2 + chunk_ofs) != 0x66727473)' -- chunk_ofs is used exactly once, immediately after assignment, feeding directly into a call-argument address expression with no other reference in the function. This forced our compile to materialize the value through an r0 scratch register then move-into-r3, where target builds param_2+ofs+4 directly into r3 in one step. Inlining to 'if (ReadF32LE(param_2 + ofs + 4) != 0x66727473)' (removing the chunk_ofs local entirely) took real 80->76 with insns held exactly 204/204. This matches claim.law.point-of-use-single-use-pointer-delay's pattern (single-use value, point-of-use computation beats a named intermediate). (2) NEUTRAL: reparenthesizing 'q = param_2 + (u32)ofs + 0xC;' to 'q = param_2 + ((u32)ofs + 0xC);' -- zero effect, reverted; MWCC canonicalizes this addition identically regardless of source grouping. (3) NEGATIVE x2, same axis, tried both before and after the chunk_ofs win to rule out interaction: declaring local 'p' before 'q' (source originally declares q, ofs, strl, [chunk_ofs], p in that order) regressed real 80->92 pre-fix and 76->90 post-fix. The mismatched register pair -- target gives the first 'p' assignment register r30 and reuses r29 for the later q-chain (feeding the 0x194 and 0xE8 field-write blocks); ours gives 'p' r27 (shared with the 'strl' local) and the q-chain r30 instead -- did not respond to declaration-order changes in either direction. (4) NEGATIVE: splitting the chained assignment 'p = (q = param_2 + ofs) + 4;' into two separate statements ('q = param_2 + ofs; p = q + 4;') regressed real 76->101 AND changed the insn count itself (204/204 -> 204/205) -- the chained form is load-bearing for both bytes and structure; reverted immediately.",
    "conclusion": "Residual class re-verified REGISTER_ONLY/SCHEDULE via fresh --ops both before and after the improvement (opcode multiset IDENTICAL throughout) -- this function's residual genuinely does NOT match the work-claim's 'structural, not register noise' framing; the prior park's classification was correct here, unlike the sibling fn_800DBA80 case (see attempt.fn-800dba80-report-retriage.20260829.v2) where an imported legacy park turned out to be factually wrong. New best (real 76, a verified net gain) re-parked on the same register-color axis for the p/q pointer-local pair: do-not-retry plain declaration-order or chained-assignment-splitting on this pair without a genuinely new idea beyond what is logged above. Unpark condition unchanged from the superseded record: new source-level/allocator-lever/compiler evidence only; two more identical-direction A/B results on the same axis would re-park immediately, but this pass already hit that bar (2 negative decl-order results, 1 negative split result) so the axis is closed for now.",
    "law_screen": "Fetched and screened all 14 core-screen laws (dead-offset-shadow-removal-safe, declared-alias-vs-repeated-cast, large-fn-batch-fuzzy-can-mask-subfield-regression, lwzu-idiom-web-retention, multifield-alias-defeats-indexed-addressing v2, offsetof-fused-immediate-counter, offsetof-overturns-typed-alias-caps, offsetof-rename-array-member-boundary, offsetof-rename-isolated-site-outlier, offsetof-rename-preserves-protected-web, pointer-walk-array-index-regression, recomputed-index-array-form-safe, scalar-read-write-lfsu-mixed-form, walked-base-symbol-identity): none directly applicable here -- this function's residual is param-local scalar/pointer register coloring, not a raw-offset-to-struct-field conversion, so the alias/offsetof-form hazard classes those laws describe don't govern it (no struct-field conversion was attempted on this function this session). Also screened matching-tag laws: point-of-use-single-use-pointer-delay APPLIED directly (see axis 1 above); declaration-initializer-split-controls-entry-schedule and independent-statement-reorder-matches-target-schedule were screened and found not applicable -- both govern entry/statement SCHEDULING shape, and this function's instruction order was already correct before any edit (opcode multiset IDENTICAL from baseline), so no schedule-order lever was in play, only register color.",
    "laws_applied": "[\"claim.law.point-of-use-single-use-pointer-delay.20260830.v1\"]",
    "measurements": "BASELINE (matches the superseded park's P5 note exactly): real 80, insns 204/204, opcode multiset IDENTICAL (pure register/schedule, confirmed fresh via --ops before editing). BEST/kept: real 76, insns 204/204 unchanged, --ops residual reduced to the same single 4-byte addi-position cluster as baseline (@0x188-0x190 target / @0x188-0x18c ours), opcode multiset still IDENTICAL.",
    "verification": "python tools/gdl/probe.py after every individual edit (BASELINE/IMPROVED/REGRESSED/NEUTRAL read from tool output, not eyeballed); final kept state re-confirmed with fndiff.py --ops and --clean; full ninja -j2 rebuild green, build/GUNE5D/main.dol: OK, project-wide 97.84% fuzzy / 58.15% matched / 26.09% linked unchanged (function remains part of a NonMatching TU); git diff --check clean; portable C only, no assembly, no config changes."
  },
  "function": "function:fn_800DACD8",
  "id": "attempt.fn-800dacd8-trio-drive.20260831.v1",
  "kind": "attempt",
  "outcome": "improved",
  "recorded_at": "2026-08-30T23:42:18Z",
  "residual_class": "REGISTER_ONLY",
  "schema_version": 1,
  "supersedes": "attempt.parked.fn_800dacd8.20260829.v1",
  "valid_from": "2026-08-30"
}
