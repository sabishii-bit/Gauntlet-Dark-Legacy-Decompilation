{
  "attempted_axis": "Execute attempt.BP_worldopen-load-inside-the-helper-does-not-move-the-return-web-pointer-form-refolds.20260903.v2's mandatory step-1 hypothesis -- the MIRROR exit order, `if (best <= value) { return value; } return best;` -- and then the one form it did not name, the v3 law's PART 4 assignment-to-the-parameter at that same mirrored polarity.",
  "attributes": {
    "law_screen": "claim.law.NM_branch-pair-fusion-is-blocked-by-a-return-not-by-a-goto.20260903.v3 APPLIED -- PART 3's recognition screen fires correctly here (the target's `@0x128 b ->130` lands on the loop tail while `@0x124 ble ->12c` lands on the value-assigning `mr r27,r0`, and both are the inline's exits), and PART 4 is what P5 tests directly and refutes for this function. claim.law.dead-nominal-local-area-tracks-declaration-count.20260831.v1 APPLIED and still explains half the loss: both probes grew the frame 48 -> 56 exactly as the two prior records measured, even though this helper declares NO locals at all -- so the growth is the inline's return temp, not a declaration. claim.law.live-zero-copy-vs-remat-is-allocator-not-source.20260831.v1 SCREENED and it is why the +0xc8..+0xe0 cluster's `addi`-vs-`li` half was not probed: that half is recorded allocator-not-source, and the hypothesis above targets only the `mr`, which is a count difference and outside that law. No webfrank pin covers WorldOpen (game/world/tower pins towerRecordLevelBeaten, TowerCheckMessages and SumnerDoSpeech, all screened).",
    "laws_applied": "[\"claim.law.NM_branch-pair-fusion-is-blocked-by-a-return-not-by-a-goto.20260903.v3\", \"claim.law.dead-nominal-local-area-tracks-declaration-count.20260831.v1\"]",
    "probed_form": "P4: `static inline s32 towerBestCompletion(s32 best, s32 value) { if (best <= value) { return value; } return best; }` defined immediately before WorldOpen, with the loop body's `if (best <= value) { best = value; }` replaced by `best = towerBestCompletion(best, value);`. P5: the same helper with the taken arm rewritten `if (best <= value) { best = value; return best; }`, everything else byte-identical to P4.",
    "residual": "Unchanged from baseline. Two clusters: the +0x124 pair (now denied across five spellings) and the +0xc8..+0xe0 prologue block, which this record reclassifies from 'independent' to the pair's compensating half and hands to the next lane as its mandatory step 1.",
    "scope": "src/game/world/tower.c probed and fully discarded with `probe.py game/world/tower WorldOpen --discard --whole-file`; nothing committed. The lane's `git status --short` is clean for this TU.",
    "verification": "`defake_gate.py baseline game/world/tower --at-head` anchored to commit 8e7b495d4, source sha1 a794a64c7 (36 functions, 33 at real 0; DATA 280/280 matched bytes). Probe scores as quoted in premise_measurement. NOTE ON PROVENANCE: this lane hit the FIRST-BASELINE TRAP -- the helper was added before WorldOpen's first probe of the session, so probe.py banked the EDITED state as its `BASELINE real 68`. The real 40 / T99/O99 / 8t / 92.3737% baseline quoted above is the v2 record's, re-confirmed as still current by `fndiff --count` at 8e7b495d4 printing `insns 99/99 real 40` before any edit. Tree restored and the object rebuilt by `--discard --whole-file`."
  },
  "denial": {
    "expiry_check": "python tools/gdl/fndiff.py game/world/tower WorldOpen --ops -- if the `replace T[73:75]@124-12c=['ble','b'] O[74:75]@128-12c=['bgt']` cluster changes shape after the +0xc8..+0xe0 cluster is worked, re-probe: this family is alignment-sensitive and the two clusters share the function's callee-saved pressure.",
    "falsifier": "Any lift spelling whose inline-return web lands in the SAME register as the caller's `best` -- observable as `mr r25,r0` (or the target's `mr r27,r0`) with NOTHING on the other path, and insns back at T99/O99.",
    "premise_measurement": "Baseline quoted from the v2 record and unchanged at commit 8e7b495d4: real 40, insns T99/O99, multiset 8t, FRESH FUZZY 92.3737%. P4, the mandated mirror `if (best <= value) { return value; } return best;`: real 68 at insns T99/O101, multiset 8t, frame 56 against the target's 48. It CLOSED the unconditional half -- the residual signature lost `+1 b`, going from `target-only: +2 addi +1 b +1 ble` to `target-only: +2 addi +1 ble` -- which is the opposite half from BF's P1 (that one closed the `ble` and not the `b`). The aligned view `fnasm game/world/tower WorldOpen 0x118:0x140 --diff` shows why it is not enough: ours emits `@0x128 bgt ->130` / `@0x12c b ->134` / `@0x130 mr r0,r25` / `@0x134 mr r25,r0` against the target's `@0x124 ble ->12c` / `@0x128 b ->130` / `@0x12c mr r27,r0`. The branch POLARITY is now the target's; what differs is that MWCC coalesced the inline's return web with `value` (volatile r0) so the `return best` exit must copy INTO it, where the target coalesced it with `best` (callee-saved r27) so the `return best` exit is empty. P5, the v3 law's PART 4 applied at that polarity -- `if (best <= value) { best = value; return best; } return best;` -- printed a --ops cluster list IDENTICAL to P4's, opcode for opcode and offset for offset, at real 78: two identical A/Bs, which AGENTS.md's iteration discipline treats as killing the axis.",
    "scope": "The max-accumulator lift as a cure for WorldOpen's +0x124 branch pair, now across FIVE spellings measured by three lanes: BF's bare-`static` two-stage, v2's `static inline` with the load moved in, v2's pointer-updating `void` form, and this record's two. It does NOT deny the +0xc8..+0xe0 constant-materialisation cluster, which remains untouched by every probe on this function."
  },
  "function": "function:WorldOpen",
  "held_fixed": "Both probes held EVERYTHING outside the helper constant -- the case-13/5/6/8 arms, the switch, the `towerLevelStatus` early exit, the `met`/`result` tail, and the untouched +0xc8..+0xe0 constant-materialisation cluster -- and varied only the helper's two return statements. The helper's signature (two s32 by value, no locals) was identical in both.",
  "hypothesis": {
    "cheapest_refuting_observation": "One probe hoisting `levelRecord = (u8*)(requirement * 2);` out of the loop to function scope. If `@0xd0 mr r24,r0` survives it, the copy is not driven by the loop-invariant hoist and the compensating reading is wrong.",
    "screened_against_target": "yes -- `fnasm game/world/tower WorldOpen 0xb8:0x140 --diff` at the committed baseline shows the target's `@0xc4 lwz r24,0(r3)` / `@0xd8 slwi r26,r24,1` pair reading the SAME register, against our `@0xc4 lwz r0,0(r3)` / `@0xd0 mr r24,r0` / `@0xd4 slwi r28,r0,1`, where the hoisted shift reads the pre-copy temp. The hoist probe itself is not run.",
    "statement": "The remaining, never-probed axis is the +0xc8..+0xe0 cluster, and it is the COMPENSATING half of the branch pair rather than an independent residual: at baseline the target is +1 instruction there (`@0xc4 lwz r24,0(r3)` loading `requirement` straight into its callee-saved web) while ours is +1 (`@0xc4 lwz r0,0(r3)` then `@0xd0 mr r24,r0`), and that extra `mr` is exactly what keeps the baseline at count parity T99/O99 while the branch pair is short by one. Every lift probe therefore has to buy the pair AND pay the `mr`, which is why all five landed at O101. Close the `mr` FIRST -- the target's zero web `@0xc8 li r27,0` feeding `@0xd0 addi r28,r27,0` and `@0xd4 addi r30,r27,0` against our three separate `@0xd8 li r25,0` / `@0xdc li r26,0` / `@0xe0 li r30,0` says the whole prologue block is scheduled differently -- and only then re-probe the lift at the new alignment."
  },
  "id": "attempt.BP_worldopen-the-return-web-is-not-reachable-by-exit-order-or-by-parameter-assignment.20260903.v3",
  "kind": "attempt",
  "outcome": "capped",
  "recorded_at": "2026-09-03T18:41:34Z",
  "refutes": "attempt.BP_worldopen-load-inside-the-helper-does-not-move-the-return-web-pointer-form-refolds.20260903.v2",
  "residual": {
    "capability_needed": null,
    "family": "branch-pair",
    "measured_at": "2026-09-03",
    "signature": "target-only: +2 addi +1 b +1 ble  ours-only: -1 bgt -2 li -1 mr"
  },
  "residual_class": "MIXED",
  "schema_version": 1,
  "supersedes": "attempt.BP_worldopen-load-inside-the-helper-does-not-move-the-return-web-pointer-form-refolds.20260903.v2",
  "valid_from": "2026-09-03"
}
