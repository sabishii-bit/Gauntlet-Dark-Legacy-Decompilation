{
  "attempted_axis": "Continue the fn_800C03E0 census from the aligned view: map every one of its nine --ops clusters back to a named source cause in one of the five per-mode blocks, then close the ones that are source-reachable while holding instruction-count parity, which the TU's extabindex section depends on.",
  "attributes": {
    "law_screen": "claim.law.EN-equal-count-opcode-respell-must-be-arbitrated-on-fuzzy-not-real.20260901.v1 APPLIED and DECISIVE: the keep is real 482 -> 484 (worse) at equal instruction counts while fresh objdiff fuzzy is 94.5843% -> 95.4388% (+0.8545), and the law says fuzzy arbitrates that disagreement. claim.law.WS_frame-widening-silently-breaks-the-tus-extab-match.20260902.v1 APPLIED and it VETOED an intermediate state: the textX fix alone reached fuzzy 95.2771% (+0.6928) but at T433/O434, and `defake_gate check` reported `extabindex 84->0 of 84 (-84 B)` because a one-instruction growth in this function shifts every later function's extabindex address -- so instruction-count parity was treated as a hard precondition for the keep, not a preference. claim.law.insn-count-parity-outranks-local-opcode-fidelity.20260831.v1 APPLIED for the same reason. claim.law.identical-multiset-is-blind-to-displacements.20260831.v1 SCREENED, not applicable: the multiset here DIFFERS. claim.law.fuzzy-can-underweight-a-real-improvement SCREENED in the reverse direction and the --ops diff was read before the keep, as it requires. Pin screen: game/pb/dbgtext carries no webfrank pin on this function.",
    "laws_applied": "[\"claim.law.EN-equal-count-opcode-respell-must-be-arbitrated-on-fuzzy-not-real.20260901.v1\", \"claim.law.WS_frame-widening-silently-breaks-the-tus-extab-match.20260902.v1\",\"claim.law.insn-count-parity-outranks-local-opcode-fidelity.20260831.v1\"]",
    "probed_form": "KEPT EDIT 1 (mode==5 block): added `s32 textX;` to the loop's block declarations, `textX = 0;` before the call, and changed `dbgTextPrintfPx(row->color, 0, line, &lbl_80348EF4, dv >> shift)` to `dbgTextPrintfPx(row->color, textX * 8, line, &lbl_80348EF4, dv >> shift)`. KEPT EDIT 2 (mode==2 loop): replaced `row = &tblB[i]; id = row->id;` with `id = tblB[i].id;`, `colorp = &row->color;` with `colorp = &tblB[i].color;`, and `row->name` with `tblB[i].name`. REVERTED PROBES: (a) hoisting textX out of the loop as `for (i = 0, textX = 0; i < 24; i++)` -> T433/O435, real 510; (b) converting the mode==3 loop's DBGROW3 macro to `row = (DbgRow*)((u8*)lbl_80344F78 + i * 28); id = row->id;` -> T433/O431, real 596, with three new clusters at @98/@f0/@15c; (c) a block-scope `DbgRow* row2` in mode==2 -> T433/O434, real 507; (d) computing `colorp = &row->color;` immediately after the row assignment -> T433/O433, real 486.",
    "residual": "Six clusters, all named. (1)+(2) the two trailer loops at +0x364 and +0x640, `li,li` in the target against `li` plus an `addi` copy in ours, with the `j = qline - 20` computation emitted first in the target and third in ours. (3) +0x3cc in the mode==5 block, where the target CSEs textX's zero into `addi r18,r19,0` and ours emits a second independent `li` -- the OPPOSITE direction to (1)/(2), which is why the CSE is judged downstream of scheduling. (4) +0x608 in the mode==4 block, where the target recomputes `divwu r4,r24,r19` for the mbBlitProject argument while ours reuses the `divwu.` test result it parked in pct's callee-saved home. Block map established this session for the next lane: mode==3 is 0x60-0x250, mode==2 is 0x254-0x3b8, mode==5 is 0x3c0-0x41c, mode==1 is 0x41c-0x57c, mode==4 is 0x580-0x67c.",
    "scope": "src/game/pb/dbgtext.c::fn_800C03E0 only, commit c2a55aa7a, 6 insertions / 5 deletions. Two source causes named and closed: a missing textX local in the mode==5 loop, and a spurious row-pointer copy in the mode==2 loop.",
    "verification": "Clean-state BASELINE banked before the first edit: real 482, insns T433/O433, multiset 6t, FUZZY (fresh report) 94.5843%. BEST: real 484, insns T433/O433, multiset 4t, FUZZY (fresh report) 95.4388%. Nine --ops clusters became six; regnorm genuine structural rows 1 -> 2. `defake_gate.py baseline game/pb/dbgtext --at-head` anchored to commit c8a28c3bb, source sha1 4e20e48c7, DATA baseline extab 56/56 and extabindex 84/84; `defake_gate.py check --rebuild` after the keep reports NO DATA-CHANGED row (both sections held) and one real-only REGRESSION row, which the fuzzy arbiter overrides per the law screen. Full `ninja -j2` prints `build/GUNE5D/main.dol: OK`; whole-image Data held at 178856 bytes and Game Code fuzzy moved 98.18% -> 98.19%. `git diff --check HEAD` and `git diff --cached --check` both exit 0."
  },
  "denial": {
    "expiry_check": "python tools/gdl/fndiff.py game/pb/dbgtext fn_800C03E0 --ops -- re-probe all three forms if the cluster list at +0x25c or +0x268 changes shape, or if real is no longer 484.",
    "falsifier": "Any form that keeps a named row pointer AND a named colorp in the mode==2 loop while emitting `add rD,base,index` straight into a callee-saved register with no following copy, i.e. reproducing the target's +0x254/+0x258 pair at T433/O433.",
    "premise_measurement": "Baseline at c8a28c3bb, `probe.py game/pb/dbgtext fn_800C03E0 --ops`: real 482, insns T433/O433, multiset 6t, FUZZY (fresh report) 94.5843%, nine clusters, RELOC-SYMBOL SCREEN clean. Keeping `colorp` alongside a FUNCTION-SCOPE `row` gives ours `@0x254 add r3,r29,r21 / @0x258 lwz r16,16(r3) / @0x25c addi r22,r3,0` against the target's `@0x254 add r18,r28,r21 / @0x258 lwz r19,16(r18)` -- an extra copy, T433/O434, real 507. A BLOCK-SCOPE `DbgRow* row2` in the same loop measured IDENTICALLY: T433/O434, real 507, multiset 5t, the same cluster list. Computing `colorp` immediately after the row assignment instead of after the id test folded `colorp` away entirely: T433/O433, real 486, and the same @268-27c reorder cluster. Only dropping the row pointer altogether kept both parity and the target's block order.",
    "scope": "Three forms of the tblB row-pointer in the mode==2 loop of fn_800C03E0 as ways to remove our spurious `addi r22,r3,0` at +0x25c WHILE KEEPING the `colorp = &row->color` pointer the target carries at +0x270. It does NOT deny the two trailer-loop `li,li`-vs-`li,addi` clusters, the mode==5 chained-zero row at +0x3cc, or the +0x608 divwu recompute, and it does not deny some fourth addressing form."
  },
  "function": "function:fn_800C03E0",
  "held_fixed": "Each probe varied one thing. The textX probe varied only the x argument of the mode==5 dbgTextPrintfPx call (literal 0 -> a block local assigned 0, then multiplied by 8), holding every declaration and every other statement fixed. The three mode==2 probes each varied only how the tblB row is addressed -- function-scope pointer local, block-scope pointer local, and no pointer at all -- holding the textX edit and all other statements fixed.",
  "hypothesis": {
    "cheapest_refuting_observation": "`python tools/gdl/fnasm.py game/pb/dbgtext fn_800C03E0 0x360:0x378 --diff` after any reordering edit: if our `addi rJ,r30,-20` is still emitted after the two zero inits, the statement-order lever does not reach this block and the idea is dead without touching the CSE at all.",
    "count_consequence": "Count-neutral. Both trailer blocks are four instructions in each stream today; the cluster pair reads as an insert plus a two-for-one replace that nets to zero, and the proposed reorder does not change that.",
    "screened_against_target": "yes -- the two trailer blocks were read in the aligned view at 0x358:0x380 and 0x634:0x660 and both show the identical target order (j, li 0, li 0, lis) against our order (li 0, copy, j, lis). NOT screened: no reordering edit has been built, so the effect on the copy is unmeasured.",
    "statement": "Two of the six surviving clusters are the SAME shape at the two trailer loops (`j = qline - 20; for (k = 0, i = 0; k < N; k++, i += S)`): the target emits `addi rJ,r31,-20` then two independent `li 0`, and ours emits `li 0` then an `addi rI,rK,0` copy then the `addi rJ`. The emission ORDER differs as well as the copy, so the next lane should attack the ORDER first -- get our `j` computation emitted before the two zero inits -- rather than trying to break the zero CSE, because the same copy-vs-li choice appears in the OPPOSITE direction at +0x3cc in the mode==5 block of this same function, which means the CSE decision is downstream of scheduling here and not a spelling the source picks."
  },
  "id": "attempt.NC_fn800c03e0-a-missing-textx-local-and-a-spurious-row-pointer-copy.20260903.v1",
  "kind": "attempt",
  "outcome": "improved",
  "recorded_at": "2026-09-03T21:36:18Z",
  "residual": {
    "capability_needed": null,
    "family": "copy-form",
    "measured_at": "2026-09-03",
    "signature": "opcode multiset: DIFFERS  target-only: +1 divwu +1 li  ours-only: -2 addi"
  },
  "residual_class": "STRUCTURAL",
  "schema_version": 1,
  "valid_from": "2026-09-03"
}
