{
  "attempted_axis": "Closed the two long-standing `replace T=[lfs,fcmpo] O=[b]` clusters at @5c0/@5f8 by splitting two `else if` chains into separate `if` statements (the arms' conditions are mutually exclusive, so the split is semantics-preserving), then re-probed the previously-rejected info->type cache that the prior record explicitly flagged for re-measurement once those clusters closed",
  "attributes": {
    "axis_log": "B1 (the pass's main win) both `} else if (fN > 0.0f && fM < fN) { goto los_done; }` arms in the los_check block rewritten as `}` + a fresh `if (fN > 0.0f && fM < fN) { goto los_done; }`: target RE-LOADS sItemZero and RE-COMPARES inside each inner chain (lfs+fcmpo) where our `else if` reused the outer guard's cr0 and needed an unconditional `b` to skip the else. Splitting the chain forces the re-test. BOTH @5c0 and @5f8 clusters resolved, insns 659->661 = EXACT PARITY with target, real 546->420, multiset 8t->2t. The two conditions are mutually exclusive (`< 0.0f` vs `> 0.0f`), so no semantic change. B2 re-probe of the prior lane's flagged axis (b): cache `type = info->type;` immediately after `info = item->info;` and use it for the `== -1` guard AND the main switch, reusing the ALREADY-DECLARED but previously write-only `s32 type` local at line 2493 (its existence is itself evidence the original cached it). The prior lane measured this at fuzzy 98.08% and rejected it on real; in the post-B1 landscape it measures 98.6248% - the cascade reversed sign exactly as that record predicted. ours-only lwz @b4 eliminated, multiset 2t->1t. KEPT. B3 follow-up probe, deleting the apparently-redundant re-read `type = item->info->type;` at line 2668 (same value, already live) to widen the local's web and try to force the missing copy: REGRESSED hard - multiset 2t->3t and target revealed as having its OWN `+2 lwz` pair at @464, i.e. the target genuinely RE-READS item->info->type there. Reverted; the line is confirmed-correct reconstruction, not redundancy. Useful negative: not every duplicated read in this TU is a reconstruction artifact.",
    "baseline_and_best": "baseline: insns 661/659, real 546, multiset 8t, objdiff fuzzy 97.63389%. after B1: insns 661/661 (parity), real 420, multiset 2t, fuzzy 98.1785%. best (B1+B2): insns 661/660, real 739, multiset 1t, objdiff fuzzy 98.6248%. Both intermediate states were measured with a full ninja so the arbitration used real per-function objdiff numbers, not inference.",
    "law_screen": "claim.law.if-else-arm-swap-controls-fallthrough-placement.20260831.v1 SCREENED as the nearest neighbour and correctly REJECTED: the outer arm ORDER already matches target (both builds fall through into the `& 47` arm at 214 and branch to the `& 0xF` arm), so no arm swap was warranted - the divergence was chain SHARING, not arm placement, which is a distinct mechanism now recorded separately. claim.law.insn-count-parity-outranks-local-opcode-fidelity.20260831.v1 APPLIED TWICE and in BOTH directions, which is the methodological point of this pass: the else-if split RESTORED parity (659->661 = target 661) so real is the arbiter there and real fell 546->420 in agreement with fuzzy; the type-cache BREAKS parity (661->660) so fuzzy is the arbiter there, and fuzzy rose 98.1785% -> 98.6248% while real rose 420->739 - obeying `real` at that second step would have discarded a real structural gain, exactly the trap the law describes. claim.law.reload-vs-cache-liveness.20260829.v1 APPLIED for the type cache (target holds info->type in r5 across the four early-return guards; ours reloaded it with `lwz r0,0(r3)` after them). claim.law.neutral-real-and-identical-multiset-do-not-prove-byte-identity.20260831.v1 APPLIED: every arbitration in this pass was settled with a full ninja + per-function objdiff fuzzy read, never on real/--ops alone. claim.law.dead-local-identity-reuse-flips-callee-saved-coloring.20260831.v1 SCREENED for the surviving mr @90 - no dead alternate carrier exists for `coltype`. claim.law.absent-target-guard-means-tautological-reconstruction.20260831.v1 screened, not applicable (the prior lane already measured the outer `f2>0.0f||f1>0.0f` guard as NON-tautological; it was not touched). The prior lane's recorded do-not-retry on the `&&` OPERAND-ORDER swap for these same clusters was HONORED - operand order was never altered; the change is the `else` deletion, a different axis. Pinned-function screen: config/GUNE5D/webfrank.json has NO unit entry for game/world/gauntworld.",
    "laws_applied": "[\"claim.law.insn-count-parity-outranks-local-opcode-fidelity.20260831.v1\", \"claim.law.reload-vs-cache-liveness.20260829.v1\", \"claim.law.neutral-real-and-identical-multiset-do-not-prove-byte-identity.20260831.v1\"]",
    "residual": "MIXED, 3 clusters. (1) The dominant one is REGISTER_ONLY and is the same single named mechanism this TU shows in fn_8005D730 and fn_80062A00: target `lha r0,8(r4); cmpwi r0,0; mr r29,r0` materializes `coltype` in a scratch and COPIES it to its callee-saved home, where our build coalesces the load straight into the home register and emits no copy - leaving us 1 instruction short (661/660). See claim.law.callee-saved-home-copy-coalescing-is-source-unreachable.20260831.v1. (2) An lfs placement swap at @2cc-@2d8 (target loads one slot earlier), not probed this pass. (3) A whole-function register-numbering offset (target r4/r25 vs ours r3/r28 for data/item) which is downstream of (1). NOT retried this pass and still open per the prior record: the pervasive +1 register cascade rooted in the same coltype copy.",
    "scope": "src/game/world/gauntworld.c::fn_8005F0F4 only, 7 insertions / 4 deletions. Portable C, no header edits, no configure.py edits, no assembly. Commit 85947f0d3.",
    "verification": "probe.py per variant against a clean BASELINE banked before the first edit; both arbitrated keeps banked with --rebase-best. Full `ninja -j2` green at every arbitration point, build/GUNE5D/main.dol: OK (sha1 gate). tools/gdl/fuzzy.py over the whole TU: no sibling regressed. Project All fuzzy 98.00% -> 98.01%. git diff --check clean."
  },
  "function": "function:fn_8005F0F4",
  "id": "attempt.fn-8005f0f4-chain-split-and-type-cache.20260831.v1",
  "kind": "attempt",
  "outcome": "improved",
  "recorded_at": "2026-08-31T15:47:11Z",
  "residual_class": "MIXED",
  "schema_version": 1,
  "supersedes": "attempt.fn-8005f0f4.20260829.v1",
  "valid_from": "2026-08-31"
}
