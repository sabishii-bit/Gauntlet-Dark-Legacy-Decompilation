{
  "attempted_axis": "Fresh ground (`gdlmem context draw_power_meter` returned ZERO attempts): the whole residual was ONE surplus `lis` in the first 0x48 bytes (multiset 1t). Read the aligned prologue, found the target pools the player record off `potionicon_tab + 0xC40` while we materialise a second base from `gPlayers`, switched the function to the TU's own `PT(i)` macro -- then censused every other `P(i)` site to see whether a TU-wide sweep existed.",
  "attributes": {
    "census": "THE TU HEADER'S TU-WIDE CLAIM IS FALSE, measured. src/game/game/player.c lines 134-138 assert that `the TARGET pools all player-record accesses off potionicon_tab ... and every other fn touching gPlayerRecords` differs `by construction`, and conclude `NOT a per-fn matching failure -- do not grind`. A zero-build census (NC_scratch/nc_pscreen.py: for each function containing a `P(i)`/`P(j)`/`P(k)` use, run `fnasm <unit> <fn> --diff` and count `potionicon_tab` versus `gPlayers` mentions across BOTH columns) screened all 17 such functions -- OtherPlayerOnOtherMovingObject, PlayerAttacking, PlayerGiveGold, PlayerOnMovingObject, PlayerSelecting, change_player, clear_player, damage_player, get_display_mode, load_player, mini_inventory_find_next_selectable_item, mini_inventory_find_previous_selectable_item, new_player, player_save_controls, remove_player_geo, reset_players, write_gold -- and EVERY ONE scored `potionicon_tab=0 gPlayers=4`, i.e. the TARGET itself relocates `gPlayers` in all 17. So the pooling is PER-FUNCTION, `P(i)` is correct in all 17, draw_power_meter was the single outlier in the other direction, and there is NO sweep to run. The header's advice is not merely stale, it is inverted for the one function it would have protected. Separately screened and NOT the same class: write_health_and_items' three IMMEDIATE rows (`addi r7,r29,868` vs ours `addi r7,r29,28`, all differing by exactly 840) are the STRING-POOL flip gate, not a base-pooling defect -- the target relocates `lbl_80113AE0` where ours relocates `...rodata.0`, our string literals living in player.o's own .rodata, which is the `.rodata 0x104 (strings)` claim the same header lists as a flip gate. No source edit applies there.",
    "law_screen": "claim.law.split-bss-symbols-are-one-record-and-array-form-defeats-synthesized-index-base.20260831.v2 APPLIED and it is the mechanism: `potionicon_tab` and the player records are ONE .bss block in the target's object (the relocation at `@0x18 lis r3,@potionicon_tab(ADDR16_HA)` reaches the records as `+0xC40`), and our `gPlayers` extern spelling forced a second base. claim.law.dead-nominal-local-area-tracks-declaration-count.20260831.v1 APPLIED and it is why rejected form (1) failed -- the added `prec` local cost 8 bytes of frame (128 -> 136) and 26 IMMEDIATE rows. claim.law.two-use-preoffset-pointer-keeps-add-then-addi.20260831.v1 SCREENED and it PREDICTED rejected form (1): the `potionicon_tab + i * PREC_STRIDE` intermediate has no second read in this function, so MWCC reassociates and the two-statement split cannot reach the target's `add`-then-`addi` order. claim.law.real-can-underweight-a-large-alignment-gain-so-arbitrate-conflicts-on-fuzzy.20260831.v1 SCREENED and its converse applied here -- see the arbitration note in attributes.verification. Webfrank pins screened: no pin names draw_power_meter, and every game/game/player pin replayed under the full build after the keep.",
    "laws_applied": "[\"claim.law.split-bss-symbols-are-one-record-and-array-form-defeats-synthesized-index-base.20260831.v2\", \"claim.law.dead-nominal-local-area-tracks-declaration-count.20260831.v1\", \"claim.law.two-use-preoffset-pointer-keeps-add-then-addi.20260831.v1\"]",
    "probed_form": "KEEP (commit 71de6e8e7): in src/game/game/player.c::draw_power_meter, `Player* p = P(i);` -> `Player* p = PT(i);` and the two initialised locals exchanged so the function opens `Player* p = PT(i); s32* table = lbl_8011FC48;`. `P(i)` is `(&gPlayerRecords[i])`; `PT(i)` is `((Player*)((u8*)potionicon_tab + (i) * PREC_STRIDE + 0xC40))`, the spelling already used at ~25 sites in this TU including the identical statement `Player* p = PT(i);` at five of them. REJECTED FORM 1 (reverted): the two-statement split `u8* prec = (u8*)potionicon_tab + i * PREC_STRIDE; Player* p = (Player*)(prec + 0xC40);` -- real 112, frame 128 -> 136, 26 IMMEDIATE rows appeared, prologue clusters UNCHANGED. REJECTED FORM 2 (reverted): declaring `Player* p; s32* table;` and assigning both at the top of the body -- NEUTRAL-IDENTICAL to the keep (same real 58, same fuzzy 96.2630%, same six clusters), so declaration-with-initialiser versus body-assignment is a proved class boundary here and no further spelling of it will move.",
    "residual": "Six clusters, all inside +0x4..+0x44, opcode multiset IDENTICAL: one `lis` hoisted above `stwu`, the two `mulli` scheduled before rather than after the base's `lis`/`addi` pair, and the `+3136` reassociated onto the index instead of deferred onto the pointer. Nothing outside the first 0x48 bytes differs.",
    "verification": "`defake_gate.py baseline game/game/player --at-head` anchored to commit c3f3aea99, source sha1 bef8acec9 (93 functions, 72 at real 0; DATA 1386/1400 matched bytes). BASELINE `probe.py game/game/player draw_power_meter --ops`: real 151, insns T289/O290, multiset 1t, fresh fuzzy 97.3633%. FINAL: real 58, insns T289/O289 (COUNT-PARITY GAINED, probe.py's own class banner), opcode multiset IDENTICAL (289/289), IMMEDIATE rows 2 -> 0, frame and slot map exact. ARBITRATION, STATED PLAINLY: fresh objdiff fuzzy FELL 97.3633% -> 96.2630% (-1.1003) while every other arbiter improved, so probe.py refused to bank and the keep was made DELIBERATELY with `--rebase-best --accept-fuzzy-loss`. The conflict was resolved by inspecting the diff (all six residual clusters are a prologue reorder inside +0x4..+0x44; objdiff penalises the two instructions we hoist above `stwu`, while `real` additionally credits the 45 branch-target rows that count parity resolved) and then by the PROJECT-level arbiter: `defake_gate.py check game/game/player --rebuild` = `GATE OK (1 improved)` with matched Data GAINED (`extabindex 829->830 of 840, NET +1 B`) and no sibling regression among the 93 functions, and a full `ninja -j2` printing `build/GUNE5D/main.dol: OK` with PROGRESS UNCHANGED on every headline: All 98.53% fuzzy / 68.17% matched, Data 178712, STRICT 57.01% (2589 fns) + EQUIVALENT 11.16% (153 fns). `git diff --check HEAD` exits 0."
  },
  "function": "function:draw_power_meter",
  "held_fixed": "The two keeps were probed one at a time. The macro swap held every declaration and statement constant and changed ONE token (`P(i)` -> `PT(i)`); the declaration-order swap then held the macro constant and exchanged ONLY the order of the two initialised locals `p` and `table`. The two rejected forms each held the macro swap constant and varied one construct.",
  "hypothesis": {
    "cheapest_refuting_observation": "`wf_word_diff.py game/game/player draw_power_meter`. If the raw differing-word count is materially larger than the ~8 words the six clusters span, the residual is not the prologue permutation it looks like and no permutation window will close it.",
    "screened_against_target": "yes -- `fnasm game/game/player draw_power_meter 0x0:0x50 --diff` at the committed state shows all six clusters inside +0x4..+0x44 and the whole body from +0x48 onward aligned instruction-for-instruction. `wf_word_diff` itself is NOT run.",
    "statement": "The whole remaining residual is the pre-frame address-materialisation class recorded as claim.law.NC_pre-frame-addr16-lo-into-r0-owes-a-callee-saved-copy.20260903.v1: ours hoists one `lis` above `stwu` and reassociates the `+3136` onto the INDEX (`addi r26,r4,3136; add r26,r30,r26`) where the target adds the base first and defers the constant (`add r26,r31,r0; ...; addi r26,r26,3136`). Because the opcode multiset is now IDENTICAL and the counts agree, this function is a PERMUTATION-class candidate and the next step belongs to the WebFrank lane, not to a source lane -- but source-exhaustion provenance for that rule is exactly this record plus the two negatives below."
  },
  "id": "attempt.NC_draw-power-meter-pooled-base-buys-count-parity-and-the-tu-wide-pooling-claim-is-false.20260903.v1",
  "kind": "attempt",
  "outcome": "improved",
  "recorded_at": "2026-09-03T20:03:11Z",
  "refutes": "attempt.PL_write-health-and-items-cell4-array-member-base.20260901.v1",
  "residual": {
    "capability_needed": null,
    "family": "prologue-form",
    "measured_at": "2026-09-03",
    "signature": "opcode multiset IDENTICAL (289/289); 6 clusters, all within +0x4..+0x44"
  },
  "residual_class": "SCHEDULE",
  "schema_version": 1,
  "valid_from": "2026-09-03"
}
