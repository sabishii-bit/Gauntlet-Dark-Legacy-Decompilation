{
  "attempted_axis": "Close the single surplus `mr` (T51/O52, ours-only -1 mr) in game/pb/pb_error.c::fn_800C13CC - the ADDR16_LO home copy for the assert-string pointer - without losing the callee-saved numbering the named local provides.",
  "attributes": {
    "law_screen": "claim.law.addrlo-home-copy-class-is-general-temp-and-shadow-coupled.20260901.v1 SCREENED: destination is callee-saved r30, so this is class P, S=0 image-wide, disposition do-not-probe. Probed anyway because the sibling fn_800C1174 in the same TU carries the same class and closed it this run; the disposition holds HERE and is refuted THERE, which is the finding. claim.law.pointer-decl-order-flips-addrlo-inplace-vs-fresh-destination.20260901.v1 SCREENED, case (a) (callee-saved destination) applied and its 'do not probe' held for every declaration-order form. claim.law.point-of-use-single-use-pointer-delay.20260830.v1 SCREENED and its cure measured NEUTRAL (identical --ops), which is a REFUTATION of the cure for the in-loop single-use case: MWCC hoisted the assignment straight back out of the loop. claim.law.declaration-initializer-split-controls-entry-schedule.20260830.v1 SCREENED and APPLIED as the explanation of the decl-initialiser regression (real 13 -> 21). claim.law.stack-slots-descend-in-declaration-order.20260831.v1 and claim.law.MV_callee-saved-numbering-has-a-width-class-ahead-of-declaration-order.20260902.v1 SCREENED and used to derive the numbering rule measured above. claim.law.noop-cast-on-array-decay-not-neutral.20260831.v1 SCREENED, and `&lbl_801164C0[0]` measured neutral here.",
    "probed_form": "KEPT (real 13 -> 12, count parity GAINED T51/O52 -> T51/O51, multiset IDENTICAL): deleted `char* file;` and its `file = lbl_801164C0;` assignment, spelling the call `fn_800C1148(0, 0, lbl_801164C0);` - the same spelling the sibling fn_800C1174 already uses at its own call site. REFUTED AND REVERTED, and the most informative negative: retyping `extern char lbl_801164C0[];` to `extern char lbl_801164C0;` with `file = &lbl_801164C0;` - the transplant of fn_800C1174's winning mechanism - because a SCALAR extern becomes SDA-addressable: our object dropped the `lis`/`addi` pair entirely (target-only +1 addi +1 lis, T51/O50, ours SHORTER by one) and the same retype took the sibling fn_800C1174 from real 42 to real 180 at 148 insns. The struct-object retype transfers; a scalar retype does not, and the discriminant is whether the type is large enough to stay out of the small-data area.",
    "residual": "real 12 at T51/O51 with the opcode multiset IDENTICAL. Two register renamings (the address temp and `off` are transposed between r30 and r31, and the save set is r29-r31 in both) and ONE instruction transposition at the loop head: the target emits `addi r3,r1,8` at @0x48 then `extsh r4,r31` at @0x4c, we emit `extsh` first. The transposition is the SAME construct as the surviving one in fn_800C1174 (`sceGsSetDefLoadImage(array, ..., (s16)local, ...)`: the target evaluates the array-address argument before the sign-extended one, we do the reverse), so the two functions in this TU now share one open mechanism. With counts equal and the multiset identical the function is inside the register-field postprocessor class on the text side, but the register-field stage cannot reach it while the streams also disagree on nothing else - the renaming is a clean 2-register transposition.",
    "scope": "src/game/pb/pb_error.c only, 1 insertion / 3 deletions, committed 11fd19f87.",
    "verification": "`defake_gate.py baseline game/pb/pb_error --at-head` anchored to commit 84f85a96a, source sha1 3bce8c059. Fresh objdiff fuzzy 93.7843% unchanged (the two swapped registers cost what the removed instruction gained). `defake_gate.py check game/pb/pb_error --rebuild` = `IMPROVED fn_800C13CC real 13 -> 12`, `GATE OK (1 improved)`. `datadiff.py --sections game/pb/pb_error` extabindex 94.4444% -> 97.2222% at this commit, net +0 B all-or-nothing, extab held at 100%. Full `ninja -j2` printed `build/GUNE5D/main.dol: OK`; both suites green; `git diff --check HEAD` and `git diff --cached --check` both exit 0."
  },
  "denial": {
    "expiry_check": "python tools/gdl/probe.py game/pb/pb_error fn_800C13CC --ops  # if the cluster list is no longer exactly the single `insert T[18:18]@48-48=[] O[18:19]@48-4c=['extsh']` / `delete T[19:20]@4c-50=['extsh']` pair at T51/O51, the alignment moved and the local-restoring forms are worth one re-probe",
    "falsifier": "Any spelling that keeps a NAMED pointer local at declaration position 2 (between `off` and `i`) and still emits `addi r30,r3,<sym>@lo` directly, with no `mr`. The sibling fn_800C1174 achieves exactly that by retyping its extern from `u8[0x28]` to a struct object, so the analogous move here is not yet exhausted - see the refuted probe below for why the obvious spelling of it fails.",
    "premise_measurement": "Six source forms, each measured with the rest of the function held constant, all at T51/O52 real 13 with the `mr` surviving verbatim: (1) `char* file = lbl_801164C0;` as a declaration initialiser REGRESSED to real 21 by hoisting the whole lis/addi into the prologue; (2) bare declaration with the assignment moved into the loop body (the point-of-use law's literal cure) measured real 13, identical --ops clusters; (3) the assignment moved to first among the three preheader statements measured real 13, identical clusters; (4) `register char* file;` measured real 13, identical clusters; (5) `file = &lbl_801164C0[0];` measured real 13, identical clusters; (6) rebuilding the TU with mw_version GC/1.2.5n (matchtool's own best-preset pick for this unit) measured real 13, identical clusters. Deleting the local instead removes the `mr` and gains count parity (real 12, T51/O51, multiset IDENTICAL) but SWAPS two callee-saved registers, because the compiler-created address temp takes the highest saved register while named locals fill upward from the lowest in reverse declaration order - measured twice: named (off, i) gives i=r29, off=r30, temp=r31; named (i, off) gives off=r29, i=r30, temp=r31; and the target's assignment (i=r29, addr=r30, off=r31) is reachable only with a NAMED local at declaration position 2, which is the arrangement that pays the copy.",
    "scope": "The ADDR16_LO HOME-COPY axis on game/pb/pb_error.c::fn_800C13CC ONLY, in the form `namedPointerLocal = <address of an ARRAY-typed extern>`: the target's `lis r3,-32751` / `addi r30,r3,25792` at @0x38-0x3c against our `lis r3,HA` / `addi r0,r3,LO` / `mr r30,r0`. It does NOT deny the loop-head extsh transposition at @0x48, and it does NOT deny a form that changes the EXTERN'S TYPE while keeping the address in a non-SDA section."
  },
  "function": "function:fn_800C13CC",
  "held_fixed": "Every probe held the loop body, the declaration list length and the pad-free local set constant and varied exactly one construct: the presence of the named local, its initialiser vs assignment form, the assignment's statement position, the `register` storage class, the address expression spelling, the declaration ORDER of the two surviving scalars, and (separately, at the build level) the compiler variant.",
  "id": "attempt.GC_fn800c13cc-the-home-copy-and-the-callee-saved-numbering-are-a-forced-trade.20260904.v1",
  "kind": "attempt",
  "outcome": "improved",
  "recorded_at": "2026-09-04T15:04:36Z",
  "residual": {
    "capability_needed": null,
    "family": "regalloc-web",
    "measured_at": "2026-09-04",
    "signature": "opcode multiset IDENTICAL (51/51); 2 register renamings (`addi r30,r3,LO` / `li r31,0` in the target against `addi r31,r3,LO` / `li r30,0` in ours) plus one transposition `insert T[18:18]@48-48=[] O[18:19]@48-4c=['extsh'] / delete T[19:20]@4c-50=['extsh']`"
  },
  "residual_class": "REGISTER_ONLY",
  "schema_version": 1,
  "valid_from": "2026-09-04"
}
