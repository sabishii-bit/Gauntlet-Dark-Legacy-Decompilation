{
  "attempted_axis": "Remove the single extra `mr r28,r0` at +0x20 -- ours materialises the gWorldName base as `addi r0,r3,LO` then copies, where the target writes `addi r28,r3,LO` straight into the variable's register -- by varying the base local's declaration position, its existence, and its type.",
  "attributes": {
    "law_screen": "claim.law.variable-identity-reopens-address-pre-and-zero-copy.20260831.v1 SCREENED and its declaration-ORDER clause CONFIRMED INERT here (probe A, NEUTRAL-IDENTICAL) -- matching that law's own note that ordering between two carriers is separately inert; its RETYPE clause was probed (C) and did not reach the cluster either. claim.law.live-zero-copy-vs-remat-is-allocator-not-source.20260831.v1 SCREENED and not applicable (no li-vs-copy token; the copy is of an ADDRESS, not a zero). claim.law.NM_branch-pair-fusion-is-blocked-by-a-return-not-by-a-goto.20260903.v2 SCREENED and not engaged (no branch token in this cluster). claim.law.dead-nominal-local-area-tracks-declaration-count.20260831.v1 SCREENED: frames agree at 40 in both streams, so no dead-local delta is hiding here.",
    "laws_applied": "[\"claim.law.variable-identity-reopens-address-pre-and-zero-copy.20260831.v1\"]",
    "probed_form": "BASELINE (retained), src/game/world/world.c:\n    s32* modelp;\n    s32 freeBefore;\n    s32 memBase;\n    char* base = gWorldName;\n    s32 size;\nAXIS A, NEUTRAL-IDENTICAL, reverted -- `char* base = gWorldName;` moved to the FIRST declaration.\nAXIS B, REGRESSED real 15 -> 34, reverted -- local deleted, all five uses written as `gWorldName + 228 + offsetof(WorldInfo, <field>)` / `strcpy(gWorldName, name)`.\nAXIS C, NEUTRAL-WORSE, reverted -- `u8* base = (u8*)gWorldName;` with `strcpy((char*)base, name);`.",
    "residual": "One instruction. Also worth inheriting from the aligned view and NOT part of this cap: at +0x34 the target's second FileExists argument relocates against the named `@lbl_803487A0` while ours emits an anonymous `@@240` for the same \"worlds\" literal -- a pool-NAMING row, invisible to the multiset, that belongs to the @@NNNN-vs-named reloc sweep rather than to this copy.",
    "scope": "src/game/world/world.c::LoadWorldDone. NO source change retained; the file was restored with `git checkout --` and the TU rebuilt.",
    "verification": "probe.py BASELINE banked before the first edit (real 15, T84/O85, multiset 1t). Axis A NEUTRAL-IDENTICAL with the proof line quoted above; axis B REGRESSED; axis C NEUTRAL-WORSE with regnorm genuine rows 0. Working tree restored to HEAD for this file; `defake_gate.py check game/world/world --rebuild` GATE OK afterwards; full `ninja -j2` prints `build/GUNE5D/main.dol: OK`."
  },
  "denial": {
    "expiry_check": "python tools/gdl/probe.py game/world/world LoadWorldDone --ops -- if real is no longer 15, or the cluster at +0x20 is no longer a single `ours-only: -1 mr`, re-run. Re-probe in particular after any change to this TU's OTHER functions: probe (B) proved the hoist POSITION is contested, so an alignment change upstream can move it.",
    "falsifier": "Any source form that emits `addi r28,r3,LO` directly at +0x1c -- i.e. the base address materialised into the callee-saved register that survives the FileExists call, with no intervening copy -- while keeping the materialisation BEFORE the three sdata zero stores. Probe (B) shows the second half is the hard one: dropping the local kills the copy AND the early position together.",
    "premise_measurement": "Three probes at commit 70d475512, baseline real 15 (insns T84/O85, multiset 1t, single cluster `insert T[8:8]@20-20=[] O[8:9]@20-24=['mr']`). Aligned view: target `1c: addi r28,r3,0 @lbl_8028C9A8(ADDR16_LO)` then straight into the three stores; ours `1c: addi r0,r3,0` + `20: mr r28,r0`. (A) Moving `char* base = gWorldName;` to the FRONT of the declaration list: NEUTRAL-IDENTICAL, object bytes unchanged (NEUTRAL-IDENTICAL-PROOF unit=game/world/world fn=LoadWorldDone bytes=06385fee76b2 real=15 insns=T84/O85 multiset=1t head=70d475512) -- the edit never reached codegen, which is CATEGORICAL for the declaration-order class. (B) Deleting the local and writing `gWorldName` at all five use sites: COUNT-PARITY GAINED (T84/O84) but REGRESSED real 15 -> 34 -- the address hoist MOVED from +0x18 to +0x4c (just before its first use, inside the if) and two fresh `mr`s appeared at +0xcc, so the early materialisation the target has is exactly what the user variable buys. (C) Retyping to `u8* base = (u8*)gWorldName;` with `strcpy((char*)base, name)`: NEUTRAL-WORSE, real 15 with the cluster UNCHANGED (still `ours-only: -1 mr`) and regnorm genuine structural rows 0 -- object bytes moved elsewhere, the copy did not.",
    "scope": "ONE cluster on ONE function: LoadWorldDone's extra copy at +0x20, reached through the DECLARATION class of the `char* base` local (position in the declaration list, presence/absence of the local, its pointer type). It does not deny statement-shape, inlined-helper, call-site or frame-level axes, and it says nothing about the function's other diff rows."
  },
  "function": "function:LoadWorldDone",
  "held_fixed": "The three `lbl_80344D8x = 0;` stores, the `if (name != 0 && FileExists(...))` guard and the whole body after it were held constant across all three probes; only the `base` local varied (position, existence, type).",
  "hypothesis": {
    "cheapest_refuting_observation": "Dump the function's other `addi rN,rM,0` sites in OUR object: if any of them writes a callee-saved register directly from a `lis` result, the compiler CAN do it here and the cause is this variable's web, not a general rule -- one `fnasm --ours` read, zero builds.",
    "screened_against_target": "no -- the target's own use-set was read (five uses, all through one base register) but no view-struct form has been compiled, and no census of direct-addi-to-callee-saved sites has been run in either stream.",
    "statement": "The extra copy is the same INLINE-RESULT-REGISTER effect that cost CritterAnimate its lift (claim.law.NM_branch-pair-fusion-is-blocked-by-a-return-not-by-a-goto.20260903.v2, part 3): MWCC computes into a scratch register and copies into the variable's web. If so, the reachable lever is not the declaration but the SET of uses -- try routing the `+228+offsetof(WorldInfo, ...)` accesses through a typed `WorldInfo*` view local initialised in the same statement, so the base and the struct pointer share one web instead of two."
  },
  "id": "attempt.NM_loadworlddone-the-extra-mr-is-an-address-init-copy-no-declaration-class-reaches-it.20260903.v1",
  "kind": "attempt",
  "outcome": "capped",
  "recorded_at": "2026-09-03T16:21:03Z",
  "residual": {
    "capability_needed": null,
    "family": "copy-form",
    "measured_at": "2026-09-03",
    "signature": "target-only: (none)  ours-only: -1 mr"
  },
  "residual_class": "STRUCTURAL",
  "schema_version": 1,
  "valid_from": "2026-09-03"
}
