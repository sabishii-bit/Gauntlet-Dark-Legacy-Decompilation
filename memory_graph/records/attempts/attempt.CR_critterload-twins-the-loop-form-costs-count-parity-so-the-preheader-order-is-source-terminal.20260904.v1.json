{
  "attempted_axis": "Execute the MANDATORY step-1 hypothesis both run-53 records leave on this pair and which neither of their denials covers -- the loop FORM, `i = 0; do { ... i += 4; } while (i < 32);` in place of the `for` -- after running both denials' expiry checks.",
  "attributes": {
    "law_screen": "claim.law.webfrank-cannot-close-a-count-asymmetric-residual.20260831.v1 SCREENED and DECISIVE -- it is why the do/while form was reverted on the count alone rather than on `real`, and why the rule request below is admissible for both twins at 104/104 and 108/108. claim.law.mwcc-ctr-loop-strength-reduction-is-defeated-by-an-in-body-exit-test.20260831.v1 SCREENED and it EXPLAINS the failure: the counted (ctr) loop MWCC synthesises from `i < 32, i += 4` -- visible as `@0x94 mtctr r0` in both streams -- depends on the entry test the `for` provides, and the do/while removes it, so the form that was supposed to reorder the preheader instead dismantles it. That is a predictable consequence this pass should arguably have derived before spending the build, and the law is strengthened, not overturned. claim.law.insn-count-parity-outranks-local-opcode-fidelity.20260831.v1 SCREENED as the arbiter that settles the revert. claim.law.identical-multiset-is-blind-to-displacements.20260831.v1 SCREENED and APPLIED: the multiset is IDENTICAL 104/104 so the residual was read from the raw aligned view, not from `--ops`. claim.law.CR_a-repeatedly-read-struct-array-member-wants-a-named-pointer-local-and-that-choice-decides-load-order-and-address-cse.20260904.v1 SCREENED and MEASURED NOT APPLICABLE here, which is worth stating because it closed four sites elsewhere in this TU this same run: that law is about a POINTER-versus-BASE choice on a struct member, while this residual's address expression is already proven to REQUIRE the raw byte-offset form (the predecessor measured that naming the base costs two instructions and parity), so the two mechanisms look alike on the `--ops` line and are opposites in the source. PIN SCREEN, run not assumed: `python memory_graph/gdlmem.py brief game/enemy/critter` returns `webfrank_pins` of 8 (CritterGetTargetPlayers, CritterLineRootColSub, CritterResolveMultipleTargets, CritterLineNodeColSub, CritterLookForCriticalMove, CritterLineCollide, CritterDoTexmodNode, ProcessCritter) and NEITHER twin is among them; the probe was reverted so both functions end this pass byte-identical to HEAD.",
    "laws_applied": "[\"claim.law.webfrank-cannot-close-a-count-asymmetric-residual.20260831.v1\", \"claim.law.mwcc-ctr-loop-strength-reduction-is-defeated-by-an-in-body-exit-test.20260831.v1\"]",
    "probed_form": "ONE probe, reverted, on CritterLoadDone only (the twins are edited in lockstep only when a form survives the first of them). Replaced `for (i = 0; i < 32; i += 4) { s32 *entry = *(s32 **)((u8 *)lbl_8025776C + i); if (*entry == 32) { sprintf(buf, &fmtbase[432], desc, (u8 *)entry + 16); break; } }` with `i = 0; do { s32 *entry = *(s32 **)((u8 *)lbl_8025776C + i); if (*entry == 32) { sprintf(buf, &fmtbase[432], desc, (u8 *)entry + 16); break; } i += 4; } while (i < 32);` -- semantically identical because the bound 32 is positive so the `for` always executes its first iteration too.",
    "residual": "RULE REQUEST, with the full decode, filed because this lane cannot author it: `config/GUNE5D/webfrank.json` and `tools/gdl/webfrank.py` are owned this run by claude-fleet-worker-WV (`python tools/gdl/claimscope.py --audit` rows, checked before any edit). THE ASK: ONE instruction_permutation rule covering BOTH twins, each a single adjacent transposition of two mutually independent instructions. PRECONDITIONS, MEASURED AT 47ae4d37c: CritterLoadDone counts 104/104 (ours/target), opcode multiset IDENTICAL (104/104), real 2, `RELOC-SYMBOL SCREEN: clean`, not pinned; CritterLoadStartNext counts 108/108, opcode multiset IDENTICAL (108/108), real 2, `RELOC-SYMBOL SCREEN: clean`, not pinned. THE WINDOW, CritterLoadDone: ours `@0x98 addi r4,r3,0 @lbl_8025776C(ADDR16_LO)` then `@0x9c li r5,0`; target `@0x98 li r5,0` then `@0x9c addi r4,r3,0 @lbl_8025776C(ADDR16_LO)`. Above them `@0x90 lis r3,0 @lbl_8025776C(ADDR16_HA)` and `@0x94 mtctr r0` are identical in both streams; below them `@0xa0 add r3,r4,r5` is identical and consumes both results. THE WINDOW, CritterLoadStartNext: the same transposition at `@0xe0`/`@0xe4`, 0x48 further into a function four instructions longer, same table, same idiom. INDEPENDENCE: `li r5,0` writes r5 and reads nothing; `addi r4,r3,0` writes r4 and reads r3, which is defined two instructions earlier at `@0x90` and is not written by the `li`. They share no register, neither is a load or a store, and there is no control op in the window -- `mtctr` is above it and the loop's `bdnz` is below. SOURCE-EXHAUSTION PROVENANCE, as the Mandatory result policy requires, and it is unusually complete for this pair: SIX attempt records now cover these two functions -- attempt.parked.critterloaddone.20260829.v1, attempt.mp-label-correction.critterloaddone.20260831.v1, attempt.critterload-preheader-schedule-reopen.20260831.v1, attempt.NV_critterloaddone-preheader-init-order-tiebreak-park.20260901.v1 (three induction axes), attempt.PR_critterloaddone-pair-two-unpinned-two-word-swaps-the-promotion-screen-surfaced.20260903.v1 (for-init placement; loop-body base local) and attempt.NM_critterloaddone-the-raw-offset-base-is-required-and-the-expression-spelling-folds.20260903.v1 (expression spelling; named base) -- plus this record's loop form, which was the last class any of them named as untried. NOTE FOR THE RULE AUTHOR, from the PR record and confirmed here: `wf_word_diff` calls this residual `CLASS: RECOLOR, only register fields differ` because it aligns by INDEX across the displacement; the aligned `fnasm --raw --diff` view shows the truth, a two-instruction SWAP. Do not author a copy_register_fields rule from the word-diff line on this pair.",
    "scope": "src/game/enemy/critter.c::CritterLoadDone only. NO EDIT RETAINED -- reverted with `probe.py --discard`, and `git diff --numstat` is empty for this file at the end of the pass. CritterLoadStartNext was never edited. No header, config, webfrank.json or symbols.txt change.",
    "verification": "Clean-state BASELINES banked on BOTH twins before any edit (first-baseline trap honoured): `probe.py game/enemy/critter CritterLoadDone --ops` = real 2, insns T104/O104, multiset 0t, FUZZY (fresh report) 98.8461%, `RELOC-SYMBOL SCREEN: clean`; `probe.py game/enemy/critter CritterLoadStartNext --ops` = real 2, insns T108/O108, multiset 0t, FUZZY (fresh report) 98.8889%, `RELOC-SYMBOL SCREEN: clean`. BOTH live denials were expiry-checked before the probe and BOTH HELD -- `fndiff.py game/enemy/critter CritterLoadDone --ops` returned the named two-row displacement at T104/O104, and `fnasm.py game/enemy/critter CritterLoadDone 0x90:0xa4 --raw --diff` showed ours still emitting the `addi` before the `li` -- so none of their four denied classes was re-run. The do/while probe returned `COUNT-PARITY LOST insns T104/O104 -> T104/O103 ... This is a CLASS change, not a score change` followed by `REGRESSED vs best 2: real 2 -> 35 (insns T104/O103, multiset 5t)`, and was reverted; after `--discard` the function re-scored `RESTORED real 2 (insns T104/O104, multiset 0t)`. Full `ninja -j2` printed `build/GUNE5D/main.dol: OK`; `python -m unittest discover tools/gdl/tests -b` = `Ran 2038 tests ... OK (skipped=2)`; `git diff --check HEAD` and `git diff --cached --check` both exit 0."
  },
  "function": "function:CritterLoadDone",
  "held_fixed": "The probe held the whole function except the loop STATEMENT FORM: the `case 7:` scan's bound (32), stride (4), the `*entry == 32` guard, the `sprintf` call and its arguments, the raw byte-offset address expression `*(s32 **)((u8 *)lbl_8025776C + i)` that the predecessor proved is REQUIRED, the switch structure and every declaration at function scope were all unchanged. Only `for (i = 0; i < 32; i += 4)` became `i = 0; do { ... i += 4; } while (i < 32);`.",
  "hypothesis": {
    "cheapest_refuting_observation": "Zero builds: `python tools/gdl/fnasm.py game/enemy/critter CritterLoadDone 0x90:0xa4 --raw --diff`. If ours ever emits `li r5,0` before the `addi r4,r3,0 @lbl_8025776C(ADDR16_LO)` in that window, the residual is closed and no rule is needed. Measured this pass at head 47ae4d37c: ours still emits `@0x98 addi r4,r3,0 @lbl_8025776C(ADDR16_LO)` then `@0x9c li r5,0` against the target's `@0x98 li r5,0` then `@0x9c addi r4,r3,0 @lbl_8025776C(ADDR16_LO)`, with `@0x90 lis r3,0 @lbl_8025776C(ADDR16_HA)` and `@0x94 mtctr r0` identical above them and `@0xa0 add r3,r4,r5` identical below. A source form that reorders those two while holding 104/104 and 108/108 also refutes the terminal claim.",
    "count_consequence": "Counts are 104/104 (ours/target) on CritterLoadDone and 108/108 on CritterLoadStartNext, and that parity is the whole reason the permutation class is reachable. The do/while probe took CritterLoadDone to 104/103 and was reverted for exactly that reason: claim.law.webfrank-cannot-close-a-count-asymmetric-residual.20260831.v1 would have put the function outside every class the moment the count moved.",
    "screened_against_target": "yes -- both denials' expiry checks were run against the live bytes BEFORE the probe and both held: `fndiff --ops` returned the same two-row `addi` displacement at @0x98-0xa0 with T104/O104, and the raw aligned view at 0x90:0xa4 showed the same order. NOT screened: no new independence audit was run on the two swapped instructions this pass, because the sibling record already established it and nothing in the window moved -- `li r5,0` writes r5 from nothing and `addi r4,r3,0` writes r4 from the r3 set at @0x90, so they share no register.",
    "statement": "BOTH TWINS ARE NOW SOURCE-TERMINAL AND SHOULD GO TO THE POSTPROCESSOR LANE AS ONE TWO-WORD instruction_permutation REQUEST -- which is the disposition attempt.NM_critterloaddone-the-raw-offset-base-is-required-and-the-expression-spelling-folds.20260903.v1 predicted in its own cheapest_refuting_observation, reached by a different route than it expected. That record said 'if the do/while form also returns NEUTRAL-IDENTICAL, the preheader order is not reachable from any source form in this function and both twins should go to the postprocessor lane'. The do/while did NOT return NEUTRAL-IDENTICAL; it returned something strictly stronger against the source path, `COUNT-PARITY LOST insns T104/O104 -> T104/O103` with `real 2 -> 35` and multiset 5t, because MWCC drops the loop's entry guard when the form guarantees one iteration. So the loop-form class does not merely fail to reorder the preheader -- it cannot preserve the count at all, which is the same wall the predecessor's named-base probe hit (`T104/O102`) and the same wall its sibling record's loop-body-base probe hit (2 -> 12 words). Every named source class on this pair is now measured: expression spelling (folds), named base (parity lost), for-init split (no change), loop-body base local (6x regression), the prior park's three induction axes, and now loop form (parity lost). The request is ONE rule covering both twins."
  },
  "id": "attempt.CR_critterload-twins-the-loop-form-costs-count-parity-so-the-preheader-order-is-source-terminal.20260904.v1",
  "kind": "attempt",
  "outcome": "negative",
  "recorded_at": "2026-09-04T06:02:53Z",
  "residual": {
    "capability_needed": "instruction_permutation over a single adjacent transposition, on BOTH twins as one request: counts are 104/104 (ours/target) on CritterLoadDone and 108/108 on CritterLoadStartNext, both with IDENTICAL opcode multisets, so both are inside the permutation class and the two are the same idiom in the same table",
    "family": "schedule-window",
    "measured_at": "2026-09-04",
    "signature": "opcode multiset: IDENTICAL (104/104); insert T[38:38]@98-98=[] O[38:39]@98-9c=['addi']; delete T[39:40]@9c-a0=['addi'] O[40:40]@a0-a0=[]"
  },
  "residual_class": "SCHEDULE",
  "schema_version": 1,
  "valid_from": "2026-09-04"
}
