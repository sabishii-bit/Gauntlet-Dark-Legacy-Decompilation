{
  "attempted_axis": "Re-verify the inherited one-instruction residual live at HEAD (the run-49 stale-carrier lesson), then spend one construct class the prior record had not tried -- commutative reassociation of the inlinee's product -- and convert the prior record's UNGATED-PROSE-DENIAL into a typed denial a later lane can screen and expire.",
  "attributes": {
    "law_screen": "claim.law.webfrank-cannot-close-instruction-count-deltas.20260831.v1 SCREENED and DECISIVE: the residual IS one instruction of count asymmetry, so no rule may be authored here and the source path is the only path. claim.law.live-zero-copy-vs-remat-is-allocator-not-source.20260831.v1 SCREENED, adjacent family (a copy retail pays and we do not) but no constant is involved. claim.law.NC_a-shared-function-scope-counter-web-is-what-makes-mwcc-cse-a-loop-preheader-zero.20260903.v1 (authored this session) SCREENED and NOT APPLICABLE, which is the interesting negative: that law's lever is declaration scope, and it cannot reach here because the register the target copies FROM is r0, MWCC's anonymous expression scratch, which no named local is ever coloured. claim.law.EN-equal-count-opcode-respell-must-be-arbitrated-on-fuzzy-not-real.20260901.v1 SCREENED, not reached (the probe folded away, so there was nothing to arbitrate). claim.law.dead-local-identity-reuse-flips-callee-saved-coloring.20260831.v1 SCREENED and REJECTED WITHOUT A PROBE for the same r0 reason the prior record gives. Pin screen: game/ui/btext carries webfrank pins on DrawGlowText and DrawGlowTextMLines; both PRECEDE StringTextHeightSub in source order, the probe was reverted whole-file, and the TU is byte-identical to HEAD.",
    "laws_applied": "[\"claim.law.webfrank-cannot-close-instruction-count-deltas.20260831.v1\"]",
    "probed_form": "The static inline helper's return expression, and nothing else: `static inline s32 TextLineHeight(char* s, s32 fh, s32 spacing) { s32 n = 0; for (;;) { s = find_newline(s); if (s == 0 || n >= 0xF) { break; } s++; n++; } return (fh + spacing) * (n + 1); }` -- i.e. `return (n + 1) * (fh + spacing);` transposed. Result NEUTRAL-IDENTICAL, object bytes unchanged, so MWCC canonicalises the commutative product before the destination-selection decision and no further spelling of the multiply can reach that decision either.",
    "residual": "Unchanged at real 55, T111/O110, one cluster. The prior record's finding that eleven of its twelve regnorm STRUCTURAL rows are branch-target artefacts of being one instruction short was re-confirmed by fndiff's own note this run: `(12 further same-opcode row(s) differ only in a normalized BRANCH TARGET, each with an unpaired cluster between the branch and its target)`. Closing the single `mr` closes them all.",
    "scope": "src/game/ui/btext.c, StringTextHeightSub and the static inline TextLineHeight only. NO RETAINED CHANGE -- the probe was reverted with `probe.py --discard --whole-file` and the file is byte-identical to HEAD. TextHeightMLines, which shares the helper and is byte-exact, was never regressed (the probe folded away entirely).",
    "verification": "`defake_gate.py baseline game/ui/btext --at-head` anchored to commit 0f39fbd2c, source sha1 2b1b6947d: 46 functions, 42 at real 0, DATA baseline extab 248/248 and extabindex 0/372. Clean-state BASELINE banked before the edit (first-baseline trap honoured): real 55, T111/O110, multiset 1t, FUZZY (fresh report) 98.3333%. Post-probe: NEUTRAL-IDENTICAL, then `probe.py --discard --whole-file` printed `RESTORED  real 55 (insns T111/O110, multiset 1t)`. `git status --short` shows no modification to src/game/ui/btext.c. The RELOC-SYMBOL SCREEN is reported NOT RUN by probe because the two streams differ in instruction count, so the wrong-global class remains UNSCREENED on this function -- that is a measured gap, not an all-clear."
  },
  "denial": {
    "expiry_check": "python tools/gdl/fndiff.py game/ui/btext StringTextHeightSub --ops -- the denial expires the moment this stops printing a single `delete T[68:69]@110-114=['mr']` at T111/O110, because every one of the three classes was measured against exactly that cluster.",
    "falsifier": "Any source form that emits an `mr` into total's home r3 immediately after the idx arm's `mullw` while holding T111/O111 -- i.e. reproducing the target's `mullw r0,r3,r0 / mr r3,r0` pair. The register the target copies FROM is r0, MWCC's anonymous expression scratch, so a named local can never be its carrier; a falsifier must therefore come from changing what the MULTIPLY's destination is allowed to be, not from naming the product.",
    "premise_measurement": "Live re-verification at head 0f39fbd2c, NOT inherited: `python tools/gdl/fndiff.py game/ui/btext StringTextHeightSub --ops` prints `==== StringTextHeightSub: target 111 insns, ours 110 / net count: TARGET is longer by 1 / opcode multiset: DIFFERS  target-only: +1 mr  ours-only: (none) / delete  T[68:69]@110-114=['mr']` plus `(12 further same-opcode row(s) differ only in a normalized BRANCH TARGET ...)`. The cluster list and its single offset are therefore CURRENT, not carried. `probe.py game/ui/btext StringTextHeightSub --ops` baseline: real 55, insns T111/O110, multiset 1t, FUZZY (fresh report) 98.3333%. Class (3) then measured `NEUTRAL   real 55 (insns T111/O110, multiset 1t) [NEUTRAL-IDENTICAL: object bytes unchanged]`, `NEUTRAL-IDENTICAL-PROOF unit=game/ui/btext fn=StringTextHeightSub bytes=dc6ece6330e3 real=55 insns=T111/O110 multiset=1t`. Classes (1) and (2) were measured by the prior record at NEUTRAL (real 55, still 110 insns) and REGRESSED (real 55 -> 79) respectively.",
    "scope": "Three source construct classes on StringTextHeightSub's single missing `mr`, where the target builds the idx arm's product in r0 and copies it into total's home r3 (`addi r3,r5,1 / add r0,r6,r30 / mullw r0,r3,r0 / mr r3,r0 / b ->1a4`) and ours targets r3 directly (`addi r3,r5,1 / add r0,r6,r30 / mullw r3,r3,r0 / b ->1a0`): (1) SINGLE-EXIT ASSIGNMENT -- rewriting the idx arm as `total = TextLineHeight(...)` with one trailing `return total;`; (2) HELPER RETURN-TEMP -- giving the inlinee a named result local; (3) COMMUTATIVE REASSOCIATION -- transposing the product's two operands inside the inlinee. It does NOT deny a fourth class, and it does NOT deny the image-wide census the prior record asked for. Classes (1) and (2) are inherited verbatim from attempt.bb-stringtextheightsub-multiply-destination-decides-the-owed-copy.20260901.v1, whose denial was PROSE ONLY and is quarantined as UNGATED-PROSE-DENIAL; this record restates them with the scope, measurement, expiry command and falsifier that record could not carry."
  },
  "function": "function:StringTextHeightSub",
  "held_fixed": "The one probe varied ONLY the operand order of the product returned by the static inline TextLineHeight: `(n + 1) * (fh + spacing)` became `(fh + spacing) * (n + 1)`. The helper's signature, its loop, its `n` counter, both call sites inside StringTextHeightSub and the third call site in TextHeightMLines (which is byte-exact and must not regress) were all held constant.",
  "hypothesis": {
    "cheapest_refuting_observation": "Grep the image for functions whose `fndiff --ops` residual is exactly `target-only: +1 mr` with count asymmetry of one, using fndiff's IMPORTABLE CORE rather than a per-function subprocess. If the population is one -- this function -- there is no class to census and the census premise is refuted in a single pass with no builds.",
    "count_consequence": "The cure must be count-POSITIVE by exactly one instruction (ours 110 -> 111), which is the unusual direction: the function is WebFrank-ineligible precisely because of that asymmetry (claim.law.webfrank-cannot-close-a-count-asymmetric-residual.20260831.v1), so no postprocessor rule can substitute for the source fix however small the residual gets.",
    "screened_against_target": "no -- the census has not been run, and neither the prior record nor this one measured the population. What IS screened against the target is the mechanism: the aligned view at 0x110 was read this run and the target's `mr r3,r0` is confirmed present at T[68] with ours absent.",
    "statement": "The prior record's own closing suggestion is still the right next move and is now the ONLY untried one: an image-wide census of arithmetic-destination-versus-operand selection paired with an owed `mr`, on the method that settled the ADDR16_LO class (claim.law.addr16-lo-home-copy-census-no-source-discriminant.20260831.v1). Three source classes have now failed on this one function, which is the point at which the population question outranks another probe: if the census finds a large population the class is worth one measurement for everybody, and if it finds a small one this function should be accepted as a named one-instruction residual rather than probed a fourth time."
  },
  "id": "attempt.NC_stringtextheightsub-the-multiply-destination-axis-gets-a-typed-denial-and-a-third-refuted-class.20260903.v1",
  "kind": "attempt",
  "outcome": "negative",
  "recorded_at": "2026-09-03T22:56:01Z",
  "residual": {
    "capability_needed": null,
    "family": "copy-form",
    "measured_at": "2026-09-03",
    "signature": "opcode multiset: DIFFERS  target-only: +1 mr  ours-only: (none); insns T111/O110; 1 ops cluster `delete T[68:69]@110-114=['mr']`"
  },
  "residual_class": "REGISTER_ONLY",
  "schema_version": 1,
  "valid_from": "2026-09-03"
}
