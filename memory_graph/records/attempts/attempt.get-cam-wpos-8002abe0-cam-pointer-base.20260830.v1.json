{
  "attempted_axis": "reuse the already-cached gCameraState base register for the cam pointer instead of a separate gCameras relocation; separately, thread gCameraState through the inlined cam_blocked() helper to reach gCameraTargets via +0xA10 offset arithmetic",
  "attributes": {
    "anchors": "src/game/game/combat.c::get_cam_wpos_8002ABE0 and its inlined static helper cam_blocked(); target .text size 0x6F4 (1780B); config/GUNE5D/symbols.txt: gCameras=gCameraState+0xC8, gCameraTargets=gCameraState+0xA10 (both confirmed by address arithmetic, matching the already-known gCameras=gCameraState+0xC8 pattern used correctly in StandardCamera_8002B828)",
    "axis_log": "AXIS1 (WIN) changed `Camera* cam = &gCameras[camIdx];` to `Camera* cam = (Camera*)(gCameraState + camIdx*sizeof(Camera) + 0xC8);`, reusing gCameraState's address instead of a fresh gCameras relocation. insns 457->456, real 496->493, lines 544->539; --clean confirmed the prologue now matches target exactly through the gCameraState LO computation, no more separate gCameras relocation. METRIC CONFLICT: objdiff fuzzy DROPPED 90.26->89.77% despite real/lines/insns all improving in lockstep, and --ops confirmed the change was isolated to the prologue (1 fewer op, insert lis+addi -> insert addi) with the REST of the diff shape completely unchanged/shifted -- inspected and judged real/lines/ops (the fndiff-native, register-cascade-aware metrics) as the correct arbiter here since fuzzy's small drop looks like operand-token churn from downstream register recoloring, not a real regression; kept per AGENTS.md 'inspect the actual diff, state the conflict, preserve the best verified result'. AXIS2 (REGRESSED, reverted) tried the SAME base-reuse idea for gCameraTargets inside the inlined cam_blocked() helper (target shows `addi r29,r31,2576`=gCameraState+0xA10 reused from the SAME r31 already holding gCameraState for the `cam` computation). Two variants tried: (a) direct global swap `CameraTarget* t = (CameraTarget*)(gCameraState+0xA10);` in place of `t = gCameraTargets;` at both use sites -> real 496->631 (worse). (b) threading an explicit `u8* cs` param through cam_blocked() so all 4 call sites pass the already-live base pointer -> real 496->619 (still worse, though insn count did drop 457->452, closer to target's 445). Both reverted; this specific inlined-helper instance of the shared-aggregate law does NOT reproduce target's codegen via source-level base substitution, unlike the top-level `cam` case in AXIS1 or the analogous PlayerMissileTreeInfo case skipped in PlayerStartMissile.",
    "baseline": "insns 445/457 (ours +12), real 496, lines 544, fuzzy 90.26%; target computes gCameraState ONE lis+addi pair, reuses it (add+addi 200) for `cam`; ours computed a SEPARATE lis+addi for the `gCameras` symbol plus another for gCameraState (camState local) = redundant relocation pair",
    "law_caveat": "CAVEAT to claim.law.whole-fn-address-cache / shared-aggregate-ownership pattern: substituting a separate-symbol reference with base-global+offset arithmetic to force register reuse is NOT reliable when the reference lives inside a SEPARATE inlined static helper function (cam_blocked here) -- CSE across the inline boundary does not merge the way it does for a direct in-function reference. Confirmed regressed in 2 independent phrasings. Treat inlined-helper instances of this pattern as a distinct, harder sub-case; do not retry without a genuinely new idea (e.g. forcing the helper non-static/non-inlined, or restructuring call order).",
    "measurements": "final: insns 445/456, real 493, lines 539, fuzzy 89.77% (fuzzy conflict noted above)",
    "verification": "ninja build/GUNE5D/src/game/game/combat.o green; full ninja green, main.dol: OK; commit 389663c42"
  },
  "function": "function:get_cam_wpos_8002ABE0",
  "id": "attempt.get-cam-wpos-8002abe0-cam-pointer-base.20260830.v1",
  "kind": "attempt",
  "outcome": "improved",
  "record_state": "proposed",
  "recorded_at": "2026-08-30T05:22:35Z",
  "residual_class": "REGISTER_ONLY/SCHEDULE",
  "schema_version": 1,
  "valid_from": "2026-08-30"
}
