{
  "attempted_axis": "alias-lever rescreen (no edit): screened this function against the three levers that carried the sibling InitCamera this session and against its own recorded cap, and recorded a newly identified structural lead that is NOT the capped constant-fold",
  "attributes": {
    "final_state": "insns 595/588, real 875, fuzzy 93.5866% -- UNCHANGED, no source edit made this pass. Multiset: target-only +1 addi +2 and +2 li +2 mr +1 or. +2 xor; ours-only -1 clrlwi. -1 lwz -1 stw.",
    "law_screen": "claim.law.uniform-stack-displacement-delta-is-a-pad-applied-last.20260831.v1 (this session's own finding): SCREENED, NOT applicable -- prologue byte-identical (stwu r1,-56, stmw r26,24(r1) both), confirming the v1 record's frame note by direct disassembly. claim.law.repeated-extern-read-needs-local-for-target-cse.20260831.v1 (this session's own finding): SCREENED, NOT applicable -- multiset carries zero lfs/lfd imbalance. claim.law.single-use-field-pointer-alias-is-folded-away.20260831.v1 and claim.law.cached-base-alias-defeats-rematerialized-index-base.20260831.v1: SCREENED -- only ONE addi token of imbalance, and it belongs to the lbl_80111A08 lead below rather than to any field-pointer alias, so the alias lever this lane exists to apply does NOT govern this function. claim.law.absent-target-guard-means-tautological-reconstruction.20260831.v1, claim.law.tautological-fp-compare-needs-extern-opacity.20260831.v1 and claim.law.extern-zero-init-blocks-constant-propagation.20260831.v1 (all post-dating the v1 cap): SCREENED against the capped `X & 0` fold. All three describe OPAQUE MEMORY reads defeating the constant folder, but target's stream materialises the two constants with `li r4,0` / `li r3,1` -- IMMEDIATES, not loads -- so extern opacity would produce the wrong instructions and none of the three unparks the cap. Recorded so the next pass does not re-derive this.",
    "new_lead": "NEW, not in the v1 record and NOT the capped axis. Target materialises TWO base registers before the guard branch: r30 = lbl_80118B60 and r31 = lbl_80111A08. Ours materialises r30 = our own `...rodata.0` section base and r31 = lbl_80118B60 -- i.e. the SAME two-base structure with the roles SWAPPED, plus a relocation-form difference (target relocates against a named symbol where our string pool relocates against the TU's local rodata section). Because r31 is live across the whole function body, this swap alone re-colours every later instruction that touches either base and is a plausible major contributor to the 875 real count on a function whose opcode multiset differs by only 13 tokens. Two things to establish next, in order: (1) whether lbl_80111A08 is the string pool base -- if so the fix is a declared `extern char* lbl_80111A08[];` table indexed in place of the inline string literals, which would fix the relocation AND the register order together; (2) whether the materialisation ORDER is reachable by moving the `debugNames = (DebugNameTables*)lbl_80118B60` declaration, since target materialises lbl_80118B60 FIRST (r30) and the pool SECOND (r31) while ours does the reverse.",
    "residual": "The v1 cap on the `(buttons & 0) | (sFlags & 1)` five-instruction boolean combine STANDS and is now screened against three newer opacity laws with a stated reason (target uses immediates, not loads). The v1 record's own unpark condition -- an untried scoped `#pragma peephole off` probe -- also remains untried and is now judged LOW probability by v1's own analysis (the fold is front-end, peephole is back-end); a reconstruction in which the constants arrive as parameters of an inlined static helper (`(v & mask) ^ want`) is the better-supported hypothesis, since inlining after argument materialisation is exactly what produces `li` constants feeding an unfolded algebraic chain. Work the new_lead above BEFORE either.",
    "scope": "src/game/game/combat.c::screen_limitation -- READ-ONLY screen, no edit, nothing committed.",
    "verification": "fndiff --ops and fnasm --diff against the object built from the current tree; no source change, so no gate needed; the session's closing defake_gate.py check --rebuild reports this function unchanged at real 875."
  },
  "function": "function:screen_limitation",
  "id": "attempt.screen-limitation-alias-lever-rescreen.20260831.v2",
  "kind": "attempt",
  "outcome": "capped",
  "recorded_at": "2026-08-31T04:44:38Z",
  "residual_class": "STRUCTURAL (capped constant-fold) + REGISTER_ONLY (new base-register-order lead)",
  "schema_version": 1,
  "supersedes": "attempt.screen-limitation-debug-mask-fold.20260830.v1",
  "valid_from": "2026-08-31"
}
