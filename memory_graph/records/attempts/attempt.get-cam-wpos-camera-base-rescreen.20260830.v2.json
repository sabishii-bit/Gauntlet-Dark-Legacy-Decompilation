{
  "attempted_axis": "rescreen of the remaining camBase-vs-cam register-choice residual (target self-updates ONE register r30 in place for the raw-base-then-cam pointer; ours splits into a volatile temp + a distinct saved register) with 2 new distinct source-shape forms, both confirmed backend-only (byte-identical compiled output to the untouched baseline)",
  "attributes": {
    "anchors": "src/game/game/combat.c::get_cam_wpos_8002ABE0; target .text size 0x6F4 (1780B); python tools/gdl/fnasm.py game/game/combat get_cam_wpos_8002ABE0; python tools/gdl/fndiff.py game/game/combat.c get_cam_wpos_8002ABE0 --count|--clean",
    "axis_log": "AXIS1 (NEUTRAL, byte-identical) rewrote the copy as `u8* camBase = gCameraState + camIdx*sizeof(Camera); *(f32*)(camBase+0xC8+offsetof(Camera,old_wpos[i])) = *(f32*)(camBase+0xC8+offsetof(Camera,wpos[i]));` (x3) then `cam = (Camera*)(camBase+0xC8);` -- separate camBase/cam locals, offsetof spelling per claim.law.offsetof-rename-preserves-protected-web. Compiled output: MWCC's own local CSE recognized the repeated `camBase+0xC8` subexpression and hoisted the addi immediately after computing camBase into r3 anyway (same r3, NOT r30) -- real still 493, insns still 445/456, only the addi's position shifted by one slot within the r3-based sequence. AXIS2 (NEUTRAL, byte-identical to AXIS1 AND to baseline) made `cam` itself the single self-referential variable: `Camera* cam = (Camera*)(gCameraState + camIdx*sizeof(Camera));` (no +0xC8 yet), copy via `*(f32*)((u8*)cam+0xC8+offsetof(...))` (x3, both sides), then `cam = (Camera*)((u8*)cam+0xC8);` -- mirrors the exact self-update idiom target's asm shows AND mirrors the InitCamera c0-hoist technique that worked for a DIFFERENT residual class. Produced byte-for-byte IDENTICAL compiled output to AXIS1 (same r3/r30 split). Two independently-phrased source forms (one with an extra named variable, one collapsing to a single self-referential variable) both normalized to the SAME r3-then-r30 codegen -- confirms this specific register CHOICE (which physical register receives the raw-base value before it becomes `cam`'s saved home) is a backend register-allocation decision not reachable through this class of source restructuring, extending the sibling AXIS2 finding from v1 (cam_blocked's gCameraTargets base-reuse, also backend-only) to the top-level cam pointer itself. Both probes reverted to the simpler original `cam->old_wpos[i]=cam->wpos[i]` form (functionally and byte-identical, cleaner source, no raw-offset debt introduced).",
    "final_state": "insns 445/456, real 493, lines 539, fuzzy 89.77% -- UNCHANGED by this pass (both probes confirmed dead-end, reverted). This function's remaining residual (camBase register choice, plus its known-dead cam_blocked/gCameraTargets sub-case from v1 AXIS2) is now screened via 3 cumulative distinct source-shape axes across 2 sessions with zero movement; do not retry the camBase/cam register-choice axis without a fundamentally new theory (e.g. one that changes overall function register PRESSURE elsewhere, since the target's extra free register at this exact point plausibly traces to the same class of root cause seen in InitCamera's 1-saved-GPR gap, not to this local expression's phrasing).",
    "law_screen": "claim.law.offsetof-rename-preserves-protected-web / offsetof-fused-immediate-counter: screened before choosing the AXIS forms below (target's asm folds the +0xC8 camera-array-element offset into the SAME immediate as the old_wpos/wpos field offsets on the walked base before advancing, which is exactly this law's pattern) -- both forms tried still failed to reproduce it; see negative result below, now recorded as an explicit caveat on the law. claim.law.lwzu-idiom-web-retention: screened, applicable in spirit (self-referential pointer walk) but the walk here isn't a loop -- see AXIS2. claim.law.multifield-alias-defeats-indexed-addressing: screened, not applicable (single scalar copy, not an indexed/walked multi-field loop).",
    "prior_baseline": "insns 445/456, real 493, lines 539, fuzzy 89.77% (unchanged by this pass -- see final_state)",
    "target_pattern_confirmed": "target computes r30 = gCameraState + camIdx*396 (the RAW pre-+0xC8 base, into the register that becomes `cam`'s eventual saved home), uses it DIRECTLY with combined big-literal offsets (300/304/308 -> 316/320/324, i.e. 0xC8+field) for the `cam->old_wpos[i] = cam->wpos[i]` copy, THEN does `addi r30,r30,200` in place to become the real `cam` pointer used everywhere after (small Camera-relative offsets like 196=0xC4=radius). Ours instead computes the raw base into r3 (a volatile/scratch register) and derives cam=r3+200 into r30 via a separate addi -- a 2-register solution vs target's 1-register self-update.",
    "verification": "both probes: ninja build/GUNE5D/src/game/game/combat.o green, fndiff --count/--clean confirmed byte-identical to baseline; reverted, no commit needed (no net change); baseline re-verified green after revert as part of this session's final full ninja (build/GUNE5D/main.dol: OK, commit 6eee2ccdc covers the sibling InitCamera fix only -- this function's source is byte-identical to its pre-session state)"
  },
  "function": "function:get_cam_wpos_8002ABE0",
  "id": "attempt.get-cam-wpos-camera-base-rescreen.20260830.v2",
  "kind": "attempt",
  "outcome": "improved",
  "recorded_at": "2026-08-30T21:09:47Z",
  "residual_class": "REGISTER_ONLY/SCHEDULE",
  "schema_version": 1,
  "supersedes": "attempt.get-cam-wpos-8002abe0-cam-pointer-base.20260830.v1",
  "valid_from": "2026-08-30"
}
