{
  "attempted_axis": "Execute the mandatory step-1 hypothesis of the v1 record (change the number or order of the LOADS rather than the multiply, so that info->animscale lands in f0) and close game/anim/anim's LAST rule, which would delete its whole unit entry from webfrank.json.",
  "attributes": {
    "law_screen": "claim.law.webfrank-pinned-function-source-freeze.20260831.v1 APPLIED - every probe was scored on the pre-webfrank raw body object via `probe.py --raw`, never on the pinned src/ object. claim.law.MV_callee-saved-numbering-has-a-width-class-ahead-of-declaration-order.20260902.v1 SCREENED and NOT applicable - this residual is entirely in VOLATILE FPRs (f0/f3), not the callee-saved set the law governs. claim.law.PP_a-register-only-verdict-cannot-see-a-reassociated-fp-product.20260903.v1 SCREENED - the v1 record already traced both streams to the same grouping by hand and cleared this function of the PointLineDist2D operand-value class; nothing in this pass's three probes changed the operand set, so that clearance still holds. claim.law.webfrank-cannot-close-instruction-count-deltas.20260831.v1 SCREENED and satisfied (106/106 in HEAD and in all three probes).",
    "laws_applied": "[\"claim.law.webfrank-pinned-function-source-freeze.20260831.v1\", \"claim.law.PP_a-register-only-verdict-cannot-see-a-reassociated-fp-product.20260903.v1\"]",
    "probed_form": "THREE probes, all reverted, HEAD restored and re-measured at 4 words. HEAD: `info->seqscale = (f32)((f64)*(s16*)(s + 0x22) * lbl_803457C0 * (f64)info->animscale);`. (1) added `f64 as = info->animscale;` as the first statement inside the guarded block and read `as` in the expression -> 17 words (REGFIELD-ONLY 4, IMMEDIATE 13). (2) added `s16 sq;` to the declaration block after `nf`, inserted `sq = *(s16*)(s + 0x22);` between the `nf` and `rep` reads, and wrote `if (sq > 0)` / `(f64)sq * lbl_803457C0 * (f64)info->animscale` -> NEUTRAL-IDENTICAL. (3) `info->seqscale = (f32)(*(s16*)(s + 0x22) * lbl_803457C0 * info->animscale);` (both (f64) casts deleted) -> NEUTRAL-IDENTICAL, same body hash as (2).",
    "residual": "The rule stays. game/anim/anim carries exactly ONE rule (InitAnim), so a later close of this f0/f3 tie deletes the whole unit entry from webfrank.json - that is the standing prize and it is unchanged. The rule itself is sound (strict, verify_consistent_recolor, no escape); this record extends its source-exhaustion provenance from two axes to five.",
    "scope": "No source change retained. src/game/anim/anim.c edited and restored three times via `probe.py --discard`; `git status --short` reports the file clean at c8a28c3bb.",
    "verification": "defake_gate baseline game/anim/anim --at-head at commit c8a28c3bb (10 functions, 10 at real 0, source sha1 d0e35a49e). Each probe measured from a fresh `probe.py game/anim/anim InitAnim --raw` build of build/GUNE5D/src/game/anim/.postprocess/body/anim.o, which prints the raw differing-word count in the same call. Final state re-measured at 4 raw words, real 6, 106/106."
  },
  "denial": {
    "expiry_check": "python tools/gdl/fnasm.py game/anim/anim InitAnim 0x8c:0xc0 --raw --diff - if ours ever emits `lfs f0,40(r30)` at +0xa4, this record has expired and the function is one step from STRICT. Re-measured 2026-09-03 at c8a28c3bb: ours still emits `lfs f3,40(r30)`, so v1's denial had NOT expired and this record extends it.",
    "falsifier": "Any spelling of the seqscale statement that makes MWCC load `info->animscale` into f0 while keeping 106 instructions.",
    "premise_measurement": "Three `python tools/gdl/probe.py game/anim/anim InitAnim --raw` rebuilds from the c8a28c3bb baseline of 4 raw differing words. (1) `f64 as = info->animscale;` staged inside the guarded block, expression reading `as`: REGRESSED to 17 raw words, DECODE `REGFIELD-ONLY 4, IMMEDIATE 13` - the added f64 local shifted thirteen stack displacements and did not touch the four register fields at all. (2) `s16 sq;` declared after `nf`, `sq = *(s16*)(s + 0x22);` hoisted beside the nf/rep reads, guard and expression reading `sq`: NEUTRAL-IDENTICAL, 4 words, NEUTRAL-IDENTICAL-PROOF bytes=a935da4e3ee5. (3) both redundant casts removed - `(f32)(*(s16*)(s + 0x22) * lbl_803457C0 * info->animscale)`: NEUTRAL-IDENTICAL, 4 words, the SAME body hash a935da4e3ee5. Two independent NEUTRAL-IDENTICALs at one byte hash.",
    "scope": "The EXPRESSION-LEVEL and DECLARATION-LEVEL source surface of InitAnim's seqscale statement only. Together with the v1 record's two operand-order probes this closes: inner multiply order, outer multiply order, redundant-cast removal, leaf naming, and staging a value into an added f64 local. It does NOT deny a pragma lever (opt_lifetimes / peephole around this function), which was still not tried, and it does not deny the function to a lane with a new idea."
  },
  "function": "function:InitAnim",
  "held_fixed": "The guard `if (*(s16*)(s + 0x22) > 0)`, the else arm `info->seqscale = info->animscale;`, the int-to-double conversion idiom, the function signature and the frame were constant across all three probes. Only ONE thing varied per probe: probe (1) varied the DECLARATION SET (one added f64 local), probe (2) varied the LEAF SPELLING (a named s16 local for the twice-read *(s16*)(s+0x22)), probe (3) varied the CAST NODES (removed both redundant (f64) casts). The arithmetic VALUE was held fixed by construction in all three - no probe changed the grouping or the operand set, only how the same product is spelled.",
  "hypothesis": {
    "cheapest_refuting_observation": "Any single-statement edit to the seqscale expression that moves the object bytes off a935da4e3ee5 without changing the instruction count refutes 'the tree is already canonical'. Measured 2026-09-03: two structurally distinct edits both landed exactly on that hash, and the only probe that DID move the bytes (the added f64 local) moved stack displacements and left all four register fields untouched.",
    "count_consequence": "Count-neutral. All three probes held 106/106, and the pragma lever named above is expected count-neutral too. A count-asymmetric result would break that parity and would invalidate the standing rule rather than close it, so any cure must stay at 106/106. This record does NOT re-screen or close the postprocessor path for InitAnim - the shipped rule stands untouched and no verifier was re-run in this pass; the work here is entirely source-side.",
    "screened_against_target": "yes - the four load destinations and the two multiply operand orders were read from `fnasm.py game/anim/anim InitAnim 0x8c:0xc0 --raw --diff` against retail bytes at c8a28c3bb before and after each probe.",
    "statement": "The f0/f3 assignment is not reachable from this statement's source text at all. The two NEUTRAL-IDENTICALs are the evidence: a leaf rename and a cast-node removal are structurally different edits to the expression tree, and both produced the SAME object bytes (a935da4e3ee5), so the tree MWCC allocates over is already canonical before register selection - the source text never reaches the decision point. The one axis outside that surface, and the only one left, is a scoped `#pragma opt_lifetimes on/reset` bracketing InitAnim (the form mb_particle.c already uses), which changes the allocator's live-range policy rather than the tree it is given."
  },
  "id": "attempt.PR_initanim-load-destination-is-unreachable-from-the-expression-two-neutral-identicals.20260903.v2",
  "kind": "attempt",
  "outcome": "capped",
  "recorded_at": "2026-09-03T21:21:58Z",
  "residual": {
    "capability_needed": null,
    "family": "regalloc-web",
    "measured_at": "2026-09-03",
    "signature": "106/106 insns, 0 mnemonic divergence, 4 differing words at +0x9c/+0xa4/+0xb0/+0xb4; K and S occupy each other's registers (target K=f3 S=f0, ours K=f0 S=f3)"
  },
  "residual_class": "REGISTER_ONLY",
  "schema_version": 1,
  "supersedes": "attempt.PR_initanim-the-inner-multiply-follows-source-order-and-the-outer-is-canonicalised.20260903.v1",
  "valid_from": "2026-09-03"
}
