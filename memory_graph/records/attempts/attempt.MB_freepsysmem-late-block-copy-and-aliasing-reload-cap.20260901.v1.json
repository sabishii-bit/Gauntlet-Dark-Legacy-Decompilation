{
  "attempted_axis": "Side-decompose freePsysMem's list-unlink residual into (a) a misplaced `block = prev` copy and (b) two aliasing reloads. (a) closed and committed; (b) capped after four probes across two baselines.",
  "attributes": {
    "law_screen": "claim.law.live-zero-copy-vs-remat-is-allocator-not-source.20260831.v1 APPLIED - it names the target-only `li r4,0` at +0xcc, a rematerialisation of zero into `prev`'s home on the path where the just-executed `cmplwi r4,0`/`bne` already proves prev==NULL. claim.law.constant-copy-vs-rematerialize-is-source-unreachable.20260831.v1 APPLIED to the same row and, by extension, to the target-only `addi r10,r8,0` at +0x78 (a redundant re-copy of `mergeNext = next` on the else path, where the copy at +0x30 already dominates); both laws are stated for the same-count direction, this site is the count-asymmetric direction (the instruction is absent from ours entirely) and the mechanism is identical. claim.law.lwzu-idiom-web-retention.20260830.v1 SCREENED: no update-load idiom in this function, does not apply. claim.law.EN-equal-count-opcode-respell-must-be-arbitrated-on-fuzzy-not-real.20260901.v1 APPLIED to the retained edit (equal counts, multiset moved) - arbitrated on a fresh full-report fuzzy.",
    "laws_applied": "[\"claim.law.live-zero-copy-vs-remat-is-allocator-not-source.20260831.v1\", \"claim.law.constant-copy-vs-rematerialize-is-source-unreachable.20260831.v1\", \"claim.law.EN-equal-count-opcode-respell-must-be-arbitrated-on-fuzzy-not-real.20260901.v1\"]",
    "probed_form": "RETAINED (real 56 -> 40, fuzzy 91.72 -> 93.52): in the `if (prev != NULL)` coalesce block, move `block = prev;` from immediately after `prev->next = block->next;` to immediately AFTER the if/else, before `bytes += prevBytes;`. One line moved, nothing else. This alone made the entire second unlink block byte-identical: the target's `addi r5,r4,0` at +0x1b4 appeared in place of our early `mr r5,r4` at +0x190, `lwz r5,4(r5)` matched, and every branch displacement in the block realigned. CAPPED AXIS (four probes, two baselines, all producing the SAME whole-prologue cascade): caching the aliasing reload in a local. Form 1, shared existing local, both blocks: `next = mergeNext->next; block->next = next; pool->next = block; if (next != NULL) { next->prev = block; } else { pool->last = block; }` and the mirror in the prev block - real 56 -> 198, count 125 -> 123. Form 2, dedicated function-scope local `PsysMemBlock* link;` declared last, both blocks - real 56 -> 204, IDENTICAL cluster list to form 1. Form 3, block-scoped local in the mergeNext block ONLY: `PsysMemBlock* link = mergeNext->next; block->next = link; ... if (link != NULL) { link->prev = block; }` - real 56 -> 201, same prologue cascade. Form 4, form 3 re-run in the prev block on the IMPROVED (real 40) baseline to test alignment sensitivity - real 40 -> 207, same prologue cascade.",
    "residual": "real 40, count-exact 125/125, four clusters. TWO ARE ALLOCATOR-CLASS, named above and NOT source-reachable: target-only `addi r10,r8,0` @+0x78 and target-only `li r4,0` @+0xcc, both redundant rematerialisations into a register that already holds the correct value. TWO ARE SOURCE-CARRIED BUT CAPPED: ours emits `lwz r7,4(r5)` @+0x15c and `lwz r7,4(r4)` @+0x19c, re-reading `block->next` / `prev->next` after the intervening `pool->next = block` store, which may alias; the target keeps the loaded value live across that store instead. The ONLY source form that removes the reload is to cache the load in a local, and all four probes of that form re-plan the whole ENTRY BLOCK: our byte-exact prologue (mflr / addi r5,r3,-16 / stw / li / li / stwu / lwz r8,-12(r3) / lis / lwz / addi / cmplwi / lwz / addi r10,r8,0 / addi / neg / bne, all `=` rows against target) collapses into a different frame and base plan, hoisting the lbl_80128710 lis/addi pair to the top and overwriting r3 with `block` instead of keeping `mem` live. The two extra reloads and the two allocator remats exactly offset each other in the multiset, which is why the function stays count-exact at 125/125. NOT WEBFRANK-ELIGIBLE: the residual is an opcode difference (our lwz vs target addi/li), which is structural and outside the tool's mandate.",
    "scope": "src/game/mb/mb_particle.c, freePsysMem only; one statement moved. Commit bee62470d.",
    "verification": "probe.py BASELINE real 56 (T125/O125, 6t) -> IMPROVED real 40, multiset 6t -> 4t, counts held equal. Fresh full-ninja report fuzzy 91.72% -> 93.52% (the parity-held arbitration the tool demands). Full ninja green: `build/GUNE5D/main.dol: OK`. defake_gate check game/mb/mb_particle = GATE OK, baseline updated. No sibling or pin regression."
  },
  "function": "function:freePsysMem",
  "id": "attempt.MB_freepsysmem-late-block-copy-and-aliasing-reload-cap.20260901.v1",
  "kind": "attempt",
  "outcome": "improved",
  "recorded_at": "2026-09-01T17:33:10Z",
  "residual_class": "MIXED",
  "schema_version": 1,
  "valid_from": "2026-09-01"
}
