{
  "attempted_axis": "Discharge the falsifier that claim.law.AL_the-addr16lo-surplus-copy-roster-is-two-classes-split-by-destination-register-and-only-the-volatile-tenth-is-reachable.20260904.v1 names ON THIS FUNCTION BY NAME -- 'ProcessCritterList is the one untried row carrying the responsive polarity' -- by measuring the declaration-order lever across three orders, then spend the remaining budget on the two axes the live typed denial leaves open.",
  "attributes": {
    "law_screen": "claim.law.AL_the-addr16lo-surplus-copy-roster-is-two-classes-split-by-destination-register-and-only-the-volatile-tenth-is-reachable.20260904.v1 SCREENED and REFUTED ON ITS OWN NAMED ROW -- see `refutes`. The law states 'the reachable remainder is ONE function -- ProcessCritterList' and makes its polarity cut rest on 'TWO probes, one each way, so it is the weakest link by construction'. Three declaration orders measured here leave the surplus copy and the T49/O50 count untouched, so by the law's own falsifier ('if a declaration-order swap there is NEUTRAL-IDENTICAL, the polarity stops predicting and the reachable remainder is ZERO, not one') the reachable remainder of the ADDR16_LO surplus-copy class is ZERO. The refutation is narrower than the law: shape V, the B/V destination cut and the move_logic22 keep are all untouched and still stand. claim.law.pointer-decl-order-flips-addrlo-inplace-vs-fresh-destination.20260901.v1 SCREENED, and its CURE was ALREADY SATISFIED IN THE TREE before this pass -- its discriminant is 'its position relative to the INT counter that competes for the @ha temp's register: p before i wins, i before p loses', and `Player *player;` is FIRST in this function's declaration list, ahead of `s32 i`, which is the law's stated winning order. The predecessor record had already written this down; this pass VERIFIED it against the live source and then measured that the losing orders cost colouring without touching the copy, which is a stronger statement than either. claim.law.NC_pre-frame-addr16-lo-into-r0-owes-a-callee-saved-copy.20260903.v1 SCREENED and CONTRADICTED -- the predecessor record screened this class as 'MEASURED NOT APPLICABLE' because 'at HEAD ours completed the LO LATE (@0x30)'; that screen is now STALE, because the predecessor's own keep (commit d5ea67b0e) moved our LO to @0x10, ABOVE the `@0x18 stwu`, which puts this function squarely inside the class the screen excluded it from. See `hypothesis`. claim.law.live-zero-copy-vs-remat-is-allocator-not-source.20260831.v1 and claim.law.constant-copy-vs-rematerialize-is-source-unreachable.20260831.v1 SCREENED and both still govern the `li r3,0` half; neither is overturned. claim.law.webfrank-cannot-close-a-count-asymmetric-residual.20260831.v1 SCREENED and DECISIVE: T49/O50 puts the function outside every postprocessor class, so no rule request accompanies this record. claim.law.pointer-walk-array-index-regression.20260830.v1 SCREENED and CONFIRMED by probe (4). PIN SCREEN, run not assumed: `python memory_graph/gdlmem.py brief game/enemy/critter` returns `webfrank_pins` of 8 (CritterGetTargetPlayers, CritterLineRootColSub, CritterResolveMultipleTargets, CritterLineNodeColSub, CritterLookForCriticalMove, CritterLineCollide, CritterDoTexmodNode, ProcessCritter) and ProcessCritterList is NOT among them; every probe was reverted, the tree ended at HEAD for this function, and no pool population or pinned window moved.",
    "laws_applied": "[\"claim.law.pointer-decl-order-flips-addrlo-inplace-vs-fresh-destination.20260901.v1\", \"claim.law.webfrank-cannot-close-a-count-asymmetric-residual.20260831.v1\", \"claim.law.pointer-walk-array-index-regression.20260830.v1\"]",
    "probed_form": "FOUR probes, all reverted, the function ends this pass byte-identical to HEAD. (1) `s32 activePlayers; s32 i; s32 total; Player *player;`. (2) `s32 activePlayers; Player *player; s32 i; s32 total;`. (3) `activePlayers = 0; i = 0; total = 0; player = gPlayers; lbl_80344664++; for (; i < 4; i++, player++) {`. (4) DIFFERENT CONSTRUCT CLASS, recorded for completeness and NOT covered by the denial above: deleted the `Player *player;` declaration and the `player = gPlayers;` statement and re-indexed the loop as `for (i = 0; i < 4; i++) { if (gPlayers[i].state == 1) {`. That REGRESSED hard -- `real 29 -> 44 (insns T49/O51, multiset 4t)`, target-only `+1 addi`, ours-only `-1 add -2 li`, clusters 3 -> 6 -- which is claim.law.pointer-walk-array-index-regression.20260830.v1 firing exactly as it predicts, and it also cost count parity in the wrong direction (O50 -> O51).",
    "residual": "Unchanged at real 29, T49/O50, one surplus instruction. THE MECHANISM, re-derived from the aligned view this pass rather than inherited: ours emits `@0x04 lis r4,0 @gPlayers(ADDR16_HA)` then `@0x10 addi r4,r4,0 @gPlayers(ADDR16_LO)` (IN PLACE, destination == source, and BEFORE the `@0x18 stwu r1,-24(r1)`) then `@0x1c addi r5,r4,0` (the surplus copy into the loop's induction register), where the target emits `@0x04 lis r3,0 @gPlayers(ADDR16_HA)` then `@0x1c addi r5,r3,0 @gPlayers(ADDR16_LO)` FRESH into r5 and reuses the freed r3 for the byte offset at `@0x24 addi r3,r6,0`, copied from the live `activePlayers` zero at `@0x10 li r6,0`. Ours instead rematerialises that offset at `@0x44 li r3,0`. The `-1 li` the multiset reports is therefore a CONSEQUENCE of the surplus address copy, not an independent defect: close the copy and the freed register makes the zero copy available. NOTE FOR ANY LANE READING THE PREDECESSOR'S MANDATORY STEP 1: it directs the next lane to 'attack the `gBig.scratch[i]` ADDRESSING rather than the initialisation'. That premise is measurably wrong -- the gBig addressing is ALREADY byte-identical in both streams apart from register naming (`@0x3c lis`, `@0x40 addi r0,rX,0 @gBig(ADDR16_LO)`, `add r4,r0,r3`, `stfs f0,0(r4)`, `addi r3,r3,4` all pair `=` or `~` on the aligned view), so there is nothing to attack there; the residual lives entirely on the gPlayers side.",
    "scope": "src/game/enemy/critter.c::ProcessCritterList only. NO EDIT RETAINED -- all four probes reverted with `probe.py --discard`, and `git diff --numstat` is empty for this file at the end of the pass. No header, config, webfrank.json or symbols.txt change.",
    "verification": "Clean-state BASELINE banked before the first edit (first-baseline trap honoured): `probe.py game/enemy/critter ProcessCritterList --ops` = real 29, insns T49/O50, multiset 1t, FUZZY (fresh report) 94.5918%, 3 clusters, and probe's own note that the RELOC-SYMBOL SCREEN cannot run because the counts differ ('the wrong-global class is UNSCREENED until then'). Both live denials were expiry-checked BEFORE any edit and BOTH HELD: `fndiff.py game/enemy/critter ProcessCritterList --ops` returned exactly the three named rows and T49/O50, so the predecessor's pointer-materialisation denial is unexpired and was not re-probed. Each probe was measured against that baseline and reverted; after the final `--discard` the function re-scored `RESTORED real 29 (insns T49/O50, multiset 1t)`. Full `ninja -j2` printed `build/GUNE5D/main.dol: OK`; `python -m unittest discover tools/gdl/tests -b` = `Ran 2038 tests ... OK (skipped=2)`; `git diff --check HEAD` and `git diff --cached --check` both exit 0."
  },
  "denial": {
    "expiry_check": "python tools/gdl/fndiff.py game/enemy/critter ProcessCritterList --ops -- re-probe these classes only if the three-row cluster list `replace T[4:5]@10-14=['li'] O=['addi']` / `replace T[9:10]@24-28=['addi'] O=['li']` / `insert O[17:18]@44-48=['li']` changes shape, or if the count delta leaves T49/O50.",
    "falsifier": "Any ordering of this function's four declarations that removes the surplus `addi r5,r4,0` copy or moves the count off T49/O50. The claim is specifically about the COPY and the COUNT, not about `real`: two of the three orders moved `real` by 8 while leaving both untouched, which is why a score-only reading of this axis would be wrong in both directions.",
    "premise_measurement": "`probe.py game/enemy/critter ProcessCritterList --ops` at head 47ae4d37c. BASELINE real 29, insns T49/O50, multiset 1t, FUZZY (fresh report) 94.5918%, 3 clusters. ORDER 1 = HEAD, `Player *player; s32 activePlayers; s32 i; s32 total;` -- real 29. ORDER 2, `s32 activePlayers; s32 i; s32 total; Player *player;` (pointer LAST) -- `REGRESSED vs best 29: real 29 -> 37 (insns T49/O50, multiset 1t)`, and the printed cluster list was IDENTICAL to baseline's three rows. ORDER 3, `s32 activePlayers; Player *player; s32 i; s32 total;` (derived from the TARGET's own zero-emission order) -- `REGRESSED vs best 29: real 29 -> 37 (insns T49/O50, multiset 1t)`, cluster list again IDENTICAL. Two identical A/Bs kill the axis per AGENTS.md iteration discipline, and the decisive fact is not the score: in ALL THREE orders the count stayed T49/O50 and the surplus `addi r5,r4,0` copy survived unchanged. (b) `activePlayers = 0; i = 0; total = 0; ... for (; i < 4; i++, player++)` -- `NEUTRAL real 29 (insns T49/O50, multiset 1t) [NEUTRAL-IDENTICAL: object bytes unchanged]`, `NEUTRAL-IDENTICAL-PROOF unit=game/enemy/critter fn=ProcessCritterList bytes=bbc23d903d86 real=29 insns=T49/O50 multiset=1t head=47ae4d37c` -- categorical for the construct.",
    "scope": "TWO axes on ProcessCritterList, added to the axis the predecessor record already denies. (a) DECLARATION ORDER of the function's four locals, measured across three distinct orders. (b) The POSITION of the `i = 0` initialisation as a statement. It does NOT deny the `gBig.scratch[i]` indexing form, which was probed once as (4) below and regressed rather than folding, and it does not deny a joint probe of (a) with some other axis."
  },
  "function": "function:ProcessCritterList",
  "held_fixed": "All three probes held every STATEMENT of the function byte-for-byte -- the two loops, both bounds, `lbl_80344664++`, the `player->state` test and the `gBig.scratch[i]` store -- and varied exactly one thing each: (1) and (2) the ORDER of the four declarations, with their types and count unchanged; (3) the POSITION of the `i = 0` initialisation statement, with the declaration list unchanged. Probe (4) is the exception and says so: it removed the `player` local entirely and re-indexed the loop, which is a different construct class and is recorded separately below.",
  "hypothesis": {
    "cheapest_refuting_observation": "Zero builds: `python tools/gdl/fnasm.py game/enemy/critter ProcessCritterList 0x0:0x20 --diff`. If our `@0x10 addi r4,r4,0 @gPlayers(ADDR16_LO)` is ever NOT before the `@0x18 stwu r1,-24(r1)`, this function is not a pre-frame instance and the counter-example evaporates. Measured this session: it IS before it, and the destination register field reads r4 in both operand positions, which is also the IN_PLACE polarity claim.law.pointer-decl-order-flips-addrlo-inplace-vs-fresh-destination.20260901.v1 clause (b) describes.",
    "count_consequence": "Instruction counts are 50/49 (ours/target). Any cure must be count-NEGATIVE by exactly one, taking 50/49 to 49/49. The surplus instruction is the copy `@0x1c addi r5,r4,0`, which exists because ours completes the low half in place at 0x10 and then needs it in the loop's induction register; the target spends ONE instruction where we spend two. Until that is closed the function is postprocessor-ineligible by construction, so no rule can substitute for the source fix.",
    "screened_against_target": "yes -- the whole aligned view `fnasm game/enemy/critter ProcessCritterList 0x0:0x60 --diff` was read at head 47ae4d37c and every instruction quoted in this record is quoted at its own offset in both streams. NOT screened: no image-wide re-census of the pre-frame ADDR16_LO population was run this pass; that is the work the statement above hands to the next lane, and this record does not claim to have done it.",
    "statement": "THE SURVIVING RESIDUAL IS A COUNTER-EXAMPLE TO A ONE-DAY-OLD LAW'S DISCRIMINATOR, AND THAT IS THE FINDING WORTH MORE THAN THE SCORE. claim.law.NC_pre-frame-addr16-lo-into-r0-owes-a-callee-saved-copy.20260903.v1 splits eleven pre-frame-ADDR16_LO functions by DESTINATION: 'FOUR write it into r0 and every one of those owes the mr ... SEVEN write it into a NAMED volatile and none of them owes a copy'. ProcessCritterList is a TWELFTH instance, it writes into a NAMED VOLATILE, and it OWES THE COPY -- ours emits `@0x10 addi r4,r4,0 @gPlayers(ADDR16_LO)` before the frame-opening `@0x18 stwu r1,-24(r1)`, destination r4 (volatile, not r0), and then pays `@0x1c addi r5,r4,0`, where the target writes the low half fresh at `@0x1c addi r5,r3,0 @gPlayers(ADDR16_LO)` and pays nothing. That is one of the law's OWN named falsifiers, verbatim: 'a re-run of the census below in which the r0-versus-named-volatile discriminator fails to separate the two groups'. The next lane on this class should re-run `NC_scratch/nc_prologue_census.py`'s successor over the WHOLE image rather than 36 functions, add this row, and re-derive the discriminator -- the law's own note that its population 'is a floor over 36 screened functions and not an image-wide total' is exactly the gap this row falls into. Note that the law's second-order prediction still holds here: the ONE lever it reports as reaching a prologue at all is a CROSS-CLASS declaration exchange, and this function has no second address-class local to exchange against, which is consistent with all three declaration orders leaving the copy in place."
  },
  "id": "attempt.CR_processcritterlist-the-declaration-order-lever-is-inert-here-so-the-addr16lo-reachable-remainder-is-zero.20260904.v1",
  "kind": "attempt",
  "outcome": "negative",
  "recorded_at": "2026-09-04T06:00:13Z",
  "refutes": "claim.law.AL_the-addr16lo-surplus-copy-roster-is-two-classes-split-by-destination-register-and-only-the-volatile-tenth-is-reachable.20260904.v1",
  "residual": {
    "capability_needed": "none, and this is categorical: the instruction counts are 50/49 (ours/target), measured this pass, so the residual is count-ASYMMETRIC and sits outside every postprocessor class by claim.law.webfrank-cannot-close-a-count-asymmetric-residual.20260831.v1; `wf_word_diff.py` refuses the function outright. No rule may be authored for it.",
    "family": "live-zero-remat",
    "measured_at": "2026-09-04",
    "signature": "DIFFERS target-only: (none) ours-only: -1 li; insns T49/O50; 3 ops clusters"
  },
  "residual_class": "STRUCTURAL",
  "schema_version": 1,
  "valid_from": "2026-09-04"
}
