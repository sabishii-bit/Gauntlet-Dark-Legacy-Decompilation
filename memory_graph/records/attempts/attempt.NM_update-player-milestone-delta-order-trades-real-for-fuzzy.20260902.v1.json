{
  "attempted_axis": "First record on this function. 123/123 with an IDENTICAL opcode multiset and two 2-instruction rotations; the axes tried were the DECLARATION ORDER of the three per-milestone deltas (ours dy/dz/dx against the target's emission order dy/dx/dz), the position of the absolute-value store between them, and the declaration order of the two entry pointer locals.",
  "attributes": {
    "law_screen": "claim.law.identical-multiset-is-blind-to-displacements.20260831.v1 APPLIED and DECISIVE: the multiset reads IDENTICAL (123/123) while `--ops` prints two 1-instruction rotations plus 2 aligned IMMEDIATE rows, one of them flagged PAIRING UNRELIABLE, so both were read in the aligned view first. claim.law.fuzzy-can-underweight-a-real-improvement.20260830 and the run-35 fuzzy-before-bank rule APPLIED and DECISIVE in the opposite direction from usual: here real was FLAT and the STRUCTURE improved, and fuzzy still fell, so the edit was discarded -- this is the rule working as a brake, not as a promoter. claim.law.MV_callee-saved-numbering-has-a-width-class-ahead-of-declaration-order.20260902.v1 APPLIED to probe 3 and consistent with it. core-screen offsetof laws: not applicable, the raw milestone offsets are already spelled through offsetof(ItemRuntime, milestones).",
    "laws_applied": "[\"claim.law.identical-multiset-is-blind-to-displacements.20260831.v1\", \"claim.law.MV_callee-saved-numbering-has-a-width-class-ahead-of-declaration-order.20260902.v1\"]",
    "probed_form": "PROBE 1, REJECTED ON FUZZY (real 46 flat, fuzzy 95.5935 -> 95.4959): swap the dz and dx declarations so the three are declared dy, dx, dz --\n        f32 dx = locals.position[0] - *(f32*)(milestone + offsetof(ItemRuntime, milestones) + 12 * sizeof(f32));\n        f32 dz = locals.position[2] - *(f32*)(milestone + offsetof(ItemRuntime, milestones) + 14 * sizeof(f32));\n(after the existing dy). The three `fsubs` came into the target's dy/dx/dz order, but the `stfs f0,24(r1)` that the target interleaves between the dy and dx subtractions stayed after both, and four load rows went the other way.\nPROBE 2, NEGATIVE (2 ops clusters -> 7): declare `f32 dx; f32 dy; f32 dz;` and assign dy first, then the two absolute_y statements, then dx and dz --\n        dy = locals.position[1] - ...;\n        locals.absolute_y.value = dy;\n        locals.absolute_y.bits &= 0x7FFFFFFF;\n        dx = ...;\n        dz = ...;\nMWCC hoisted the whole store/lwz/clrlwi/stw block ABOVE the remaining deltas instead of interleaving it.\nPROBE 3, NEUTRAL-IDENTICAL: swap the two entry declarations --\n    u8* runtime = (u8*)&sItemRuntime;\n    u8* player = (u8*)player_ptr;\nAll three reverted with probe.py --discard; src/game/world/items.c is byte-unchanged by this record.",
    "residual": "real 46 at 123/123, 25 raw differing words, opcode multiset IDENTICAL. TWO 1-instruction rotations. (1) At +0x1c the target issues `lis r4,@sItemRuntime(ADDR16_HA)` before `lwz r3,0(r3)`; ours issues the load first and defers the lis, and the two `addi` that follow (`addi r31,rX,@sItemRuntime(ADDR16_LO)` and `addi r4,r1,40`) are correspondingly swapped. (2) At +0x64..+0x74 the target orders `stfs f0,24(r1)` / `fsubs f1,f5,f4` / `lwz r0,24(r1)` / `fsubs f2,f3,f2` where ours orders `fsubs` / `stfs` / `fsubs` / `lwz` -- the same four instructions, rotated by one slot. Everything else is the FPR/GPR renaming those two rotations drag behind them, plus the callee-saved pair r28/r29 being assigned to `player`/`offset` in the opposite order from the target.",
    "scope": "src/game/world/items.c, function update_player_milestone, probe only -- NO source change retained. game/world/items carries three webfrank pins (DistanceToClosestPlayer, InitLighting, InitItemInfoData); none was touched and no instruction count moved.",
    "verification": "probe.py BASELINE real 46 before the first edit; defake_gate baseline game/world/items taken at commit 260e636be. Fresh-report fuzzy measured for the baseline (95.5935) and for probe 1 (95.4959) via probe.py --fuzzy, which is the arbiter that decided the cap. All probes reverted; `git status --porcelain` shows only the untracked LANE_LOCK. No commit."
  },
  "denial": {
    "expiry_check": "python tools/gdl/probe.py game/world/items update_player_milestone --fuzzy at the current HEAD -- if the baseline fuzzy is no longer 95.5935 the arbitration that rejected probe 1 was taken at a different alignment and probe 1 must be re-measured before being trusted as negative.",
    "falsifier": "A fresh-report fuzzy reading above 95.5935 for the dy/dx/dz declaration order. The probe's structural argument is sound; only the fuzzy number rejected it, so a later alignment could flip it.",
    "premise_measurement": "Probe 1 put the three fsubs into the target's dy/dx/dz emission order and moved real 46 -> 46 with the IMMEDIATE row count going 2 -> 4; its fresh objdiff fuzzy READ 95.4959 against the baseline's 95.5935, so the mandated fuzzy-before-bank arbiter REJECTED it despite the structural argument. Probe 2 (absolute_y statements hoisted between dy and dx) took the ops clusters from 2 to 7 and pulled the whole store/load block above the remaining deltas. Probe 3 returned probe.py NEUTRAL-IDENTICAL: NEUTRAL-IDENTICAL-PROOF unit=game/world/items fn=update_player_milestone bytes=0d8890fe0e8b real=46 insns=T123/O123 multiset=0t head=260e636be, a proved allocator class boundary between the two pointer declarations.",
    "scope": "Three axes on this one function: the dx/dz declaration order, moving the absolute_y store between the delta assignments, and the `player`/`runtime` entry declaration order. It does NOT deny the two rotations being closed by some other means, and no postprocessor screen was run here at all."
  },
  "differing_words": 25,
  "function": "function:update_player_milestone",
  "held_fixed": "All three probes held the GetPlayerPos call and its stack argument, the two tolerance loads, the `sNumMilestones` loop bounds and `offset += 0x68` induction, the absolute-value bit-mask idiom, the fqdist call, the milestone[] shift loop and the whole ShowMilestones tail; probe 1 varied ONLY the relative order of the dx and dz declarations, probe 2 ONLY the position of the two absolute_y statements relative to them, probe 3 ONLY the order of the two entry pointer declarations.",
  "id": "attempt.NM_update-player-milestone-delta-order-trades-real-for-fuzzy.20260902.v1",
  "kind": "attempt",
  "outcome": "capped",
  "recorded_at": "2026-09-02T23:15:26Z",
  "residual": {
    "capability_needed": null,
    "family": "schedule-window",
    "measured_at": "2026-09-02",
    "signature": "0t opcode multiset IDENTICAL (123/123); insns T123/O123; 2 aligned same-opcode IMMEDIATE rows; RAW DIFFERING WORDS = 25 of 123 (tools/gdl/composed_census/wf_word_diff.py, measured 2026-09-02); real 46; fuzzy 95.5935"
  },
  "residual_class": "REGISTER_ONLY",
  "schema_version": 1,
  "valid_from": "2026-09-02"
}
