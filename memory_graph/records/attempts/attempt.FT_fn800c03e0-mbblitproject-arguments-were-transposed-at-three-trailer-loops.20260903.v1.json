{
  "attempted_axis": "Census fn_800C03E0 from the aligned view rather than from its `real` number, then close what the census names. The census found the residual is NOT allocator noise at the top: three trailer loops call mbBlitProject with its two scalar arguments TRANSPOSED relative to the target, which is a semantic reconstruction bug that every score was reading as register churn. Corrected at all three sites; two further structural axes probed and reverted.",
  "attributes": {
    "law_screen": "claim.law.declared-alias-vs-repeated-cast.20260830.v1 SCREENED, NOT APPLICABLE to the keep -- the keep is an argument transposition, not an aliasing question. claim.law.live-zero-copy-vs-remat-is-allocator-not-source.20260831.v1 SCREENED AND CONFIRMED against the textX probe -- the target's `@0x3f0 slwi r4,r18,3` reads a variable that is provably zero while ours emits `@0x3f4 li r4,0`, which is exactly this law's live-zero shape; the probe that tried to reach it from source LOST COUNT PARITY (T433/O434) because MWCC hoisted the now-invariant shift out of the loop, so the law's 'allocator not source' reading is upheld here rather than overturned. claim.law.identical-multiset-is-blind-to-displacements.20260831.v1 SCREENED AND APPLIED -- the multiset stayed 6t across the whole keep while `real` moved 500 -> 482, which is precisely a delta the multiset cannot see, so the keep was arbitrated on fresh objdiff fuzzy per the parity-held rule. claim.law.stack-slots-descend-in-declaration-order.20260831.v1 SCREENED, NOT APPLIED -- no frame delta; frame is 184 in both streams. claim.law.EN-equal-count-opcode-respell-must-be-arbitrated-on-fuzzy-not-real.20260901.v1 APPLIED -- counts were equal and unchanged (T433/O433) at every keep, so probe's PARITY-HELD banner was obeyed and each keep was taken on a FRESH report fuzzy, not on `real`. WEBFRANK PIN SCREEN: config/GUNE5D/webfrank.json pins dbgTextInit in this TU; fn_800C03E0 is a different function and the edit changes neither the anonymous pool population nor a pinned window's relocations, and the full ninja replayed the build green.",
    "laws_applied": "[\"claim.law.identical-multiset-is-blind-to-displacements.20260831.v1\", \"claim.law.EN-equal-count-opcode-respell-must-be-arbitrated-on-fuzzy-not-real.20260901.v1\", \"claim.law.live-zero-copy-vs-remat-is-allocator-not-source.20260831.v1\"]",
    "probed_form": "KEEP, three hunks, verbatim: `mbBlitProject(quad, j, 2);` -> `mbBlitProject(quad, 2, j);` at each of the three trailer loops (the `k < 3, i += 12` loop in the `lbl_8034475C == 3` block, and the two `k < 6, i += 6` loops in the `== 2` and `== 4` blocks). PROBE 1 (reverted), two edits, verbatim: `        for (i = 0; i < 24; i++) {` -> `        s32 textX;\\n        for (textX = i = 0; i < 24; i++) {` in the `lbl_8034475C == 5` block, and `dbgTextPrintfPx(row->color, 0, line, &lbl_80348EF4,` -> `dbgTextPrintfPx(row->color, textX * 8, line, &lbl_80348EF4,`. RESULT: COUNT-PARITY LOST T433/O433 -> T433/O434, real 482 -> 509, multiset 6t -> 7t, extabindex MOVED -- MWCC hoisted the now-loop-invariant `textX * 8` out of the loop (ours gained `slwi` at @0x3d0 and an addi at @0x3f4) whereas the target computes it INSIDE the loop at @0x3f0. Discarded. PROBE 2 (reverted), one hunk applied at both `k < 6` trailer loops, verbatim: `        for (k = 0, i = 0; k < 6; k++, i += 6) {` -> `        i = 0;\\n        for (k = 0; k < 6; k++, i += 6) {`. RESULT: real 486 against the kept state's 482, multiset still 6t, extabindex MOVED. Discarded. Both discarded with `probe.py game/pb/dbgtext.c fn_800C03E0 --discard`; `git diff --numstat` empty afterwards.",
    "residual": "At HEAD 23a0f848e: real 482, fuzzy 94.5843%, insns T433/O433, multiset 6t (`target-only: +1 divwu +1 li +1 slwi  ours-only: -3 addi`), 9 clusters. Decomposed for the next lane, none of it retried this run beyond the two reverted probes: (a) LIVE-ZERO at @0x3f0 -- target keeps a variable that is provably 0 and emits `slwi r4,r18,3`, initialised before the loop as `addi r18,r19,0` (a copy of i, i.e. a chained `textX = i = 0`), while ours folds to `li r4,0`; reachable only by a form that does NOT let MWCC hoist the shift, which the probe above did. (b) CSE-SHARE at @0x608 -- target discards the guard quotient (`@0x5e8 divwu. r0,r24,r0` with a freshly rematerialised `@0x5e4 li r0,4882`) and RECOMPUTES the argument as `@0x608 divwu r4,r24,r19` against the persistent 4882 in r19, while ours keeps the guard quotient (`divwu. r17,r3,r0`) and reuses it (`addi r4,r17,0`); our source spells both the guard and the argument as the same literal `pct / 4882` at source lines 499 and 502, so MWCC CSEs them. (c) an ours-only `addi` at @0x25c -- target computes the row pointer straight into a callee-saved register (`add r18,r28,r21` then `lwz r19,16(r18)` and `lwz r3,20(r18)`) while ours computes into volatile r3 and copies (`add r3,r29,r18`, `addi r22,r3,0`). (d) The dominant remaining cost is a 12-web callee-saved ROLE PERMUTATION plus 43 lifetime permutations, reported by `savedregs.py game/pb/dbgtext.c fn_800C03E0`: r30/r29/r28/r26/r25/r24/r22/r21/r20/r18/r17/r16 all hold a different value than in the target, 6 live ranges escape to a volatile in ours, 2 volatile roles are promoted in ours, and emission order differs (target r27 r26 r28 r23 r30 r29 r31 r16 r19 r24 r20 r21 r18 r17 r22 r25 against ours r27 r28 r29 r23 r26 r31 r30 r22 r18 r19 r25 r24 r21 r20 r16 r17). savedregs classes that as a DECLARATION-LIST fact; it is a multi-run job on the declaration list at source lines 292-304, not a scheduling one. DISPATCH CORRECTION: the work order called this function virgin; it is not -- attempt.DB_fn-800c03e0-structural-decomposition.20260901.v1 is accepted against it and was surfaced by `gdlmem context fn_800C03E0`.",
    "scope": "src/game/pb/dbgtext.c, fn_800C03E0 only, three one-token hunks. Committed as 23a0f848e `Correct mbBlitProject argument order in fn_800C03E0 trailer loops (real 500 -> 482)`. SEMANTIC CHANGE: the callee previously received this function's two scalar arguments transposed at all three sites, so this is a behaviour correction, not only a score move. dbgtext was NOT flipped -- fn_800C03E0 remains the TU's sole blocker (11 of 12 functions at real 0) and is still 482 real from exact, so the full flip audit and finish_tu were correctly not attempted.",
    "verification": "defake_gate baseline anchored to commit c3f3aea99, source sha1 8011dcb93 (12 functions, 11 at real 0; DATA 2 non-text sections, 140/140 matched bytes, extab 56/56, extabindex 84/84). Baseline `probe.py game/pb/dbgtext.c fn_800C03E0 --ops` = real 500, FUZZY (fresh report) 94.5150%, insns T433/O433, multiset 6t, 9 clusters, RELOC-SYMBOL SCREEN clean. After the two verified sites: IMPROVED real 500 -> 488, FUZZY 94.5612%. After the third: IMPROVED real 488 -> 482, FUZZY 94.5843%. Count parity T433/O433 held at every step, so each keep was arbitrated on fresh fuzzy per the PARITY-HELD banner. `defake_gate.py check game/pb/dbgtext.c --rebuild` = `IMPROVED fn_800C03E0 real 500 -> 482` / `GATE OK (1 improved)`, exit 0, anchored baseline c3f3aea99 against HEAD 23a0f848e. `datadiff.py game/pb/dbgtext.c --sections` returns zero rows, exit 0 -- no data section moved by the keep. `git diff --check HEAD` exit 0 and `git diff --cached --check` exit 0 before committing. Full `ninja -j2` prints `build/GUNE5D/main.dol: OK`; PROGRESS after the keep reads All 98.53% fuzzy / 68.17% matched, Postprocessor split STRICT 57.01% (2589 fns) + EQUIVALENT 11.16% (153 fns) -- unchanged, as expected for a fuzzy gain inside a still-NonMatching function. `python -m unittest discover tools/gdl/tests -b` = Ran 1577 tests, OK (skipped=2). `python -m memory_graph.test_graph --changed` = SKIP, no changed path under the four measured roots."
  },
  "function": "function:fn_800C03E0",
  "held_fixed": "The argument-order correction changed ONLY the two scalar arguments of mbBlitProject at the three trailer-loop call sites; every loop bound, every other call, every declaration and the whole control-flow shape of fn_800C03E0 were held constant, and the 11 sibling functions in dbgtext stayed at real 0 throughout. The two reverted probes each varied exactly one thing against that kept state: the textX probe varied ONLY whether the second argument of dbgTextPrintfPx in the `lbl_8034475C == 5` loop is the literal 0 or `textX * 8` with textX hoisted into the for-init, holding the loop bound, the body and every other block fixed; the loop-init probe varied ONLY whether `i`'s zero-initialisation sits inside the for-init comma expression or in a preceding statement, at both `k < 6` trailer loops, holding the loop bounds and bodies fixed.",
  "hypothesis": {
    "cheapest_refuting_observation": "Grep every mbBlitProject call site image-wide and read each one's two argument registers off the target with `fnasm <unit> <fn> <range> --diff`. If no other site shows target r4/r5 holding our r5/r4 values, the class is exactly these three sites and the generalisation is wrong. Not run this run -- only the three sites inside fn_800C03E0 were checked, and all three were transposed.",
    "screened_against_target": "yes -- the transposition was read directly from the target at two independent sites before any edit (`@0x394 addi r5,r19,0` with `@0x398 li r4,2` against our `@0x394 addi r4,r19,0` / `@0x39c li r5,2`, and `@0x670 addi r5,r19,0` with `@0x674 li r4,2` against our `@0x670 addi r4,r19,0` / `@0x674 li r5,2`), and the third site was confirmed by measurement (real 488 -> 482 with fuzzy rising). The IMAGE-WIDE generalisation in the statement above is NOT screened.",
    "statement": "The transposed-argument class is not exhausted by the three sites fixed here. mbBlitProject's two scalar arguments were transposed at every `mbBlitProject(quad, j, 2)` site and correct at every `mbBlitProject(quad, <expr>, 4)` site, which means the reconstruction transposed them wherever the SECOND argument is a small literal and the third is a variable. dbgtext's sibling TUs call the same mb_blit entry points (MBNewTempQuad / mbBlitCalcWidth / mbBlitProject / MBBlitSetColor appear together in pb_diag.c and the mb_* family), so the next lane should run the same aligned-view check on every mbBlitProject call site image-wide before doing any register work, because a transposed argument reads as allocator churn in `real`, in the opcode multiset and in fuzzy alike."
  },
  "id": "attempt.FT_fn800c03e0-mbblitproject-arguments-were-transposed-at-three-trailer-loops.20260903.v1",
  "kind": "attempt",
  "outcome": "improved",
  "recorded_at": "2026-09-03T19:49:40Z",
  "residual": {
    "capability_needed": "consistent-recolor over a 12-web callee-saved permutation",
    "family": "regalloc-web",
    "measured_at": "2026-09-03",
    "signature": "opcode multiset: DIFFERS target-only: +1 divwu +1 li +1 slwi ours-only: -3 addi; insns T433/O433; 9 ops clusters"
  },
  "residual_class": "MIXED",
  "schema_version": 1,
  "valid_from": "2026-09-03"
}
