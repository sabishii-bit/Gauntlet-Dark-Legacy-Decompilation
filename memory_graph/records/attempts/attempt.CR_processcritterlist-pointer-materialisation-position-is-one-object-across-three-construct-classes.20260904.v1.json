{
  "attempted_axis": "First SOURCE work on a function whose two prior records are both screen-only (neither spent a probe): re-derive the residual from the aligned view, then move the `player = gPlayers` materialisation out of the for-init by three distinct construct classes.",
  "attributes": {
    "law_screen": "claim.law.live-zero-copy-vs-remat-is-allocator-not-source.20260831.v1 SCREENED and it still governs the SURVIVING cluster (`li r3,0` vs `addi r3,r6,0`) -- not overturned. claim.law.constant-copy-vs-rematerialize-is-source-unreachable.20260831.v1 SCREENED, same cluster, same verdict. claim.law.point-of-use-single-use-pointer-delay.20260830.v1 SCREENED and APPLIED IN ITS CONVERSE DIRECTION: the law describes an EAGER initializer paying a copy after stmw; here ours materialised the address LATE and paid the copy anyway, and its companion (multi-use address hoisted to a persistent register) is the direction that moved this function. claim.law.pointer-decl-order-flips-addrlo-inplace-vs-fresh-destination.20260901.v1 SCREENED: its DIAGNOSIS transfers exactly (ours `addi r4,r4,0 @gPlayers(ADDR16_LO)` is the IN_PLACE shape, target `addi r5,r3,0` is FRESH, and the target reuses the freed @ha temp as the loop counter) but its CURE is already satisfied -- `player` is already FIRST in the declaration list, ahead of the int counter, which is the law's stated winning order. claim.law.webfrank-cannot-close-instruction-count-deltas.20260831.v1 SCREENED and DECISIVE: T49/O50 is count-asymmetric. claim.law.regnorm-positional-pairing-fabricates-displacement-rows.20260831.v1 APPLIED to REFUTE the superseded record's structural count (see attributes.residual). claim.law.fuzzy-can-underweight-a-real-improvement SCREENED in the opposite direction -- here fuzzy OVER-weighted relative to real and the aligned-view read settled it. EAGER-PRE-PROLOGUE ADDR16_LO class (attempt.NC_mbox-loadmodelfixed-is-the-fourth-eager-pre-prologue-instance-and-the-strs-alias-is-load-bearing.20260903.v1) SCREENED and MEASURED NOT APPLICABLE: that class is 'ours completes the LO early and pays a copy'; at HEAD ours completed the LO LATE (@0x30) and paid the copy anyway, which is the mirror image, so the class's typed cap does not reach this function. Pin screen: config/GUNE5D/webfrank.json DOES carry pins on game/enemy/critter (8 of them) but none on ProcessCritterList; the edit changed no instruction COUNT (T49/O50 before and after) so no pinned window's relocation addends moved, and the full ninja replayed every rule green.",
    "laws_applied": "[\"claim.law.point-of-use-single-use-pointer-delay.20260830.v1\", \"claim.law.regnorm-positional-pairing-fabricates-displacement-rows.20260831.v1\", \"claim.law.webfrank-cannot-close-instruction-count-deltas.20260831.v1\"]",
    "probed_form": "KEPT FORM (commit d5ea67b0e), one line moved: `activePlayers = 0; total = 0; player = gPlayers; lbl_80344664++; for (i = 0; i < 4; i++, player++) {` replacing `activePlayers = 0; total = 0; lbl_80344664++; for (player = gPlayers, i = 0; i < 4; i++, player++) {`. PROBE 2 (identical object): the same plus `i = 0;` hoisted, `for (; i < 4; i++, player++)`. PROBE 3 (REGRESSED T49/O52): `i = activePlayers = 0; total = 0; player = gPlayers; lbl_80344664++; for (; i < 4; i++, player++)`. PROBE 4 (identical object): `Player *player = gPlayers;` as a declaration initializer with the for-init restored to `for (i = 0; ...)`.",
    "residual": "TWO REFUTATIONS AND ONE SURVIVING WORD. (1) attempt.mp-label-correction.processcritterlist.20260831.v1 recorded `5 STRUCTURAL rows ... 5 CFG-BRANCH-TARGET ... READING: branch-target (if/else vs goto SHAPE, source-reachable)`. Measured false: all five rows are `T bne <fn+0x54> / O bne <fn+0x58>`-shaped, i.e. every one of our branch targets is exactly +4, which is the signature of ONE extra instruction earlier in the function, not of five structural rows. Both `probe.py --ops` (`5 further same-opcode row(s) differ only in a normalized BRANCH TARGET ... those ARE explainable as downstream drift`) and `regnorm --map` (`STRUCT-BRANCH-DISP ... [displacement-only + count delta -> alignment artifact, usually not real]`) now label them artifacts. The record's own residual_class STRUCTURAL rested entirely on those five rows. (2) The gate's `genuine structural rows 0 -> 2 ROSE -- structure agrees with real` banner is an ARTIFACT OF IMPROVED ALIGNMENT, not a regression: at HEAD the two instructions in question (`li r6,0` and the gPlayers ADDR16_LO `addi`) were UNPAIRED, so regnorm could not score them at all; in the keep state they PAIR and are correctly reported as 2 genuine rows. Unpaired fell 9 -> 3 and paired rose 57 -> 60 over the same edit, so strictly MORE of the stream is now accounted for. SURVIVING: one instruction, `li r3,0` @0x44 in ours against `addi r3,r6,0` @0x24 in the target -- the live-zero copy-vs-remat class, with both governing laws screened and still standing.",
    "scope": "src/game/enemy/critter.c::ProcessCritterList only, commit d5ea67b0e, 2 insertions / 1 deletion. No header, config, webfrank.json or symbols.txt change. The other 85 functions in the TU were unmoved.",
    "verification": "Clean-state BASELINE banked before the first edit (first-baseline trap honoured): `probe.py game/enemy/critter ProcessCritterList --ops` = real 25, T49/O50, multiset 1t, FUZZY (fresh report) 84.0816%, 6 clusters. KEEP: real 29, T49/O50, multiset 1t, FUZZY (fresh report) 94.5918%. THE METRICS DISAGREED and were arbitrated in ONE call: `probe.py ... --arbitrate` printed `BANKED real 25 fuzzy 84.0816% / CURRENT real 29 fuzzy 94.5918% / DELTA real +4 fuzzy +10.5102 [= +0.0019 PROJECT-WEIGHTED] / ARBITER: fuzzy ROSE -- KEEP the current state`, both numbers from fresh report builds. The keep was then confirmed on an ALIGNMENT-INDEPENDENT count, measured live at the same commit on both states with `regnorm.py game/enemy/critter ProcessCritterList --map`: HEAD `57 paired, 4 renaming, 5 STRUCTURAL (0 genuine), 9 unpaired (2 displaced)` with THREE ours-only unmatched instructions (`addi r4,r3,0`@0x30, `addi r5,r4,0`@0x40, `li r3,0`@0x44); KEEP `60 paired, 7 renaming, 7 STRUCTURAL (2 genuine), 3 unpaired` with ONE (`li r3,0`@0x44). `defake_gate.py check --rebuild` reported `REGRESSION ProcessCritterList real 25 -> 29 (genuine structural rows 0 -> 2 ROSE -- structure agrees with real)`; that banner is WRONG here and the regnorm rows above are why (see attributes.residual). Full `ninja -j2` printed `build/GUNE5D/main.dol: OK`; `python -m unittest discover tools/gdl/tests -b` = `Ran 1829 tests ... OK (skipped=2)`; `python -m memory_graph.test_graph --changed -b` = `Ran 440 tests ... OK`. `git diff --check HEAD` and `git diff --cached --check` both exit 0. `datadiff.py game/enemy/critter --sections` shows extab 584 B at 98.3108% unmoved (the edit is count-neutral)."
  },
  "denial": {
    "expiry_check": "python tools/gdl/fndiff.py game/enemy/critter ProcessCritterList --ops -- re-probe these classes only if the cluster list stops being the three rows `replace T[4:5]@10-14=['li'] O=['addi']`, `replace T[9:10]@24-28=['addi'] O=['li']`, `insert O[17:18]@44-48=['li']`, or if the count delta leaves T49/O50.",
    "falsifier": "Any spelling of the pointer materialisation that produces an object DIFFERENT from the three above (they are byte-equal on every text arbiter), or any form that removes the `addi r5,r4,0` copy while holding T49/O50.",
    "premise_measurement": "Three construct classes produced ONE object. `probe.py game/enemy/critter ProcessCritterList --ops` at head 0f4151839: baseline `BASELINE real 25 (insns T49/O50, multiset 1t)`, `FUZZY (fresh report): 84.0816%`, 6 clusters. Probe (1) statement-before: `real 25 -> 29`, 3 clusters. Probe (2) `i = 0` also hoisted, `for (; i < 4; ...)`: `real 29 -> 29`, cluster list IDENTICAL to (1). Probe (4) `Player *player = gPlayers;` declaration initializer: `real 35 -> 29`, cluster list IDENTICAL to (1) and (2). Probe (3) chained `i = activePlayers = 0`: `REGRESSED real 29 -> 35 (insns T49/O52, multiset 5t)` \u2014 it destroyed the counted (ctr) loop, adding slwi/bge/cmpwi/subfic.",
    "scope": "ONE axis on ProcessCritterList: the POSITION and FORM of the `player = gPlayers` materialisation (statement-before, for-init-hoisted, declaration-initializer). It does NOT deny the zero copy-vs-remat cluster, and it does NOT deny loop-shape or addressing levers on `gBig.scratch[i]`."
  },
  "function": "function:ProcessCritterList",
  "held_fixed": "Every probe held the 49-instruction loop body, both loop bounds, the `lbl_80344664++` statement and all four declarations' TYPES constant, and varied exactly one dimension of WHERE/HOW `player = gPlayers` is expressed: (1) a statement before `lbl_80344664++`, (2) additionally `i = 0` hoisted out of the for-init, (3) chained `i = activePlayers = 0`, (4) a declaration initializer `Player *player = gPlayers;`.",
  "hypothesis": {
    "cheapest_refuting_observation": "Zero builds: `python tools/gdl/fnasm.py game/enemy/critter ProcessCritterList 0x24:0x2c --diff`. If the target's `addi r3,r6,0` does not read r6 (the `li r6,0` at 0x10 = activePlayers), then it is not a copy of the activePlayers zero and the whole zero-coalescing framing is wrong; write the final park instead of probing.",
    "count_consequence": "Instruction counts 50/49 (ours/target), measured this session and unchanged by the keep. The cure must be count-NEGATIVE by exactly one, taking 50/49 to 49/49. Until then this function is WebFrank-INELIGIBLE by claim.law.webfrank-cannot-close-instruction-count-deltas.20260831.v1, and `wf_word_diff.py` refuses it outright: `ProcessCritterList: count-asymmetric (50 vs 49 insns) -- outside every postprocessor class by construction`. This record therefore reclassifies the function AWAY from the postprocessor path, not into it.",
    "screened_against_target": "yes -- the whole 53-row aligned view `fnasm game/enemy/critter ProcessCritterList 0x0:0xc8 --diff` was read at both HEAD and the keep state, and every register quoted above is quoted at its own definition offset in both streams. NOT screened: no form that changes the indexing of `gBig.scratch[i]` has been compiled.",
    "statement": "The surviving residual is ONE extra instruction and it is the ZERO REMAT, not the pointer: ours emits `@0x44 li r3,0` where the target emits `@0x24 addi r3,r6,0`, copying the live `activePlayers` zero into the strength-reduced `gBig.scratch[i]` byte offset. Every register named here is quoted at its own defining instruction in BOTH streams, read from `fnasm game/enemy/critter ProcessCritterList 0x0:0xc8 --diff` at the keep state. activePlayers: target `@0x10 li r6,0`, ours `@0x24 li r6,0`. player: target `@0x1c addi r5,r3,0  @gPlayers(ADDR16_LO)`, ours `@0x1c addi r5,r4,0`. i (the byte offset): target `@0x24 addi r3,r6,0`, ours `@0x44 li r3,0`. total: target `@0x28 li r28,0`, ours `@0x28 li r28,0`. The HOMES therefore already agree function-wide -- regnorm's register map is `r0->r0x1, r3->r4x6, r4->r3x2, r5->r5x1`, a bare r3/r4 transposition, and r0 is the gBig base in both streams (target `@0x40 addi r0,r4,0  @gBig(ADDR16_LO)`, ours `@0x40 addi r0,r3,0  @gBig(ADDR16_LO)`, consumed by the identical `@0x54 add r4,r0,r3`) -- so nothing about allocation is left to steer. The next lane should attack the `gBig.scratch[i]` ADDRESSING rather than the initialisation: the target reuses the FREED gPlayers @ha temp -- the r3 defined by `@0x04 lis r3,0  @gPlayers(ADDR16_HA)` -- as the offset induction variable, which is the canonical shape named in claim.law.pointer-decl-order-flips-addrlo-inplace-vs-fresh-destination.20260901.v1 case (b), and no probe this pass changed how that array is indexed."
  },
  "id": "attempt.CR_processcritterlist-pointer-materialisation-position-is-one-object-across-three-construct-classes.20260904.v1",
  "kind": "attempt",
  "outcome": "improved",
  "recorded_at": "2026-09-04T01:25:47Z",
  "refutes": "attempt.mp-label-correction.processcritterlist.20260831.v1",
  "residual": {
    "capability_needed": null,
    "family": "live-zero-remat",
    "measured_at": "2026-09-04",
    "signature": "DIFFERS target-only: (none) ours-only: -1 li; insns T49/O50; 3 ops clusters"
  },
  "residual_class": "STRUCTURAL",
  "schema_version": 1,
  "valid_from": "2026-09-04"
}
