{
  "attempted_axis": "EnemyStartMissile: execute the superseded park's own unpark condition (a form where the speed product has two live consumers so coalescing is illegal) across four statement shapes, and settle whether the WebFrank half of that condition is reachable",
  "attributes": {
    "law_screen": "claim.law.GW_block-scope-splits-a-coalesced-copy-but-a-same-scope-copy-folds.20260901.v1 APPLIED and its BOUNDARY EXTENDED (see probed_form B): the law's positive case pairs a block-local confined to one arm of an if/else with a join-live variable; here the def and the copy are STRAIGHT-LINE with no CFG join between them, and block scope alone was REGRESSED-not-neutral. This sharpens the law's stated discriminator: block scope is necessary but NOT sufficient - a control-flow join between the def and the copy is the operative half. claim.law.chained-init-constant-share.20260829.v1 SCREENED (it is the lever the superseded record already read backwards); not re-probed. claim.law.single-use-field-pointer-alias-is-folded-away.20260831.v1 SCREENED and CONFIRMS probes B and C: a staging local with one use folds into that use regardless of scope. claim.law.DC_copy-form-class-is-directional-and-its-inverse-population-is-unserved.20260901.v1 SCREENED: this is the unserved inverse direction (target has the copy, ours is count-short). Core-screen defake laws screened, none applicable: no raw-offset conversion attempted.",
    "laws_applied": "[\"claim.law.GW_block-scope-splits-a-coalesced-copy-but-a-same-scope-copy-folds.20260901.v1\"]",
    "probed_form": "KEPT (real 143 -> 131, fresh fuzzy 99.2096 -> 99.3815, commit 4a3131f74). Replaced the three-statement form `speed = desc->speed * PF(gCurLevel, offsetof(level_data, ene_mspeed), f32); dir[0] = launchPos[0] - target[0]; invSpeed = (f32)(lbl_80346318 / (f64)speed);` with an ASSIGNMENT-EXPRESSION form that also moves the product ahead of dir[0]: `invSpeed = (f32)(lbl_80346318 / (f64)(speed = desc->speed * PF(gCurLevel, offsetof(level_data, ene_mspeed), f32))); dir[0] = launchPos[0] - target[0];`. This did NOT create the copy but it made the whole surrounding FPR colouring exact: the region 0x8c-0xb8 is now byte-identical except the three words named in `residual`, where before it also differed at 0x94/0x98/0x9c/0xa4/0xa8/0xb0 (lfs/lfd destinations f1/f0/f2 now match the target instead of f2/f1/f0). regnorm renaming rows for the function fell 21 -> 15. THREE NEGATIVE PROBES on the surplus-fmr cluster itself, all leaving the cluster BIT-FOR-BIT unchanged (`delete T[45:46]@b4-b8=['fmr']`), each reverted: (B) BLOCK-SCOPED staging local - `{ f32 prod = desc->speed * PF(gCurLevel, offsetof(level_data, ene_mspeed), f32); invSpeed = (f32)(lbl_80346318 / (f64)prod); dir[0] = launchPos[0] - target[0]; speed = prod; }` = REGRESSED real 131 -> 143. This is NOT a repeat of the superseded record's probe B, which used a FUNCTION-scope `prod`; scope was the varied axis and it is now measured dead in both settings. (C) CSE-with-register-locals, written to remove the alias hazard the superseded record blamed for probe A's failure - `{ f32 baseSpeed = desc->speed; f32 mspeed = PF(gCurLevel, offsetof(level_data, ene_mspeed), f32); invSpeed = (f32)(lbl_80346318 / (f64)(baseSpeed * mspeed)); dir[0] = launchPos[0] - target[0]; speed = baseSpeed * mspeed; }` = REGRESSED real 131 -> 265 (a colouring cascade). (D) ADJACENT repeated spellings with NO intervening store, the one variant the superseded record's probe A did not test (its two spellings straddled the `dir[0]` store) - `invSpeed = (f32)(lbl_80346318 / (f64)(desc->speed * PF(gCurLevel, offsetof(level_data, ene_mspeed), f32))); speed = desc->speed * PF(gCurLevel, offsetof(level_data, ene_mspeed), f32); dir[0] = launchPos[0] - target[0];` = REGRESSED real 131 -> 143, cluster unchanged. Adjacency is therefore NOT the barrier: MWCC folds the second spelling into speed's home whether or not a store intervenes.",
    "residual": "ONE instruction, fully named and unchanged by six distinct source forms across two sessions. Target: `fmuls f3,f1,f0` at +0xa0 into a caller-saved scratch, `fdiv f30,f2,f3` at +0xac, then `fmr f31,f3` at +0xb4 copying the product into speed's callee-saved home. Ours: `fmuls f31,f1,f0` straight into the home and `fdiv f30,f2,f31`. Every other word in 0x8c-0xb8 now matches. MECHANISM: MWCC targets the assignment destination's home register with the producing instruction whenever the value's only consumers are that assignment and expressions dominated by it; no source spelling reaches the allocator's choice of the fmuls destination. REFUTATION OF THE SUPERSEDED UNPARK CONDITION: that record offered 'or WebFrank treatment' as the alternative unpark route. That half is now DISPROVED, not merely untried - the residual is COUNT-ASYMMETRIC at 291/290 (target longer by one instruction), and per the AGENTS.md postprocessor-class gate a count-asymmetric residual is provably outside EVERY postprocessor class, all of which are instruction-count-preserving. The only remaining route is a source form that denies MWCC the home register at the multiply, which six forms have now failed to find.",
    "scope": "src/game/game/combat.c, EnemyStartMissile only; one statement-shape matching edit retained. No header touched. No pool literal added or removed, so no WebFrank pin required re-derivation.",
    "verification": "probe.py against a clean banked BASELINE (real 143) taken BEFORE the first edit. Best real 131, counts held at T291/O290 throughout, so the keep is parity-held and was arbitrated on a FRESH report fuzzy per AGENTS.md discipline 9c: 99.2096 -> 99.3815, both metrics improving, no conflict. defake_gate.py check game/game/combat --rebuild = GATE OK (1 improved, no sibling regression) then --update-improved. Full `ninja -j2` = 'build/GUNE5D/main.dol: OK' with all six combat.c WebFrank pins replaying clean. All three negative probes reverted via probe.py --revert; final `git status --short` empty. Committed 4a3131f74."
  },
  "function": "function:EnemyStartMissile",
  "held_fixed": "The `speed`/`invSpeed` DECLARATION ORDER and their callee-saved homes (f31/f30) were held CONSTANT across all four probes below; only the STATEMENT SHAPE of the speed-product/invSpeed pair was varied. Also held constant: every other statement in the function, and the pool population (no literal was added or removed, so none of the TU's 6 WebFrank pins needed re-derivation).",
  "id": "attempt.CB_enemystartmissile-assignment-expression-colouring-and-fmr-axis-close.20260901.v1",
  "kind": "attempt",
  "outcome": "improved",
  "recorded_at": "2026-09-01T21:58:13Z",
  "residual": {
    "capability_needed": null,
    "family": "copy-form",
    "measured_at": "2026-09-01",
    "signature": "DIFFERS target-only: +1 fmr ours-only: (none); insns T291/O290; 1 ops clusters"
  },
  "residual_class": "STRUCTURAL",
  "schema_version": 1,
  "supersedes": "attempt.enemystartmissile-product-copy-fmr-park.20260831.v1",
  "valid_from": "2026-09-01"
}
