{
  "attempted_axis": "Test the already-loaded s32 local instead of re-reading the s16 struct field. Our source did `coltype = data->coltype;` and then `if (data->coltype == 0)`, re-reading the field; MWCC serviced the retest from the CSE'd lha result but re-normalised it with a redundant `extsh.`, which the target does not have because it tests the int local with `cmpwi`. Changing the guard to `if (coltype == 0)` dropped the opcode multiset from 4 tokens to 1 and the gate's genuine structural rows from 25 to 17, leaving a single target-only `mr` as the only opcode difference in a 661-instruction function.",
  "attributes": {
    "law_screen": "claim.law.GT_narrow-store-sign-extension-needs-an-int-variable-transit.20260902.v1 screened and APPLIED IN ITS INVERSE DIRECTION: the law says a narrowing that transits an int variable materialises the sign-extension, and the corollary used here is that re-reading a narrow FIELD when an int local already holds the value materialises a redundant one. claim.law.declared-alias-vs-repeated-cast.20260830.v1 screened and APPLIED: this is the value-side analogue of its address-side rule -- prefer the declared local over the repeated field read. claim.law.local-alias-vs-direct-global-spelling-diverges.20260831.v1 screened, not applicable: the base here is a parameter-derived pointer, not an SDA global. claim.law.lwzu-idiom-web-retention.20260830.v1 screened, not applicable: no load-update idiom at this site.",
    "laws_applied": "[\"claim.law.GT_narrow-store-sign-extension-needs-an-int-variable-transit.20260902.v1\", \"claim.law.declared-alias-vs-repeated-cast.20260830.v1\"]",
    "pin_interaction": "This edit renumbered the TU's anonymous literal pool and therefore invalidated the DOWNSTREAM webfrank instruction_permutation pin on fn_8005FDA8, aborting the build with `instruction permutation relocation input hash changed`. Handled per AGENTS.md 15b, not parked: tools/gdl/composed_census/wf_rederive_pin.py game/world/gauntworld fn_8005FDA8 reported ALL FOUR body and region hashes `(unchanged)` -- which is the audit -- and only the two relocation hashes moved, exactly the signature claim.law.WF_a-pool-fix-renumbers-relocation-hashes-not-body-hashes.20260901.v1 describes. The two relocation hashes were pasted back by surgical text edit. The sibling pin on fn_8005FB48 needed no change and re-applied cleanly (27 atoms adjusted).",
    "probed_form": "KEPT: `if (coltype == 0) { return -1.0f; }` replacing `if (data->coltype == 0) { return -1.0f; }`. NEUTRAL-IDENTICAL (reverted): the assignment-in-condition spelling `R = data->radius; if ((coltype = data->coltype) == 0) { return -1.0f; }`, tried to buy the remaining target-only `mr` -- it produced byte-for-byte the same object as the plain form (real 471, multiset 1t, same three clusters), so MWCC folds the assignment-in-condition into the same code here and that axis is dead for this row.",
    "residual": "ONE opcode row remains in 661 instructions: target-only `mr` at T[36:37]@0x90, where the target keeps the lha result in the callee-saved r29 with an explicit copy (`cmpwi r0,0; mr r29,r0`) and ours allocates the value directly with no copy, leaving ours one instruction SHORT at 660 against 661. The assignment-in-condition idiom, which is the standard source-level producer of that copy, is measured dead above. Also still open and NOT probed: an aliased-base family visible only in the IMMEDIATE rows, where the target addresses the info struct directly (`lfs f29,12(r4)`, `lfs f1,16(r4)`, `lfs f0,4(r27)`) while ours goes through the `sub`/`data` +4 alias (`lfs f29,8(r31)`, `lfs f1,12(r31)`, `lfs f0,88(r25)`). Those cost no opcode rows but are eligibility-deciding words.",
    "scope": "src/game/world/gauntworld.c (one guard operand) and config/GUNE5D/webfrank.json (two relocation hash values on the fn_8005FDA8 pin, no structural or guard change).",
    "verification": "Arbitrated on fresh fuzzy with BOTH halves measured live by the same tool after restoring source and pin together: baseline (multiset 4t, T661/O661) 98.9153%, kept state (multiset 1t, T661/O660) 98.9395%. python tools/gdl/defake_gate.py check game/world/gauntworld.c --rebuild independently reported CONFLICT with `genuine structural rows 25 -> 17 FELL`, accepted with --arbitrate --update-improved -> GATE OK. `real` INVERTED on this keep (210 -> 471) and was correctly overruled. ninja -j2 -> build/GUNE5D/main.dol: OK. Committed as 02f45249f."
  },
  "function": "function:fn_8005F0F4",
  "held_fixed": "Only the guard's operand changed. The `coltype = data->coltype;` assignment, its position, and the `R = data->radius;` line immediately after it were left byte-identical, so the closed `extsh.`/`addi` row is attributable to the retest alone and not to any reordering. The `sub`/`data` alias locals and the aliased-base lfs rows (target `lfs f29,12(r4)` against our `lfs f29,8(r31)`) were deliberately NOT touched.",
  "hypothesis": {
    "cheapest_refuting_observation": "One probe: grep fn_8005F0F4 for every later `data->coltype` or equivalent read, respell one as `coltype`, and re-read `python tools/gdl/fndiff.py game/world/gauntworld.c fn_8005F0F4 --ops`. If the target-only `mr` at T[36:37] survives, the copy is allocator-assigned and this function is a one-row REGISTER_ONLY park.",
    "screened_against_target": "yes -- python tools/gdl/fnasm.py game/world/gauntworld.c fn_8005F0F4 0x78:0xa4 --diff shows the target's `cmpwi r0,0` followed by `mr r29,r0` at +0x8c/+0x90, i.e. the tested value being copied into a callee-saved register, against our single-instruction allocation.",
    "statement": "Buy the last target-only `mr` at T[36:37] by giving `coltype` a live range that survives a call or a pressure point, so MWCC is forced to home it in a callee-saved register with an explicit copy rather than allocating the lha destination directly. The assignment-in-condition idiom is already measured dead here, so the next form to try is a SECOND use of `coltype` placed after the first call in the function -- if the value is already read again later, spelling that later read as `coltype` instead of re-reading `data->coltype` should both close this row and remove another redundant field read."
  },
  "id": "attempt.GT_fn8005f0f4-field-retest-forces-a-redundant-extsh.20260902.v1",
  "kind": "attempt",
  "outcome": "improved",
  "recorded_at": "2026-09-02T01:40:11Z",
  "residual": {
    "capability_needed": "register-pressure-equivalence",
    "family": "copy-form",
    "measured_at": "2026-09-02",
    "signature": "DIFFERS target-only: +1 mr ours-only: (none); insns T661/O660; 3 ops clusters"
  },
  "residual_class": "REGISTER_ONLY",
  "schema_version": 1,
  "supersedes": "attempt.GW_fn8005f0f4-missing-radius-variable-and-frame-reseat.20260901.v1",
  "valid_from": "2026-09-02"
}
