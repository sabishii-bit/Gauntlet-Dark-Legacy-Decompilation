[project]
name = "gdl-memory-mcp"
version = "0.1.0"
description = "Local MCP adapter for the GDL project memory graph"
requires-python = ">=3.10"
dependencies = [
    "mcp>=2,<3",
]

[tool.uv]
package = false
