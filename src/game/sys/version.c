/* VERSION.OBJ: writable build-version text, shared by boot, credits and errors.
 * Xbox names the independent char[4] object Version; the GC references point
 * to "3.5" and its terminator at 0x80126A98. Following alignment is not data. */
char Version[] = "3.5";
