/* DBMODES.OBJ: these two hooks are genuinely empty in the retail game.
 * Xbox's module records and the named PS2 implementations identify them;
 * the earlier/later calls in main distinguish Check from Do. */

/* 0x80042394 */
void DoFingerLoModes(void) {}

/* 0x80042398 */
void CheckFingerLoModes(void) {}
